---
id: "MVP-999"
tipo: epica
titulo: "Pendientes transversales y diferidos"
estado: borrador
prioridad: media
hito: "Hito Z — Cierre de pendientes transversales"
tickets: []
historias: []
depende_de: []
bloquea: []
relacionado_con: ["MVP-008"]
responsable: "@andres"
revisores: []
ai_context:
  dominios: ["gobernanza", "priorizacion", "calidad"]
  modulo_path: "03-modulos/"
  componentes: ["backlog", "triage", "planificacion"]
  etiquetas: ["mvp", "pendientes", "transversal"]
  nivel_riesgo: medio
creado_en: "2026-07-24"
actualizado_en: "2026-08-10"
---
<!-- actualizado_en refleja la ultima anotacion en el registro de puntos (MVP-809: cierre de P-112 y P-114, notas en P-069 y P-111, y alta de P-119 a P-122 desde el repaso de los 47 requisitos de usuario). -->

# EPICA MVP-999 — Pendientes transversales y diferidos

## Contexto

Durante el desarrollo aparecen requisitos o ajustes que no encajan de forma clara en las epicas activas, o que no bloquean la planificacion inmediata y conviene diferir para no frenar la entrega de valor principal.

Sin una epica explicita para estos casos, los pendientes quedan dispersos y se pierde capacidad de priorizacion transversal.

## Objetivo

Centralizar y priorizar las historias detectadas fuera del encaje natural de las epicas activas, manteniendo trazabilidad y control de alcance sin detener el desarrollo en curso.

## Requisitos de usuario de alto nivel

- **Como** Product Owner, **quiero** un contenedor unico para pendientes transversales o diferibles, **para** no bloquear la planificacion de las epicas activas.
- **Como** equipo de desarrollo, **quiero** que esos pendientes se conviertan en historias formales, **para** tratarlos con criterios de prioridad y calidad equivalentes al resto del backlog.

## Alcance

- Alta de historias que no encajan de forma clara en epicas MVP-001..MVP-006.
- Alta de historias detectadas durante revisiones de epica que pueden posponerse sin bloquear hitos activos.
- Priorizacion y secuenciacion de pendientes transversales al cierre del roadmap principal.

## Fuera de alcance

- Resolver incidencias criticas que bloqueen epicas activas: esas deben ubicarse en su epica correspondiente.
- Sustituir la refinacion normal de historias dentro de cada epica.
- Acumular trabajo indefinidamente sin decision de prioridad.

## Criterios de aceptación de la épica

- [ ] **CA-1**: Todas las historias dadas de alta en MVP-999 tienen justificacion de por que no encajan en otra epica activa.
- [ ] **CA-2**: Cada historia de MVP-999 tiene criterios de aceptación verificables y trazabilidad de origen.
- [ ] **CA-3**: El backlog de MVP-999 se revisa y prioriza de forma periodica hasta su cierre.

## Historias de esta épica

> Ver `_indice.md` para el estado actualizado.

## Registro de puntos para revision final

Usa esta seccion para anotar hallazgos durante el desarrollo de otras epicas sin crear historias todavia.
Cuando una epica cierre su `MVP-x99`, estos puntos deben revisarse, priorizarse y convertirse en historias si aplica.

| Punto | Fecha deteccion | Origen (epica/historia) | Tipo | Descripcion breve | Impacto | Bloqueante | Destino propuesto | Estado de revision | Historia creada |
|---|---|---|---|---|---|---|---|---|---|
| P-001 | 2026-07-24 | MVP-001 / MVP-103 | ux | Definir inventario y plantillas unificadas para todos los emails salientes del producto, incluyendo criterios de contenido legal (RGPD/LOPDGDD y LSSI/ePrivacy si aplica), para planificar su maquetacion coherente en un bloque transversal. **Resuelto en MVP-715**: el inventario resulto ser de **cinco** correos, no cuatro (los dos avisos de alerta de operacion tambien salen del producto), y esta en `docs/06-integraciones/correos-del-producto.md`. Los cinco se componen con una plantilla unica (`ProductEmailTemplate`) que impone cabecera, cuerpo, llamada a la accion y pie legal con responsable, motivo del envio y forma de dejar de recibirlo. | medio | no | MVP-715 | resuelto | MVP-715 |
| P-002 | 2026-07-24 | MVP-001 / MVP-104 | funcional | Administracion de miembros del Workspace: listar miembros activos y revocar acceso (transicion de `status` a `revocado`) desde la UI. El estado `revocado` del catalogo `worker_member_status` y el metodo de dominio `WorkspaceMember.Revoke()` ya estan implementados en MVP-104, pero no hay endpoint ni pantalla que los use. Queda fuera del alcance de MVP-104 ("Administracion avanzada de miembros"). Encaja en MVP-204 (maestro de trabajadores y miembros). **Resuelto en MVP-204**: `GET /workspace-members` (vista unificada de personas) + `POST /workspace-members/{userId}/revoke` con guardas CA-8 (no revocar al propietario unico ni al ultimo activo), y la vista «Miembros y accesos» en la UI. | medio | no | MVP-204 | resuelto | MVP-204 (HU-3/HU-4, CA-4/CA-7/CA-8) |
| P-003 | 2026-07-24 | MVP-001 / MVP-104 | funcional | El estado `invitado` del catalogo `worker_member_status` debe existir y usarse: debe poder verse la lista de personas que pertenecen a un Workspace con su estado (`invitado`/`activo`/`revocado`) y, para las que esten en `invitado`, poder reenviar la invitacion por email o por enlace igual que la primera vez. Confirmado como requisito (no como mero valor reservado). Encaje analizado: administracion de miembros = misma superficie que P-002, por lo que se incorpora a MVP-204 (HU-3/HU-5, CA-4/CA-5/CA-6). Nota tecnica: `workspace_members.user_id` es NOT NULL con FK a `users` y el invitado por email puede no tener cuenta aun; la representacion del estado `invitado` (vista unificada sobre `workspace_invitations` vs. fila materializada) se decide en el tech-design de MVP-204. No impacta MVP-104 (el selector solo mira membresias `activo`). **Resuelto en MVP-204**: se adopta la **vista unificada** (miembros reales union invitaciones email pendientes, `invitado`), sin materializar fila ni cambiar esquema; el reenvio (`POST /workspaces/invitations/{id}/resend`) reutiliza el emisor de MVP-103 (rota token, renueva caducidad, un solo uso) por email o por enlace. | bajo | no | MVP-204 | resuelto | MVP-204 (HU-3/HU-5, CA-4/CA-5/CA-6) |
| P-004 | 2026-07-24 | MVP-001 / MVP-102 / MVP-104 | funcional | Gestion del ciclo de vida del Workspace: hoy existe alta (`POST /api/v1/workspaces`), creacion de Workspaces **adicionales** desde la app (P-013, MVP-107) y cambio de activo (`PUT /api/v1/workspaces/active`), pero no hay plan explicito para edicion (renombrado/ajustes) ni eliminacion (baja logica o fisica) de Workspaces existentes. **Asimetria detectada en la 2a revision de MVP-199 (2026-07-26): tras P-013 el usuario puede crear Workspaces adicionales pero no renombrarlos ni eliminarlos.** Definir alcance MVP/post-MVP, reglas de seguridad (quien puede hacerlo), precondiciones (workspace activo, miembros, datos historicos) y contrato API/UI. **Decision del PO (2026-07-26): NO se resuelve en MVP-001; se traslada a la epica de maestros (MVP-002), en la superficie de administracion de MVP-204 junto a P-002/P-003.** **Replanteado (2026-07-28, durante MVP-204): NO se incluye en MVP-204.** El spec de MVP-204 no lo contempla y es un concern distinto (ciclo de vida del Workspace, con reglas de seguridad y confirmacion de borrado); mezclarlo ampliaria el alcance de un maestro de personas. Decision del PO: se **crea como historia propia** en MVP-002 (renombrar/eliminar Workspace). **Redactada como MVP-206 (2026-07-28)**, ampliada con la casuistica de propiedad aportada por el PO: un Workspace nunca queda sin propietario; propietario unico que se va -> se le pregunta traspasar (elige a quien) o dar de baja; varios propietarios -> reasignacion automatica; baja = **logica** (no fisica) + email a los miembros con enlace de solicitud de traspaso/reactivacion autorizada por quien la dio de baja; la baja de cuenta del propietario unico no deja Workspaces huerfanos. **Implementado en MVP-206**: renombrado (`PATCH /workspaces/active`, cualquier miembro por RN-034), baja logica (`deleted_at`/`deleted_by_user_id` + filtro en `IWorkspaceRepository`, sin retrabajo en los maestros), traspaso automatico y explicito, entidad `workspace_reactivation_requests` con enlace de un solo uso, reapertura por quien dio de baja y guarda de no-orfandad para la baja de cuenta. Reglas formalizadas como **RN-038/RN-039/RN-040**. | alto | si | MVP-002 / MVP-206 | resuelto | MVP-206 |
| P-005 | 2026-07-25 | MVP-001 / MVP-105 | seguridad | Headers de seguridad HTTP obligatorios por `docs/07-seguridad/autenticacion-autorizacion.md` (`Strict-Transport-Security`, `X-Content-Type-Options`, `X-Frame-Options`, `Content-Security-Policy`) no implementados en `Program.cs` y no asignados a ninguna historia. Detectado al cerrar el perimetro de seguridad en MVP-105. **Decision con PO: por ser seguro por defecto y transversal, se implementa YA en MVP-105 (`SecurityHeadersMiddleware`) en vez de diferirlo, para que todo desarrollo posterior lo herede sin retrofit.** | medio | no | MVP-105 | resuelto | MVP-105 |
| P-006 | 2026-07-25 | MVP-001 / MVP-105 | observabilidad | `X-Request-Id` en todas las respuestas, exigido por las convenciones de `docs/02-arquitectura/contratos-api.md` (trazabilidad y correlacion de errores 500), no implementado ni asignado. Transversal a toda la API. **Decision con PO: refuerza directamente la "trazabilidad minima" de MVP-105 y es seguro por defecto, se implementa YA en MVP-105 (`RequestIdMiddleware`) en vez de diferirlo.** | medio | no | MVP-105 | resuelto | MVP-105 |
| P-007 | 2026-07-25 | MVP-001 / MVP-105 | tecnico | Cliente HTTP comun en el frontend con manejo centralizado de 401/403 de scope. Hoy cada `*.service.ts` hace `fetch` a mano. Con el enforcement introducido en MVP-105, cuando lleguen recursos con ambito de Workspace conviene un cliente unico que reaccione a `AUTH_WORKSPACE_SCOPE_REQUIRED` (forzar onboarding/seleccion de Workspace) y `AUTH_WORKSPACE_FORBIDDEN` (refrescar contexto), ademas del refresh de token ya existente. **Decision con PO: se difiere a MVP-202 (primer maestro con recurso scoped), donde la UX del 403 quedara definida; construirlo ahora sin consumidor real seria una abstraccion prematura.** **Resuelto en MVP-202**: `services/http-client.ts` (`createHttpClient`) + `contexts/ApiContext.tsx` (`ApiProvider`/`useApiClient`) centralizan token y reacciones a `AUTH_UNAUTHENTICATED`/`AUTH_WORKSPACE_SCOPE_REQUIRED`/`AUTH_WORKSPACE_FORBIDDEN`. Lo consumen los recursos scoped: `plot.service` (nuevo) y `season.service` (migrado, cierra P-018). Los servicios de auth/workspace/invitation (ruta critica de login) se mantienen y se migraran de forma incremental. | bajo | no | MVP-202 | resuelto | MVP-202 |
| P-008 | 2026-07-25 | MVP-001 / MVP-106 (rev. MVP-199) | legal/ux | Paginas de **Politica de Privacidad** y **Terminos del Servicio** para el usuario final y **consentimiento de cookies**. Hoy los enlaces del login (`LoginPage.tsx:119,123`) apuntan a rutas inexistentes; MVP-106 solo corrige el comportamiento roto de los enlaces, no crea el contenido. El marco de cumplimiento existe solo como doc interno (`docs/07-seguridad/privacidad-datos.md`, RGPD/LOPDGDD, LSSI/ePrivacy). Requiere contenido legal validado. Enlazar con P-001 (emails y criterios legales). **Resuelto en MVP-505 (2026-07-31)**: Politica de Privacidad y Terminos del Servicio como paginas publicas (`/legal/privacidad`, `/legal/terminos`), enlazadas desde login, landing y Ajustes; los botones deshabilitados con «proximamente» desaparecen. El contenido describe lo que el sistema hace de verdad, con marcadores visibles para los datos del responsable (decision del PO) y aviso de documento pendiente. Sobre el **consentimiento de cookies**: el inventario (RN-042) mostro que **todas** las tecnologias son estrictamente necesarias salvo la carga de tipografias desde el CDN de Google, que se **elimina autoalojandolas** en vez de gestionarla con un banner. Sin tecnologias no esenciales, mostrar banner seria peor cumplimiento (la guia de la AEPD lo reserva para las no exentas), asi que se entrega lo que la norma si exige: informar, con un panel de privacidad en Ajustes. | medio | si | MVP-005 / MVP-505 | resuelto | MVP-505 |
| P-009 | 2026-07-25 | MVP-001 / MVP-101 (rev. MVP-199) | tecnico | Codigo muerto en el login: boton "Acceder como invitado / Demo" (`LoginPage.tsx:105-114`) que nunca se cablea (`onDemoAccess` no se pasa desde `App.tsx`). Retirar o decidir su uso. **Resuelto: retirada oportunista en MVP-106 (prop `onDemoAccess` y bloque demo eliminados de `LoginPage.tsx`).** | bajo | no | MVP-106 | resuelto | MVP-106 |
| P-010 | 2026-07-25 | MVP-001 / MVP-102 (rev. MVP-199) | ux | El asistente de creacion de Workspace muestra "Paso 1 de 3" (`CreateWorkspacePage.tsx`) pero los pasos 2-3 (temporada inicial, MVP-201) no existen aun en el build; el indicador puede confundir. Ajustar el copy/indicador mientras MVP-201 no este entregado. **Resuelto en MVP-201**: la temporada pasa a ser una oferta **cancelable** (no un paso obligado del asistente), por lo que se **retira** el contador "Paso X de Y" del alta de Workspace en vez de renumerarlo. | bajo | no | MVP-201 | resuelto | MVP-201 |
| P-011 | 2026-07-26 | MVP-001 / MVP-107 | ux/tecnico | La bandeja/campanita de invitaciones recibidas (MVP-107) se refresca solo al montar la sesion: una invitacion recibida mientras el usuario ya esta dentro no aparece hasta recargar o re-loguear. El tiempo real y los canales externos estan fuera de alcance de MVP-107. Evaluar un refresco ligero al recuperar foco de ventana y/o polling con intervalo, generalizable cuando el centro de notificaciones cubra mas tipos (RU-31). **Retriado en la revision del MVP (2026-08-10)**: pasa a `MVP-008` con alcance minimo: refrescar la bandeja al recuperar el foco de la ventana. El tiempo real y el centro de notificaciones completo (`RU-31`) siguen fuera. **Resuelto en `MVP-808` (2026-08-10)**: `NotificationsContext` refresca la bandeja al recuperar el foco de la ventana —escuchando `visibilitychange` y `focus`, porque ninguno cubre solo el caso— con un **intervalo minimo de 30 s** entre refrescos. No hay polling ni conexion persistente: el disparador es que la persona vuelva. Evidencia: `NotificationsContext.test.tsx`, 22 casos. `Deberia_TraerLaInvitacionNueva_Cuando_SeVuelveALaVentanaPasadoElIntervalo` comprueba que la invitacion emitida con la sesion abierta aparece sin recargar; `Deberia_NoLanzarUnaPeticionPorCadaCambioDePestana_Cuando_SeVuelveVeinteVecesSeguidas` cuenta las peticiones de **20 idas y vueltas (40 eventos)**: 1 por fuente, las de la carga inicial, y 2 y 2 tras pasar el intervalo. Falla sin el cambio: con el intervalo a 0 el mismo caso cae con «expected to be called 1 times, but got 41 times». La precision de por que aqui se acepta lo que `RN-006` descarto para el dashboard esta escrita en la propia `RN-006`. | bajo | no | MVP-008 / MVP-808 (alcance minimo, con P-029) | resuelto | MVP-808 |
| P-012 | 2026-07-26 | MVP-001 / MVP-107 | tecnico/calidad | El frontend sigue sin arnes de tests unitarios (`vitest`/`jest`), ya señalado en MVP-106. La logica añadida en MVP-107 (`NotificationsContext` con tracking de "vistas", `useInvitationActions`, mapeo de aptitud) queda cubierta solo por tipado + build + lint + QA manual. Introducir arnes minimo de frontend y cubrir esta logica de decision. Encaje analizado: candidato a MVP-501 (cobertura minima del nucleo) o historia propia si se quiere antes; se registra aqui para priorizar. **Resuelto en MVP-501 (2026-07-30)**: arnes de **Vitest + Testing Library** sobre `jsdom`, con config propia (`vitest.config.ts`) separada del build y tipos en `tsconfig.test.json`; 70 tests que cubren el cliente HTTP comun (reacciones a `AUTH_UNAUTHENTICATED`/`AUTH_WORKSPACE_SCOPE_REQUIRED`/`AUTH_WORKSPACE_FORBIDDEN`, serializacion de query y contrato de error), el `NotificationsContext` con su tracking de «vistas» en `localStorage`, los mensajes de aptitud de invitacion y el filtro de destino post-login. | medio | no | MVP-005 / MVP-501 | resuelto | MVP-501 |
| P-013 | 2026-07-26 | MVP-001 / MVP-107 | funcional/ux | Crear un Workspace **adicional** desde la app no estaba previsto en ninguna historia: MVP-102 cubre solo el **primero** ("primer acceso"; multiples explicitamente fuera de alcance) y MVP-104 solo el selector/cambio. El backend `POST /api/v1/workspaces` no restringe al primero, pero la UI solo abria el asistente en el estado "cero Workspaces" (`OnboardingRoute` redirige a `/app` si ya hay uno). Detectado al revisar el flujo durante MVP-107, que lo hizo mas visible (el invitado sin Workspace puede "crear el propio" pero quien ya tiene uno no). **Resuelto en MVP-107 por decision de producto**: accion "＋ Nuevo Workspace" en `WorkspaceSwitcher` → `/app/workspaces/new`, reutilizando `CreateWorkspacePage` en modo `additional`. Edicion/borrado siguen en P-004. | medio | no | MVP-107 | resuelto | MVP-107 |
| P-014 | 2026-07-26 | MVP-001 / MVP-107 (defecto en MVP-104) | bug/calidad | **Bug real en producción de la funcionalidad de listar/cambiar Workspaces (MVP-104).** `WorkspaceRepository.ListActiveMembershipsAsync` ordenaba con `.OrderBy(membership => membership.Name)` sobre el DTO proyectado en el `Join`; EF Core no lo sabe traducir a SQL y `GET /api/v1/workspaces` devolvía **HTTP 500** desde su entrega. El frontend lo tragaba a lista vacía, así que el selector solo mostraba el Workspace activo y parecía que los demás "desaparecían". Invisible hasta ahora porque el selector se deshabilitaba con lista vacía. **Resuelto en MVP-107**: ordenar por la columna real antes de proyectar. Brecha de testing asociada: los tests unitarios usan repositorios mockeados (NSubstitute) y no ejercitan la traducción EF; se añadió una regresión con SQLite real. La cobertura de integración contra PostgreSQL para todos los endpoints sigue pendiente en **MVP-501**. | alto | no | MVP-107 (fix) / MVP-501 (integración) | resuelto | MVP-107 |
| P-015 | 2026-07-26 | MVP-001 / MVP-107 (auditoria de fidelidad) | ux/tecnico | **Deuda transversal de sistema de diseño.** El frontend real nunca portó los fundamentos del prototipo (`prototype/terrenario-mvp`): tipografía **Inter + Plus Jakarta Sans** (clase `.font-headline`), iconografía **Material Symbols Outlined** y utilidades (`.ambient-shadow`, `.hide-scrollbar`). Sin esa base, cada pantalla improvisaba (emojis en vez de iconos, `font-bold` en vez de display). Detectado en la auditoría de fidelidad al cerrar la épica durante MVP-107. **Resuelto ahora**: fuentes + Material Symbols + utilidades portadas a `index.html`/`index.css`; emojis→iconos Material y `font-headline` aplicados en Login, onboarding, Landing (hero a 2 columnas, sin métricas inventadas por coherencia con MVP-106), selector, campanita y pantallas de invitación. La paleta de color ya se respetaba. | medio | no | MVP-107 (fix) | resuelto | MVP-107 |
| P-016 | 2026-07-26 | MVP-001 / MVP-107 (auditoria de fidelidad) | ux/tecnico | **Shell de aplicación ausente.** El área operativa era un placeholder centrado; MVP-104/107 improvisó una cabecera mínima en vez del shell del prototipo (Sidebar + TopNavbar). Detectado en la misma auditoría. **Resuelto ahora**: `AppSidebar` (marca, selector de Workspace, navegación con los 8 módulos del producto —los de épicas MVP-002..004 deshabilitados con "Pronto", sin enlaces rotos— y footer de usuario) + `AppTopbar` (título, campanita, menú móvil) + `AppLayout` con contenedor de contenido común (corrige la coherencia de tamaño/espaciado entre secciones). Las vistas operativas se encenderán en sus épicas. | medio | no | MVP-107 (fix) | resuelto | MVP-107 |
| P-017 | 2026-07-27 | MVP-002 / MVP-201 | funcional/deuda | **Generalización del maestro de temporadas (MVP-203).** MVP-201 introdujo un endpoint mínimo de creación de la primera temporada activa (`POST /api/v1/seasons`, 409 si ya hay activa), como contrapartida de la oferta cancelable. MVP-203 debe: (a) generalizar a CRUD completo (alta de varias, edición, cierre, listado y cambio de activa) reutilizando/ampliando `POST /seasons`; (b) formalizar la máquina de estados `planificada/activa/cerrada` sobre los booleanos `is_active`/`is_closed` de MVP-201 (mapeo ya propuesto en `modelo-de-datos.md`, sin cambio de esquema); (c) decidir si materializa `active_crop` (evolución por cultivo RU-18/RU-19), hoy diferido por regir RN-022 "una activa por Workspace"; (d) reevaluar el aviso de fecha fuera de rango (RN-023), fuera de alcance de MVP-201. Nota: el hueco de Workspaces preexistentes sin temporada **queda cubierto** por la oferta de MVP-201 (al activar un Workspace sin temporada se ofrece crearla), sin backfill. **Resuelto en MVP-203**: (a) CRUD completo (`GET /seasons`, `PATCH /seasons/{id}`, `POST /seasons/{id}/activate`); decisión de producto **"crear cambia la activa"** → el `409` desaparece (se retira `SeasonConflictException`/`SEASON_ALREADY_ACTIVE`). (b) `SeasonStatus` derivado, sin cambio de esquema. (c) `active_crop` **se mantiene diferido** (sigue rigiendo RN-022); reevaluar con la evolución por cultivo. (d) RN-023 **fuera de alcance**: es un aviso de las historias operativas (MVP-003/004), no del maestro; se registra su seguimiento en esas épicas. | medio | no | MVP-203 | resuelto | MVP-203 |
| P-020 | 2026-07-27 | MVP-002 / MVP-203 | doc/deuda | **Consolidación del catálogo de módulos en `docs/03-modulos/`.** La KB solo contiene `modulo-ejemplo` (plantilla, marcada para retirar al crear el primer módulo real) y el catálogo de `_vision-general.md` sigue vacío. El checklist de MVP-201 difería a MVP-203 "documentar el módulo de Temporadas en `03-modulos/`", pero MVP-202 (Terrenos) sentó el precedente de **no** crear módulo por historia y usar el `tech-design.md` como documentación de módulo. Crear un único módulo aislado (Temporadas) sería incoherente y arrastraría retirar `modulo-ejemplo` y poblar el catálogo. Propuesta: consolidar **de una vez** los módulos reales entregados (al menos Temporadas y Terrenos) en `03-modulos/`, actualizar el catálogo/mapa de `_vision-general.md` y retirar `modulo-ejemplo`, como tarea transversal de documentación. Sin impacto funcional. **Resuelto en MVP-716 (2026-08-08)**: seis fichas en `docs/03-modulos/` (`identidad-y-workspaces`, `maestros-operativos`, `diario-y-operativa`, `produccion-y-dashboard`, `plataforma-de-aplicacion`, `observabilidad`), cada una enlazando los `tech-design.md` de sus historias en vez de duplicarlos, y con una tabla de superficie entregada que las contrasta con `src/`. El reparto se verificó contra el código y salió **un módulo más** de los cinco previstos: el chasis compartido (contrato de error, concurrencia, acceso a datos, cliente HTTP, shell y presencia pública) no cabía en ninguno de los core. `_vision-general.md` queda con mapa, catálogo, trazabilidad módulo-épica y relaciones. `modulo-ejemplo` retirado; el único enlace roto que dejaba (`docs/00-meta/README.md`) apunta ya al catálogo real, y las instrucciones de `AGENTS.md` y `.github/copilot-instructions.md` se reformularon. | bajo | no | MVP-716 | resuelto | MVP-716 |
| P-021 | 2026-07-27 | MVP-002 / MVP-203 | ux/deuda | **Resumen de producción por temporada en el maestro.** El prototipo `TemporadasView` muestra "Producción total (Kg)" por campaña; MVP-203 lo **omite deliberadamente** (no inventar métricas: el dato de cosechas llega con `HARVEST` en MVP-004). Cuando exista `HARVEST`, enriquecer cada tarjeta de temporada con su producción agregada (y, en su caso, un enlace a la vista de campaña). Análogo al detalle diferido de Terrenos (P-019). Bloqueado por MVP-004. **Resuelto en MVP-403 (verificado en MVP-799, 2026-08-08)**: estaba construido y la fila nunca se cerro. La tarjeta de campana de `TemporadasView` pinta la produccion agregada (`TemporadasView.tsx`, con el comentario `P-021` en el propio render) y la trae `GET /dashboard/kg-by-season` en **una sola peticion**, no una por temporada. El checklist de `MVP-403` ya lo daba por cubierto. Solo faltaba la anotacion. | bajo | no | MVP-004 / MVP-403 | resuelto | MVP-403 |
| P-018 | 2026-07-27 | MVP-002 / MVP-201 | tecnico | `seasons` es el **primer recurso con ámbito de Workspace** consumido por el frontend (`GET/PUT /seasons/active` con `[RequireWorkspaceScope]`). Confirma la necesidad del cliente HTTP común con manejo centralizado de 401/403 de scope ya registrada en **P-007** (diferida a MVP-202). Por ahora `season.service.ts` hace `fetch` a mano como el resto; sin regresión, pero es el segundo consumidor scoped que refuerza P-007. Sin acción propia: se consolida con P-007 en MVP-202. **Resuelto en MVP-202**: `season.service.ts` migrado al cliente HTTP común junto a `plot.service.ts` (ver P-007). | bajo | no | MVP-202 | resuelto | MVP-202 |
| P-019 | 2026-07-27 | MVP-002 / MVP-202 | tecnico/deuda | **Reconciliación del ER de `PLOT` con la implementación del maestro (MVP-202)** y campos diferidos. MVP-202 divergió del ER canónico de `docs/02-arquitectura/modelo-de-datos.md` por decisión de alcance, ya corregida y documentada en el propio modelo y en el `tech-design.md`: (a) se **añadió `is_active`** (no estaba en el ER) porque CA-3 y el contrato lo exigen; (b) se usa **`location` (texto libre)** en vez de `latitude`/`longitude` (mapas/coordenadas fuera de alcance); (c) **`soil_metadata` (JSONB)** no se materializa. Pendiente de decidir en una fase posterior si se recuperan coordenadas/`soil_metadata` (y con qué historia) o si se depuran del ER canónico. Sin impacto funcional en el MVP. Relacionado: el **modal de detalle de terreno con histórico** (prototipo `TerrenoDetailModal`, cosechas/labores por parcela) se difiere porque sus datos dependen de MVP-003/MVP-004; MVP-202 entrega el maestro (listado+alta+edición+inactivación) sin esa vista de detalle. **La parte de detalle se entregó en MVP-407 (2026-07-30)**: el detalle muestra los campos reales (RN-028, no los inventados por el prototipo) y el histórico de cosechas y labores por terreno leído del diario unificado (MVP-305). **Queda pendiente solo la parte de ER** (recuperar coordenadas/`soil_metadata` o depurarlas del ER canónico), sin impacto funcional. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog. Se decide cuando exista una historia de mapas o de suelo que reclame las coordenadas o `soil_metadata`; hasta entonces el ER conserva la anotacion de la divergencia. | bajo | no | Backlog post-MVP (solo la parte de ER; el detalle se entrego en MVP-407) | backlog-post-mvp | - |
| P-022 | 2026-07-28 | MVP-002 / MVP-204 | tecnico/deuda | **`workers.user_account_id` reservado, no materializado en MVP-204.** El modelo canónico prevé vincular un trabajador a una cuenta del sistema, pero MVP-204 no lo usa: los miembros del Workspace se exponen como seleccionables desde `workspace_members` (RN-027), no como filas de `workers`, así que el trabajador nace siempre con `user_account_id = null`. La columna y la FK opcional (`ON DELETE SET NULL`) se crean igualmente para no reabrir el esquema al usarlo. Análogo a `soil_metadata`/`active_crop` (diferidos). Reevaluar si en una fase posterior se quiere convertir un trabajador sin cuenta en miembro (o vincularlo) y con qué historia. Sin impacto funcional en el MVP. **Actualizado en la 2a pasada de MVP-299 (2026-07-28)**: la columna **sí se materializa** en `MVP-208`, en el sentido miembro -> `workers` (cada miembro activo pasa a tener su fila de responsable, lo que cierra P-034). Queda pendiente solo el sentido contrario —vincular a una cuenta una fila de cuadrilla ya existente—, que sigue fuera de alcance. **Resuelto en su parte de ida (MVP-208)**: `Worker.CreateForMember` + `MemberRosterService` materializan la fila al crear el Workspace y al aceptar una invitacion, la migracion `AddMemberWorkers` hace el backfill de los miembros existentes y el indice unico parcial `ux_workers_workspace_user_account` garantiza una fila por cuenta y Workspace. El sentido inverso (convertir cuadrilla en miembro) sigue en MVP-999. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog. El escenario que dolia -dos filas para la misma persona- lo resuelve la **fusion** de `P-036`/`P-041`, que entra en `MVP-008`; vincular explicitamente una fila de cuadrilla a una cuenta es alcance distinto y sin consumidor hoy. | bajo | no | Backlog post-MVP (solo el sentido cuadrilla -> cuenta) | backlog-post-mvp | - |
| P-023 | 2026-07-28 | MVP-002 / MVP-204 | tecnico/calidad | El arnés de tests de frontend sigue sin existir (ya señalado en P-012 desde MVP-106/107). Las vistas nuevas de MVP-204 (`TrabajadoresView`, `MiembrosView`) y sus servicios añaden lógica de decisión (combinación de miembros seleccionables, gating de revocar por `can_revoke`/`is_self`, reenvío por email/enlace con copia de enlace) cubierta solo por tipado + build + lint + QA manual conducida. Se consolida con P-012 (candidato a MVP-501). Sin acción propia: refuerza P-012. **Resuelto en MVP-501 (2026-07-30)** junto a P-012: `MiembrosView` tiene cobertura específica del gating (`status` x `can_revoke` x `is_self` x `channel`), de la confirmación en dos pasos de «Retirar acceso» y «Anular invitación», y del reenvío por email frente a enlace. | medio | no | MVP-005 / MVP-501 (con P-012) | resuelto | MVP-501 |
| P-024 | 2026-07-28 | MVP-002 / MVP-206 | funcional/legal | **Baja de cuenta de usuario (RGPD / derecho de supresión) como flujo completo.** MVP-206 define e implementará solo la **regla de no-orfandad** (un propietario único que se da de baja debe resolver cada Workspace: traspaso o baja lógica), pero **no** el flujo de baja de cuenta en sí (UI de "eliminar mi cuenta", borrado/anonimización de datos personales, confirmaciones, plazos de retención, revocación de sesiones/tokens). Requiere marco legal (ver `docs/07-seguridad/privacidad-datos.md`) y encaje con P-001/P-008 (emails y páginas legales). Crear historia propia (candidata a MVP-005/MVP-502 de endurecimiento o a la épica de identidad). **Punto de enganche ya entregado en MVP-206**: `WorkspaceOwnershipGuard.EnsureAccountClosureAllowedAsync` (lanza `BUSINESS_RULE_WORKSPACE_OWNERSHIP_UNRESOLVED`) y `GET /api/v1/workspaces/ownership-obligations`; la historia de baja de cuenta debe **llamarlos**, no reimplementar la regla (RN-038). **Resuelto en MVP-505 (2026-07-31)**, respetando esa condicion: `CloseAccountHandler` llama a `EnsureAccountClosureAllowedAsync` tal cual. El modelo es **anonimizacion inmediata + purga diferida**: desaparecen ya nombre, correo e identificador de Google —en la cuenta, en los maestros de responsables (RN-036) y en las invitaciones pendientes dirigidas a su correo—, se revocan todas las sesiones y la cookie de refresco. El `google_sub` deja de coincidir con el de Google, asi que volver a entrar crea una cuenta nueva y vacia: eso separa una supresion de una desactivacion. La fila sobrevive anonimizada porque el historico operativo de terceros guarda quien lo registro. Confirmacion por frase tecleada, comprobada tambien en servidor. 12 tests de integracion contra API y base de datos reales. | medio | si | MVP-005 / MVP-505 | resuelto | MVP-505 |
| P-025 | 2026-07-28 | MVP-002 / MVP-205 | ux/deuda | **Agrupacion del menu lateral por secciones.** Con la entrada «Tareas» (MVP-205) el `AppSidebar` llega a **10 entradas, 5 de ellas maestros** (Terrenos, Temporadas, Trabajadores, Tareas, Miembros y accesos) mezcladas con la operativa (Diario, Vision General, Cosechas, Compras) y Ajustes. Hoy no rompe ningun flujo —los modulos pendientes siguen deshabilitados con «Pronto»— pero la lista plana escalara mal cuando se enciendan todos. Decision del PO (2026-07-28): **no** reestructurar el menu dentro de MVP-205 (tocaria el shell de P-016 y excede el alcance de un maestro); se registra para hacerlo de una vez cuando esten encendidos los modulos de MVP-003/MVP-004. Propuesta: secciones «Operativa» / «Maestros» / «Configuracion» sobre el shell existente. **Resuelto en MVP-406 (2026-07-30)**: `AppSidebar` agrupa las 10 entradas en esas tres secciones sobre el shell existente, ninguna suelta. | bajo | no | MVP-004 / MVP-406 | resuelto | MVP-406 |
| P-026 | 2026-07-28 | MVP-002 / MVP-205 | funcional/alcance | **La prevencion de duplicados del catalogo de tareas se adelanta de MVP-302 a MVP-205.** El alcance de `MVP-302` («Guardado de tarea libre en catalogo») incluye la «prevencion basica de duplicados evidentes dentro del mismo Workspace», pero la guarda pertenece al catalogo, no al flujo que lo alimenta: un maestro que admite «Poda» y «poda» como tareas distintas contradice RN-026, y anadir el indice unico despues obligaria a una migracion con limpieza de datos ya creados. Decision del PO (2026-07-28). **Resuelto en MVP-205**: guarda de aplicacion (`ExistsWithNameAsync` con `lower(name)`) + invariante de datos (`ux_tasks_workspace_name` UNIQUE sobre `(workspace_id, lower(name))`) + traduccion de la violacion a `409 CONFLICT_TASK_NAME_DUPLICATE`. **Accion pendiente en MVP-302**: su spec debe pasar a *reutilizar* la guarda en vez de construirla (su CA de duplicados ya queda cubierta); la normalizacion avanzada de nombres (acentos, similitud) sigue fuera de alcance en ambas. **Aplicada en la revision de cierre MVP-299 (2026-07-28, hallazgo R-14)**: el alcance de MVP-302 ya dice que reutiliza la guarda de MVP-205. **Cerrado tambien por el lado consumidor en MVP-302 (2026-07-29)**: el guardado de una tarea libre no intenta el alta para tratar el 409, sino que consulta la **misma** comparacion `lower(name)` (`ITaskRepository.FindByNameAsync`) para *resolver* el nombre: reutiliza la tarea existente o reactiva la inactivada. En el flujo de actividad un 409 no seria accionable —el usuario no tiene nada que arreglar—, asi que ese codigo queda reservado a `POST /tasks`, donde el alta si es el objetivo. La misma guarda se extiende a temporadas, trabajadores y terrenos en **MVP-207** (hallazgo R-06): **hecho**, con los indices `ux_seasons_workspace_name`, `ux_workers_workspace_name` y `ux_plots_workspace_name`, de modo que los cuatro maestros de la epica se comportan igual. | bajo | no | MVP-205 (guarda) / MVP-302 (ajuste de alcance) | resuelto | MVP-205 |
| P-027 | 2026-07-28 | MVP-002 / MVP-205 | tecnico/calidad | **Los `PATCH` de campos parciales de los maestros responden 500 ante un cuerpo JSON con bytes no UTF-8.** Detectado al probar MVP-205 con un cuerpo mal codificado: el patron comun `[FromBody] Dictionary<string, JsonElement>` acepta los bytes y revienta despues en `JsonElement.GetString()` (`Cannot transcode invalid UTF-8 JSON text`), que no esta capturado, en vez de responder `400`. Es **transversal**: mismo patron en `PlotsController` (MVP-202), `WorkersController` (MVP-204) y `TasksController` (MVP-205). Impacto real bajo (un cliente correcto siempre envia UTF-8) pero ensucia la observabilidad con 500 que no son fallos del servidor. Propuesta: capturar el fallo de transcodificacion en el borde de transporte —o mejor, en un helper comun de lectura de `PATCH` parcial— y devolver `400 VALIDATION_REQUIRED`. No se corrige en MVP-205 por afectar a tres controladores fuera de su alcance. **Ampliado en la 2a pasada de MVP-299 (2026-07-28)**: es el mismo borde de transporte que describe **P-043** (los codigos de validacion del alta), asi que ambos deben resolverse en la misma pasada y con el mismo helper comun de lectura de cuerpo. **Resuelto en MVP-502 (2026-07-31)**: helper comun `PartialUpdateBody` con la primitiva `JsonText`, que es el unico sitio donde se captura el fallo de transcodificacion, mas un `InvalidRequestBodyFilter` que lo traduce a `400 VALIDATION_FORMAT_INVALID` en el borde de transporte. **Corregido en los ocho controladores, no en los tres que nombraba este punto**: el mismo patron estaba tambien en `Seasons`, `Activities`, `Harvests`, `Purchases` y `Consumptions`, que llegaron despues de registrarse, y cerrarlo solo en tres habria sido cerrarlo de nombre. No queda ninguna llamada a `GetString()` sin red en controladores. | bajo | no | MVP-005 / MVP-502 (con P-043) | resuelto | MVP-502 |
| P-028 | 2026-07-28 | MVP-002 / MVP-205 | doc/tecnico | **Reconciliacion de la tarea en `ACTIVITY` con el catalogo.** El ER canonico declara la tarea de una actividad como un `string task` suelto, anterior al catalogo; `contratos-api.md` ya preve `task_id?` + `task_text?` (RN-025: catalogo **o** texto libre). MVP-205 anade la entidad `TASK` al ER y documenta la relacion, pero **no** toca `ACTIVITY` (no es su alcance). Quien materialice la entidad de actividad —**MVP-301**— debe cerrar como quedan los dos campos (FK opcional a `tasks` + texto libre) y actualizar el ER. Registrado solo para no perder la trazabilidad; ya tiene destino en una historia existente. **Nota (MVP-208)**: el equivalente para el *responsable* (`P-034`) ya esta cerrado desde el lado del maestro, asi que MVP-301 solo tiene que resolver los dos campos de la tarea; `worker_id` es una FK simple a `workers` para cualquier persona. **Resuelto en MVP-301 (2026-07-29)**: `ACTIVITY` materializa los **dos** campos como **excluyentes** —`task_id` FK opcional a `tasks` con `ON DELETE RESTRICT` y `task_text` acotado a 120 caracteres, la misma cota que `TASK.name` para que una tarea escrita al vuelo siempre quepa al guardarse en el catalogo (`MVP-302`)—; el agregado exige exactamente uno, porque guardar los dos permitiria que divergieran y el diario no sabria cual mostrar. El `PATCH` sustituye la pareja completa cuando llega cualquiera de los dos. ER y `contratos-api.md` §5 actualizados; la respuesta anade `task` ya resuelto (derivado, no columna). | bajo | no | MVP-301 | resuelto | MVP-301 |
| P-029 | 2026-07-28 | MVP-002 / MVP-206 | ux/funcional | **La solicitud de reactivacion de un Workspace solo se avisa por email; no hay notificacion in-app.** El centro de notificaciones (campanita, MVP-107) solo cubre invitaciones recibidas y se refresca al montar la sesion (P-011). Quien dio de baja un Workspace se entera de que tiene una decision pendiente por correo, con enlace a `/reactivations`; si el correo no llega o se pierde, la solicitud queda esperando sin señal en la aplicacion. Ademas, al solicitante no se le notifica el resultado (autorizada/denegada): lo descubre porque el Workspace reaparece —o no— en su selector. MVP-206 no lo incluye porque generalizar el centro de notificaciones excede el alcance de la historia y arrastra P-011. Propuesta: extender `NotificationsContext` a mas tipos de aviso (RU-31) cubriendo solicitudes de reactivacion pendientes de decidir y su resolucion. **Retriado en la revision del MVP (2026-08-10)**: pasa a `MVP-008` con alcance minimo, junto a `P-011`: anadir el tipo «solicitud de reactivacion pendiente» a la bandeja, para que una decision irreversible deje de depender de que llegue un correo. Notificar al solicitante el resultado y generalizar a mas tipos siguen fuera. **Resuelto en `MVP-808` (2026-08-10)**: la campanita gana el tipo «solicitud de reactivacion pendiente de decidir», con enlace a `/reactivations`, y el contador suma los dos tipos. **Sin endpoint nuevo**: se consume `GET /api/v1/workspaces/reactivations`, que existe desde `MVP-206`; lo que faltaba no era el dato, era que alguien lo mirara. El correo de `RN-040` **se sigue enviando** (`CA-5`): el aviso se suma, no sustituye, y el test `Solicitar_Deberia_ConsumirElEnlaceYAvisarAQuienDioDeBaja` queda declarado guarda de eso. Evidencia: `NotificationBell.test.tsx` (5 casos: enlace `href="/reactivations"`, `aria-label` «2 aviso(s) pendiente(s)» con un aviso de cada tipo, y ausencia de botones de decision en la campanita, porque autorizar es irreversible y se decide en la pantalla que lo explica); en `NotificationsContext.test.tsx`, la desaparicion del aviso al resolver y el fallo **independiente** de las dos fuentes —que una no pueda leerse no puede esconder a la otra—; y contra PostgreSQL real, 3 casos sobre `ListPendingAuthorizationsAsync`, uno de ellos una `[Theory]` con las dos vias de resolucion, `autorizada` y `denegada`. **Queda fuera y sigue abierto**: quien dio de baja su unico Workspace no tiene shell, asi que no ve la campanita; para esa persona el correo sigue siendo la unica via, junto al enlace a `/reactivations` que ya hay en Ajustes. Se deja constancia como **candidato a punto nuevo**, sin abrirlo: es decision del PO. | medio | no | MVP-008 / MVP-808 (alcance minimo, con P-011) | resuelto | MVP-808 |
| P-030 | 2026-07-28 | MVP-002 / MVP-206 | ux/legal | **Dos plantillas de email nuevas** (aviso de baja de Workspace con enlace de reactivacion, y aviso de solicitud a quien dio de baja) que refuerzan **P-001** (inventario y maquetacion unificada de todos los emails salientes, con criterios legales). El transporte ya es comun (`SmtpMailer`, extraido en MVP-206), pero la composicion sigue siendo ad-hoc por tipo de correo. Sin accion propia: se consolida con P-001. **Resuelto en MVP-715**: los dos correos del ciclo de vida del Workspace usan ya la plantilla comun, con su version en texto plano y su pie legal, igual que el resto del inventario. | bajo | no | MVP-715 | resuelto | MVP-715 |
| P-031 | 2026-07-28 | MVP-002 / MVP-206 | tecnico/calidad | **EF+SQLite no traduce `ORDER BY` sobre `DateTimeOffset`, y eso esta moldeando las consultas.** Ya obligo a ordenar en memoria en `ListPendingEmailAsync` (MVP-204) y en MVP-206 en `FindOtherActiveOwnerAsync`, `ListPendingAuthorizationsAsync` y —al ampliarse la cobertura de SQL real— tambien en `FindDefaultForUserAsync`, que antes ordenaba en base de datos. PostgreSQL si lo soporta: se esta degradando la consulta de produccion para que el arnes de tests la pueda ejercitar. Impacto real bajo (conjuntos pequeños), pero es una decision implicita que conviene hacer explicita. Propuesta: al montar el arnes de integracion contra PostgreSQL (MVP-501), decidir un criterio unico —mapear los `timestamptz` a `DateTime` o dejar de usar SQLite para estas consultas— y revertir los ordenes en memoria que solo existan por el test. **Resuelto en MVP-501, 2a pasada (2026-07-31, decision del PO: no arrastrar deuda tecnica)**. El criterio unico elegido es **dejar de usar SQLite**: el arnes de integracion y los 11 ficheros de tests de repositorio pasan a **PostgreSQL real en contenedor** (Testcontainers, una base por clase de test sobre un contenedor compartido, con el esquema creado aplicando las migraciones reales). Con esa condicion dada se revierten **ocho** consultas —no cuatro, como estimaba este punto—: `FindDefaultForUserAsync` y `FindOtherActiveOwnerAsync` (que materializaban la tabla entera para quedarse con **una** fila, no solo ordenaban de mas), `ListPendingAsync` de invitaciones, `ListPendingAuthorizationsAsync` de reactivaciones y el desempate por fecha de captura de los cuatro listados operativos (actividades, cosechas, compras y consumos); ademas se retiran los dos `OrderBy` redundantes que quedaban en `ListWorkspaceInvitationsHandler` y `ListWorkspacePeopleHandler`. Contrapartida aceptada y declarada en `docs/04-ingenieria/estrategia-testing.md`: **los tests del backend exigen Docker**. La suite completa (595 tests, contenedor incluido) tarda ~28 s. | bajo | no | MVP-005 / MVP-501 | resuelto | MVP-501 |
| P-032 | 2026-07-28 | MVP-002 / MVP-206 | funcional/ux | **Edicion del perfil de usuario.** El prototipo `AjustesView` incluye un bloque «Perfil del titular» con nombre y correo editables que MVP-206 **no porta**: la identidad llega de Google (RN-036) y hoy no es editable en el producto; permitir cambiar el email ademas rompería la aptitud de las invitaciones dirigidas (MVP-103/107). Decidir si el MVP quiere un nombre de visualizacion propio (independiente del de Google) y con que historia. Sin impacto funcional actual. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog. Tiene un consumidor natural que aun no existe: un nombre de visualizacion editable por el Workspace es uno de los discriminadores candidatos de `P-047`, asi que conviene decidirlo con ese punto y no antes. | bajo | no | Backlog post-MVP (con P-047) | backlog-post-mvp | - |
| P-033 | 2026-07-28 | MVP-002 / MVP-206 | funcional/deuda | **No hay retencion ni expurgo de lo que queda tras una baja logica.** Los Workspaces dados de baja permanecen indefinidamente con todos sus datos (por diseño, RN-039), y sus solicitudes de reactivacion caducadas o cerradas tampoco se limpian. Es el fuera-de-alcance explicito de MVP-206 (purga y retencion), pero conviene planificarlo junto al marco de privacidad (P-008/P-024): «no se borra nada» necesita una politica de cuanto tiempo. **Resuelto en MVP-505 (2026-07-31)**: `RN-041` extiende los 24 meses que ya regian para «cuenta cancelada» a todo lo que el producto conserva por diseno y no tenia plazo —Workspaces de baja (RN-039), registros operativos eliminados (RN-037), solicitudes de reactivacion cerradas o caducadas e invitaciones terminales—. El plazo vive **tambien en codigo** (`AccountRetentionPolicy`) y la respuesta de la baja devuelve la fecha concreta de purga, para que `MVP-503` pueda verificarlo contra el sistema en vez de leerlo. **Pendiente de despliegue**: la rutina que ejecuta el expurgo al vencer el plazo necesita programacion periodica, que es infraestructura; anotado para el gate de `MVP-504`. | bajo | no | MVP-005 / MVP-505 (con P-024) | resuelto | MVP-505 |
| P-034 | 2026-07-28 | MVP-002 / MVP-204 (rev. MVP-299) | tecnico/modelo | **No hay identidad unica de «responsable» para el registro de actividades.** El ER declara `ACTIVITY.worker_id` como FK a `workers` y `contratos-api.md` exige `worker_id*` en `POST /api/v1/activities`, pero MVP-204 decidio deliberadamente (ver P-022) que **los miembros del Workspace no son filas de `workers`**: se exponen como seleccionables desde `workspace_members`, identificados por `user_id`. RN-027 obliga a que todo miembro aparezca automaticamente como responsable seleccionable, de modo que hoy **un miembro elegido como responsable no se puede guardar**: su `user_id` no es un `workers.id`. Tampoco existe un endpoint unico de responsables: `TrabajadoresView` combina en cliente dos listados con espacios de identificadores distintos. Detectado en la revision de cierre de MVP-002; es el equivalente para el *responsable* de lo que P-028 registra para la *tarea*. Opciones: (a) materializar una fila `workers` por miembro activo usando el `user_account_id` ya reservado, o (b) hacer el responsable polimorfico en `ACTIVITY` (`worker_id?` XOR `member_user_id?`). **Recomendacion de la revision: opcion (a)**, porque mantiene un unico espacio de identificadores para el diario (MVP-003) y el dashboard (MVP-004) y no reabre el contrato de actividades. Quien materialice la entidad de actividad debe cerrarlo y actualizar el ER. **Reasignado en la 2a pasada de MVP-299 (decision del PO, 2026-07-28): se resuelve dentro de MVP-002, en `MVP-208` (CA-1..CA-5), y no en MVP-301.** Motivo: el maestro incompleto es el de responsables, no el diario que lo consume; resolverlo en la epica que lo entrega permite marcar su CA-3 con evidencia en vez de reformularlo, y evita que MVP-003 construya sobre un responsable que no se puede guardar. Se adopta la **opcion (a)** (fila `workers` por miembro via `user_account_id`), que ademas cierra el hallazgo `R-16` sin codigo nuevo: el indice `ux_workers_workspace_name` de MVP-207 pasa a cubrir la union miembro/cuadrilla. **Resuelto en MVP-208 (CA-1..CA-5)**: `workers` es el maestro unico de responsables; `GET /workers` devuelve las dos clases con `kind` (`member`/`crew`) y `user_account_id`; el maestro sigue a la membresia sin accion manual (alta al crear Workspace y al aceptar invitacion, inactivacion al revocar, resincronizacion del nombre desde Google en el login) y de un miembro solo se edita la tarifa (422 al renombrarlo o inactivarlo). Verificado end-to-end que un miembro es direccionable por `workers.id` y que el 409 de duplicado cubre la frontera miembro/cuadrilla. | alto | si | MVP-002 / MVP-208 | resuelto | MVP-208 |
| P-035 | 2026-07-28 | MVP-002 / MVP-204 (rev. MVP-299) | ux/doc | **Campos del prototipo no portados en el maestro de Trabajadores.** `TrabajadoresView` del prototipo captura «Rol / Especialidad» y «Telefono»; el maestro real solo tiene `name` y `hourly_rate`. No son «datos laborales avanzados» (el fuera-de-alcance de MVP-204), sino identificacion y contacto de la cuadrilla, asi que la omision no esta justificada en el spec: el checklist de MVP-204 marca la pantalla «cubierto» sin anotarla, a diferencia de MVP-202, que si registro sus divergencias en P-019. Decidir si el MVP quiere esos campos y con que historia; anadirlos despues es una migracion aditiva sin riesgo. Sin impacto funcional actual. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog. El telefono de una persona de la cuadrilla es una **categoria de dato personal nueva** (`RN-041`) y obligaria a revisar la Politica de Privacidad ya publicada; ninguno de los dos campos desbloquea nada hoy. | bajo | no | Backlog post-MVP (campos de maestros, con P-054) | backlog-post-mvp | - |
| P-036 | 2026-07-28 | MVP-002 / MVP-202, MVP-204, MVP-205 (rev. MVP-299) | funcional/ux | **Un registro de maestro creado por error no se puede borrar, solo inactivar.** La politica de inactivacion con historico de la epica (terrenos, trabajadores y tareas) es correcta para lo que ya se uso, pero no contempla el caso trivial de una fila recien creada y nunca referenciada: queda para siempre en la lista de inactivos. RN-037 (borrado fisico con confirmacion explicita) cubre solo **registros operativos**, no maestros. Propuesta: permitir el borrado fisico de un registro de maestro **sin uso historico**, con confirmacion explicita, cuando existan las entidades operativas que permitan comprobar ese «sin uso» (MVP-003/MVP-004); hasta entonces no es comprobable. **Revisado en MVP-499 (2026-07-30)**: el PO decide **diferir a post-MVP**. No es un defecto sino funcionalidad nueva (backend + UI) que no bloquea la salida a MVP; su premisa —comprobar el «sin uso»— **ya se cumple** (existen actividades, cosechas, compras y consumos), así que queda listo para priorizarse con P-041. **Retriado en la revision del MVP (2026-08-10)**: pasa a `MVP-008`. La condicion por la que se difirio -poder comprobar el «sin uso historico»- ya no existe, y el sintoma si: cualquier Workspace real arrastra filas que su dueno no puede retirar. Alcance: borrado fisico con confirmacion cuando no hay uso, y fusion (con `P-041`). | bajo | no | MVP-008 / MVP-806 (con P-041) | triado | MVP-806 |
| P-037 | 2026-07-28 | MVP-002 / MVP-205 (rev. MVP-299) | ux | **La navegacion lateral no marca la seccion activa.** `AppSidebar` navega con `button` mas `navigate()` en vez de `NavLink`, asi que no hay estado seleccionado ni `aria-current`: con seis entradas ya encendidas, el usuario no ve en que seccion esta. Es la misma superficie que P-025 (agrupacion del menu por secciones) y conviene resolverlo en la misma pasada, cuando esten encendidos los modulos de MVP-003/MVP-004. **Resuelto en MVP-406 (2026-07-30)**: cada entrada es un `NavLink`, que marca la activa y expone `aria-current="page"`; verificado en `/app/cosechas`. | bajo | no | MVP-004 / MVP-406 (con P-025) | resuelto | MVP-406 |
| P-038 | 2026-07-28 | MVP-002 / MVP-207 | ux | **`/app/invitations` sigue dentro de la guarda de oferta de temporada.** MVP-207 (CA-5) saca a Terrenos de la guarda para que los maestros de administracion se comporten igual, pero la pantalla de invitar sigue dentro. Como «Miembros y accesos» si esta fuera, en un Workspace sin temporada activa la persona entra a Miembros sin problema y, al pulsar «Invitar persona», se topa con el desvio a `/app/temporada/nueva`: exactamente la clase de defecto que describe R-09, aplicada a una pantalla que el CA-5 no nombra (habla de «maestros de administracion», e invitar no es un maestro). **Decision del PO (2026-07-28): corregirlo en la misma pasada, para no arrastrar el error.** **Resuelto en MVP-207**: `/app/invitations` pasa al bloque de administracion de `App.tsx`, junto a los cinco maestros y a Ajustes. Tras el cambio, el unico destino que queda detras de `RequireSeasonOffer` es el Home (`/app`), que es donde MVP-201 queria la oferta: al entrar, no al administrar. Verificado en UI conducida con un Workspace sin temporada activa: «Invitar persona» desde Miembros abre la pantalla de invitar, y `/app` sigue ofreciendo crear la temporada. | bajo | no | MVP-207 | resuelto | MVP-207 |
| P-039 | 2026-07-28 | MVP-002 / MVP-207 | ux/funcional | **A la persona invitada no se le avisa de que su invitacion se ha anulado.** MVP-207 (CA-4) deja el enlace inservible y el preview explica el motivo («quien te invito ha anulado esta invitacion»), pero solo si la persona llega a abrirlo: no sale ningun correo ni aviso in-app, asi que quien recibio el email original puede intentar entrar dias despues sin saberlo. Es el mismo hueco que P-029 describe para las solicitudes de reactivacion y depende de la misma generalizacion del centro de notificaciones (P-011) y del inventario de emails (P-001). Sin accion propia: se consolida con P-029/P-011. **DESCARTADO en la revision del MVP (2026-08-07, decision del PO)**: basta con que se avise **al intentar unirse**, que es justo lo que ya hace el preview del enlace; un correo mas para decir que algo ya no vale anade ruido sin anadir informacion que la persona no vaya a recibir cuando la necesite. | bajo | no | Descartado | descartado | - |
| P-040 | 2026-07-28 | MVP-002 / MVP-207 | ux | **Encaje del bloque «Prepara tu explotacion» del Home con la Vision General (MVP-004).** MVP-207 (CA-6) sustituye el copy muerto del Home por un checklist de maestros pendientes de poblar, deliberadamente sin metricas (el dashboard es alcance de MVP-004). Cuando llegue la Vision General habra que decidir si el Home pasa a ser esa vista y el checklist se retira, se conserva como bloque secundario mientras queden maestros vacios, o se mueve al onboarding. Registrado para que MVP-004 lo cierre en vez de duplicar pantallas de inicio. **Resuelto en MVP-499 (2026-07-30)**: el Home tiene dos caras según la preparación del Workspace —checklist mientras falten maestros, **Visión General cuando está preparado**—, reutilizando la misma vista sin duplicar pantallas. **Revisado en MVP-703 (2026-08-08, `P-087`)**: la segunda cara pasa a ser el **Diario de campo**; el checklist no cambia. | bajo | no | MVP-004 / MVP-499 | resuelto | MVP-499 / MVP-703 |
| P-041 | 2026-07-28 | MVP-002 / MVP-207 | funcional/ux | **Los duplicados renombrados por la migracion de MVP-207 no se pueden fusionar.** La migracion que crea los indices unicos resuelve los duplicados preexistentes renombrandolos con sufijo (« (2)», « (3)»…) sin perder datos, pero el usuario se queda con filas que probablemente queria unificar y solo puede renombrarlas o inactivarlas: no hay fusion de dos registros de maestro ni borrado fisico (P-036). Es un caso acotado —solo afecta a datos creados antes de la guarda— pero refuerza P-036: cuando existan las entidades operativas que permitan comprobar el «sin uso historico», conviene cubrir tambien la fusion, no solo el borrado. Sin accion propia: se consolida con P-036. **Ampliado en MVP-208 (2026-07-28)**: el backfill de miembros aplica la misma politica, asi que una fila de cuadrilla que ocupaba el nombre de un miembro queda renombrada « (2)» y probablemente sea la misma persona; el usuario solo puede renombrarla o inactivarla. Es el escenario mas probable de fusion real (la cuadrilla «Juan Perez» y el miembro «Juan Perez») y refuerza la prioridad del punto. **Revisado en MVP-499 (2026-07-30)**: diferido a post-MVP junto a P-036, con el que se construira en una sola pasada (borrado + fusion de maestros sin uso). **Retriado en la revision del MVP (2026-08-10)**: pasa a `MVP-008` junto a `P-036`, en una sola pasada: borrado de lo no usado mas fusion de dos filas del mismo maestro reapuntando sus referencias. | bajo | no | MVP-008 / MVP-806 (con P-036) | triado | MVP-806 |
| P-042 | 2026-07-28 | MVP-002 / MVP-207 (defecto en MVP-107) | doc/deuda | **El catalogo cerrado `invitation_status` estaba desactualizado desde MVP-107.** `contratos-api.md` y `modelo-de-datos.md` seguian declarando `pendiente, aceptada` cuando MVP-107 ya habia anadido `rechazada` como estado persistido. Misma clase de deriva que R-05 (contrato de temporadas), detectada al tener que anadir `anulada` en la misma tabla. **Resuelto en MVP-207**: el catalogo pasa a `pendiente, aceptada, rechazada, anulada` en los dos documentos, con la descripcion de quien fija cada estado terminal. Se registra solo para trazabilidad: sugiere revisar los catalogos cerrados al cerrar cada epica, no solo las secciones de endpoints. | bajo | no | MVP-207 | resuelto | MVP-207 |
| P-043 | 2026-07-28 | MVP-002 / 2a pasada de MVP-299 | tecnico/contrato | **Los codigos de validacion del alta (`POST`) no distinguen el motivo del error, y uno de ellos sale en ingles.** `InvalidModelStateResponseFactory` (`Program.cs`) colapsa toda la validacion de binding a `VALIDATION_REQUIRED`, asi que un nombre demasiado largo en `POST` de `plots`, `workers`, `seasons` o `tasks` responde `VALIDATION_REQUIRED` con mensaje «…es demasiado largo», mientras que el mismo caso en `PATCH` si devuelve `VALIDATION_*_NAME_LENGTH` (la validacion de dominio). Un cliente no puede distinguir «falta» de «demasiado largo» en el alta. Ademas, un `start_date` mal formado en `POST /seasons` devuelve el texto por defecto de ASP.NET **en ingles** («The request field is required.»), que la UI muestra tal cual al usuario. Detectado en la 2a pasada como hallazgo `R-18`: su parte **documental** se arregla en `MVP-208` (CA-9, que el contrato diga lo que la API hace hoy); esta fila cubre la parte **tecnica**, que es transversal a toda la API y no solo a los maestros. Propuesta: emitir el codigo de dominio correcto desde el borde de transporte (o validar en el dominio tambien en el alta) y traducir los mensajes por defecto. Mismo borde que **P-027**: resolver juntos. **Resuelto en MVP-502 (2026-07-31)**: el obstaculo real era que `ModelState` solo transporta un **texto**, sin hueco para un codigo ni forma fiable de saber que atributo fallo cuando hay `[Required]` y `[StringLength]` en la misma propiedad. Se resuelve con anotaciones propias (`RequiredField`, `MaxTextLength`) que componen el mensaje como `CODIGO<US>texto` y un `ModelStateErrorTranslator` que lo descompone; la convencion vive solo en esos dos ficheros. El alta y la edicion responden ya el mismo codigo. Se anade **`VALIDATION_FORMAT_INVALID`** para «el valor llego pero no se puede interpretar», que es un arreglo distinto de «falta», y ningun mensaje por defecto de ASP.NET llega en ingles al cliente. `contratos-api.md` actualizado. | medio | no | MVP-005 / MVP-502 (con P-027) | resuelto | MVP-502 |
| P-044 | 2026-07-28 | MVP-002 / 2a pasada de MVP-299 | funcional/ux | **Dos Workspaces del mismo usuario pueden llamarse igual.** Verificado renombrando «Test 02» a «Test 01»: responde `200` y `GET /workspaces` devuelve dos entradas identicas, que el selector (MVP-104) y el dialogo de baja (MVP-206) muestran indistinguibles. MVP-002 cerro la unicidad de nombre «en los maestros» (MVP-207), pero el Workspace —el contenedor cuyo ciclo de vida absorbio esa misma epica— quedo fuera: ni `MVP-102` (alta) ni `MVP-206` (renombrado) validan mas que obligatoriedad y longitud. El riesgo real no es de integridad (los datos estan bien aislados) sino de accion equivocada: dar de baja el Workspace que no era. Decidir si el MVP quiere unicidad por propietario, un discriminador en el selector (fecha de alta, numero de miembros) o ninguna de las dos. Hallazgo `R-19`. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog junto a `P-047`: los dos exigen decidir antes **que discriminador** se muestra, decision de producto que no esta tomada, y el escenario necesita dos contenedores homonimos, que a la escala actual todavia no se da. | bajo | no | Backlog post-MVP (homonimos, con P-047) | backlog-post-mvp | - |
| P-045 | 2026-07-28 | MVP-002 / 2a pasada de MVP-299 | ux | **Una temporada desbancada se rotula «planificada» aunque sea una campaña pasada.** Con la decision «crear cambia la activa» (P-017), crear «2026/2027» desbanca a «2025/2026», que pasa a `is_active=false, is_closed=false` y por tanto al estado derivado `planificada`. Es coherente con la derivacion y esta contratado, pero el vocabulario engaña: `season_status` es un catalogo cerrado de producto y «planificada» describe algo por venir. Opciones: renombrar el valor, derivar el estado tambien de las fechas, o cerrar automaticamente la desbancada. No se decide aqui porque lo consume quien filtre por temporada. Hallazgo `R-22`. Consolidar con **P-021** (enriquecimiento de la tarjeta de temporada) y revisar al cierre de MVP-004. **Resuelto en MVP-209 (2026-07-30)**: al plantearlo, el PO reformulo el problema de fondo —el modelo fundia dos conceptos en `Season.is_active`—, asi que en vez de un parche se separo el **estado** (derivado ahora tambien de las fechas: `abierta` cubre las campañas pasadas no cerradas, `planificada` solo las futuras) de la **temporada de trabajo**, que ademas pasa a ser **por usuario** (`workspace_members.active_season_id`). Ya no hay «desbanque» global: crear fija solo mi temporada de trabajo. | bajo | no | MVP-002 / MVP-209 | resuelto | MVP-209 |
| P-046 | 2026-07-28 | MVP-002 / 2a pasada de MVP-299 | ux/tecnico | **No hay pantalla de error para rutas desconocidas.** `App.tsx` mapea `/app/*` a `HomeView` y el resto a `/`, asi que un enlace roto, un error de tecleo o una ruta retirada renderizan el Home sin decir nada: el usuario cree que ha llegado a donde queria. Ninguna historia lo cubre porque el shell (P-016) se entrego con los modulos deshabilitados y no habia rutas que fallar; con seis secciones encendidas ya las hay. Hallazgo `R-23`. Es la misma superficie que **P-025** (agrupacion del menu) y **P-037** (seccion activa): conviene resolverlo en la misma pasada de navegacion. **Resuelto en MVP-406 (2026-07-30)**: nueva `NotFoundView`; `/app/*` deja de caer en el Home y `*` de redirigir en silencio a `/`. La 404 bajo `/app` vive dentro del shell y fuera de la guarda de temporada; la salida depende de la sesion (Home o landing). | bajo | no | MVP-004 / MVP-406 (con P-025 y P-037) | resuelto | MVP-406 |
| P-047 | 2026-07-28 | MVP-002 / MVP-208 | ux | **Dos personas con el mismo nombre de Google en un Workspace se distinguen solo por un sufijo « (2)».** Al materializar a los miembros como responsables (MVP-208, CA-1), el indice unico de nombre obliga a desempatar. Cuando el nombre lo ocupaba una fila de cuadrilla la regla es clara (la cuadrilla cede el nombre), pero entre **dos cuentas homonimas** ninguna es renombrable —su nombre es el de su cuenta (RN-036)— y el sufijo lo acaba tomando quien entra despues. Se eligio asi porque la alternativa era rechazar la materializacion y **dejar a esa persona fuera del Workspace**, que es un dano mucho mayor. El resultado es correcto pero poco informativo: en el desplegable de responsables de MVP-301 se veran «Juan Perez» y «Juan Perez (2)» sin pista de quien es quien. Propuesta: un discriminador con significado en el maestro (inicial del apellido, email enmascarado o una etiqueta editable por el Workspace), reutilizable ademas por **P-044** (Workspaces homonimos), que es el mismo problema en otro contenedor. Sin impacto funcional: solo de legibilidad. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog junto a `P-044`, por la misma razon: sin la decision del discriminador, entrar ahora es disenar a ciegas. | bajo | no | Backlog post-MVP (con P-044) | backlog-post-mvp | - |
| P-048 | 2026-07-28 | MVP-002 / 3a pasada de MVP-299 | funcional/alcance | **Un miembro no propietario no puede abandonar un Workspace.** No hay via, ni de API ni de UI, para salir por voluntad propia de un Workspace al que a uno le invitaron: `MVP-204` (CA-7) cubre retirar el acceso **a otra** persona y la pantalla oculta la accion sobre uno mismo (`is_self`); `MVP-206` cubre la salida **del propietario** (traspaso o baja logica). Un miembro corriente que ya no colabora arrastra ese Workspace en su selector indefinidamente y, desde `MVP-208`, sigue ademas como responsable seleccionable en el. Es el hueco simetrico del ciclo de vida que absorbio `MVP-002`, y no lo cubre ninguna historia de MVP-001..MVP-006. Decidir el flujo (¿confirmacion?, ¿aviso al resto?, ¿reingreso solo por invitacion nueva, como el revocado?) y su encaje: la superficie natural es «Miembros y accesos» o «Ajustes», y la guarda de no dejar el Workspace sin miembro activo ni sin propietario (CA-8 de MVP-204, RN-038) ya existe y debe reutilizarse, no reimplementarse. Hallazgo `R-26`. No se resuelve en `MVP-002` porque no rompe ningun CA de la epica, no bloquea a MVP-003/MVP-004 y es alcance nuevo del ciclo de vida de la **membresia**, no de los maestros **Retriado en la revision del MVP (2026-08-10)**: pasa a `MVP-008`. Es un hueco de salida sin alternativa -no hay via, ni de API ni de UI, para dejar un Workspace al que te invitaron- y con invitaciones por enlace compartible aparece en cuanto haya uso compartido. La guarda de `RN-038` ya existe y se reutiliza. | medio | no | MVP-008 / MVP-807 (con P-049) | triado | MVP-807 |
| P-049 | 2026-07-28 | MVP-002 / 3a pasada de MVP-299 | ux/coherencia | **La UI nunca ofrece revocar a un copropietario aunque la API lo permita.** `WorkspaceMembersController` calcula `can_revoke` como `status == activo && role != workspace_owner`, mientras la guarda real de `RevokeMemberHandler` solo protege al propietario **unico** (`CountActiveOwnersAsync <= 1`), que es lo que dice literalmente el CA-8 de `MVP-204`. Mientras el propietario era siempre uno la diferencia no se notaba; `MVP-206` introdujo Workspaces con varios propietarios y ahora la regla publicada y la accion disponible no coinciden. No es un fallo de seguridad —la UI es mas restrictiva que la API—, sino una incoherencia a decidir: o `can_revoke` refleja la guarda real, o la guarda se endurece para prohibirlo siempre. Hallazgo `R-27`. Misma superficie y misma decision de producto que **P-048**: resolver juntos **Retriado en la revision del MVP (2026-08-10)**: pasa a `MVP-008` dentro de la historia de `P-048`: misma superficie y la misma decision de producto, y alinear `can_revoke` con la guarda real es media hora dentro de ella. | bajo | no | MVP-008 / MVP-807 (con P-048) | triado | MVP-807 |
| P-050 | 2026-07-28 | MVP-002 / 3a pasada de MVP-299 | doc/tecnico | **El ER de `PURCHASE` no declara `season_id` ni la relacion `SEASON -> PURCHASE`.** `RN-021` exige que toda actividad, cosecha **y compra** quede asociada a una temporada, y `contratos-api.md` §7 ya contrata `season_id*` en el alta de compra y `season_id?` como filtro del listado, pero el ER de `modelo-de-datos.md` no lo recoge: `PURCHASE` solo referencia `workspace_id`. Es la misma clase de deriva que **P-028** (la tarea de `ACTIVITY`), detectada al comprobar que el CA-3 de `MVP-002` se cumple tambien para las compras. Sin impacto funcional hoy: la entidad esta pendiente. Quien la materialice —`MVP-303`— debe añadir el campo, la relacion y la regla al ER en la misma pasada en la que `MVP-301` cierre `task_id`/`task_text`. Hallazgo `R-28`. **Resuelto en MVP-303 (2026-07-29)**: `purchases.season_id` es NOT NULL con FK `ON DELETE RESTRICT`, el dominio lo exige (`VALIDATION_PURCHASE_REQUIRED_FIELDS`) y el ER recoge el campo, la relacion `SEASON -> PURCHASE` y el indice `(workspace_id, season_id)`. En la misma pasada se cerro el **mecanismo del consumo** que el spec de MVP-303 exigia decidir antes: `purchase_id` anulable sobre `PURCHASE_CONSUMPTION`, no una entidad propia. | bajo | no | MVP-303 (con P-028) | resuelto | MVP-303 |
| P-051 | 2026-07-29 | MVP-003 / MVP-301 | tecnico/ux | **El diario no tiene paginacion.** `GET /api/v1/activities` devuelve **todas** las actividades vivas del Workspace y el cliente las pinta enteras; los filtros de rango de fechas existen pero son opcionales. Las convenciones de `contratos-api.md` ya definen el patron (`?page=&limit=` con `meta:{ total, page, limit }`), asi que no hay que inventarlo, solo aplicarlo. Hoy no rompe nada —un Workspace recien estrenado tiene decenas de registros— pero una explotacion con dos campanas de historico llegara a miles y el diario es **la vista principal del MVP** (RN-033), la que mas se abre. Afecta por igual a compras y consumos (`MVP-303`/`MVP-304`) y al diario unificado (`MVP-305`), asi que conviene decidirlo **una vez** para los tres: paginacion clasica, scroll infinito o ventana temporal por defecto (p. ej. la temporada activa). No se resuelve en `MVP-301` porque la decision es del diario completo, no del listado de actividades. **Ampliado en MVP-305 (2026-07-29)**: el diario unificado (`GET /api/v1/diary`) mezcla los tres tipos **en memoria** dentro del servidor, reutilizando los tres puertos ya probados en vez de un `UNION` en SQL. Es equivalente mientras no haya paginacion —en los dos casos se traen todas las filas del rango—, pero deja de serlo en cuanto la haya: **paginar sobre tres listas ya materializadas no es paginar**. Quien resuelva este punto debe mover la mezcla a SQL en la misma pasada, no solo anadir `page`/`limit`. **Consolidado en MVP-506 (2026-07-30)** junto a `P-052` y `P-056`, en la epica de endurecimiento `MVP-005` (el nucleo del punto es escala/salida a MVP, no reabre `MVP-003`). **Resuelto en MVP-506 (2026-07-31)**: paginacion clasica `page`/`limit` con `meta:{ total, page, limit }` —la decision que el spec dejaba abierta— y, sobre todo, **la mezcla movida a SQL** (`UNION ALL` en un `DiaryRepository` propio), que es lo que este punto exigia: recortar la pagina sobre cuatro listas ya materializadas habria seguido trayendo el historico entero en cada peticion, una interfaz paginada sobre un backend que no lo esta. Los totales de cabecera pasan a una unica consulta agregada sobre el conjunto filtrado. El orden desempata por `id` para que la paginacion sea estable con altas en lote. | medio | no | MVP-005 / MVP-506 (con P-052 y P-056) | resuelto | MVP-506 |
| P-052 | 2026-07-29 | MVP-003 / MVP-301 | tecnico | **Los filtros del diario se aplican en cliente aunque el servidor los soporta.** Registrado cuando `DiarioView` se traia el listado completo de actividades y filtraba en memoria. **Parcialmente resuelto en MVP-305**: terreno, temporada y tipo ya viajan al servidor (`GET /api/v1/diary`); **queda la busqueda por texto**, que sigue siendo local sobre lo ya filtrado. Es coherente con el resto de la aplicacion y hoy da mejor respuesta —teclear no dispara una peticion por letra—, pero deja de serlo en cuanto haya paginacion (`P-051`): buscar sobre una pagina no es buscar. Sin accion propia: se resuelve con `P-051` y en la misma decision. **Resuelto en MVP-506 (2026-07-31)**: la busqueda por texto viaja al servidor y se resuelve sobre el diario completo, con cada tipo buscando en los campos que tiene. En cliente queda solo la espera antes de disparar la peticion (350 ms), que era la objecion legitima de este punto: teclear no puede ser una peticion por letra. | bajo | no | MVP-005 / MVP-506 (con P-051) | resuelto | MVP-506 |

| P-053 | 2026-07-29 | MVP-003 / MVP-303 (defecto en MVP-205) | bug/ux | **El foco no vuelve al campo tras anadir una tarea en `TareasView`.** El `tech-design` de `MVP-205` afirma que «el foco vuelve al campo: poblar el catalogo es escribir varias tareas seguidas», pero no ocurre: `newNameInput.current?.focus()` se llama dentro del manejador, cuando `isCreating` **sigue siendo `true`** y el input esta `disabled`; enfocar un elemento deshabilitado no hace nada, y `setCreating(false)` se aplica despues, en el `finally`. Verificado en UI conducida: tras anadir una tarea el `document.activeElement` es `BODY`. Impacto bajo pero directo sobre la promesa de la pantalla —hay que volver a pulsar el campo por cada tarea—. La correccion es mover el foco a un `useEffect` que se dispare tras el re-render, que es como se ha implementado en `ComprasView` (MVP-303). No se corrige aqui por estar fuera del alcance de la historia y afectar a una entrega ya validada. **Resuelto en MVP-399 (2026-07-29)** como correccion de cierre: el foco se pide desde un `useEffect` disparado tras el re-render, cuando `isCreating` ya es `false` y el input vuelve a estar habilitado. Mismo patron que `ComprasView`. Verificado en UI conducida: tras anadir una tarea, `document.activeElement` vuelve a ser el campo. | bajo | no | MVP-399 (correccion de cierre de la epica) | resuelto | MVP-399 |
| P-054 | 2026-07-29 | MVP-003 / MVP-303 | ux/doc | **Campos del prototipo no portados en el libro de compras.** `ComprasView` del prototipo captura ademas «Categoria» (fertilizantes, fitosanitarios, riego, combustible, mantenimiento) y «Proveedor»; el modelo real de la KB solo tiene `product`, `total_quantity`, `total_cost`, `purchase_date` y `season_id`, que es lo que fija RN-031 y el contrato. La omision es deliberada —no inventar campos que la KB no pide— pero conviene decidirla: la **categoria** habilitaria un desglose de gasto por tipo en el dashboard (MVP-004) y el **proveedor** es informacion de contacto real de la explotacion. Anadirlos despues es una migracion aditiva sin riesgo. Es el mismo tipo de punto que `P-035` registro para el maestro de Trabajadores. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog. La **categoria** solo cobra sentido con el desglose de gasto por tipo, que `RN-009` deja expresamente fuera: sin el, es un campo que se rellena y nadie lee. El **proveedor** es la misma clase de maestro de terceros que `P-062`, y se decide con el. | bajo | no | Backlog post-MVP (con P-035) | backlog-post-mvp | - |

| P-055 | 2026-07-29 | MVP-003 / MVP-304 | ux/accesibilidad | **Los modales de la aplicacion no atrapan el foco.** Con un modal abierto (`PlotFormModal`, `WorkerFormModal`, `SeasonFormModal`, `ActivityFormModal`, `PurchaseFormModal`, `ConsumptionFormModal`, `CloseWorkspaceModal`, `InvitationModal`) los controles del fondo siguen siendo alcanzables con el tabulador y siguen pudiendo activarse, porque el overlay solo tapa visualmente. Tampoco se cierra con `Escape` de forma uniforme ni se devuelve el foco al control que lo abrio. Detectado al conducir la UI de MVP-304: pulsando el envio del formulario en linea del fondo con el modal abierto se dispara el alta equivocada. Impacto real bajo con raton, alto para quien navegue con teclado o lector de pantalla. Es **transversal desde MVP-202** —ningun modal lo hace— y no algo que introduzca esta historia. Propuesta: un componente `Modal` comun con trampa de foco, cierre con `Escape`, `aria-modal`/`role="dialog"` y restauracion del foco, y migrar los ocho modales a el. **REABIERTO en la revision del MVP (2026-08-06): se dio destino `MVP-502` y esa historia se cerro sin hacerlo.** Verificado en el codigo entregado: no existe ningun componente `Modal` comun, y `ActivityFormModal`, `HarvestFormModal`, `PurchaseFormModal`, `ConsumptionFormModal`, `PlotFormModal`, `WorkerFormModal` y `SeasonFormModal` **no tienen ni una sola** ocurrencia de `role="dialog"`, `aria-modal` ni manejo de `Escape`. El alcance de `MVP-502` era hardening de seguridad y PII, no accesibilidad, asi que el punto nunca tuvo historia que lo construyera: el destino se anoto pero nadie lo recogio. Es ademas el unico hallazgo de accesibilidad con impacto real (teclado y lector de pantalla) y el defecto que lo origino —activar el formulario del fondo con un modal abierto— sigue reproduciendose. **Resuelto en MVP-704 (2026-08-08)**: componente comun `components/common/Modal.tsx` y los **once** modales migrados. Habia tres formas distintas de hacer lo mismo y ocho de no hacerlo: `ConfirmDialog` ponia `role`/`aria-modal` en el **velo** en vez de en el panel, `InvitationModal` tenia su propio `Escape`, y `TerrenoDetailModal` y `CloseWorkspaceModal` cerraban con `onClick` en el velo. Tres piezas: `inert` sobre `#root` —que exige portal a `body` para que el dialogo no se apague a si mismo, y un contador para que dos modales apilados no devuelvan la vida al fondo antes de tiempo—, trampa de foco que solo interviene en los extremos, y restauracion al disparador. Verificado sobre la aplicacion en marcha: de los **30 controles del fondo, 0** pueden recibir foco, tras 40 tabuladores reales el foco sigue dentro, y `document.elementFromPoint` sobre el boton de envio del fondo devuelve el velo. Los once abiertos uno a uno. 13 tests, incluido el defecto original y el caso apilado. | medio | no | MVP-704 | resuelto | MVP-704 |

| P-056 | 2026-07-29 | MVP-003 / MVP-399 | ux/funcional | **El diario no deja filtrar por responsable.** `GET /api/v1/diary` admite `from`, `to`, `plot_id`, `season_id` y `type`, pero no `worker_id`, aunque el dato existe en la actividad y `GET /api/v1/activities` si lo soporta. La consecuencia es que «que hizo Antonio esta semana» —una pregunta natural en una explotacion con cuadrilla— no se puede responder desde la vista principal, solo desde la API. No se anade en `MVP-399` porque el filtro solo aplica a un tipo de entrada de tres y conviene decidirlo junto al resto de la navegacion del diario: filtro por responsable, paginacion (`P-051`) y busqueda en servidor (`P-052`) son la misma pasada. Hallazgo `R-02`. **Resuelto en MVP-506 (2026-07-31)**: `worker_id` en `GET /diary` y selector en `DiarioView`. Deja fuera cosechas, compras y consumos **por definicion** —no tienen responsable—, siguiendo el precedente del filtro por terreno con las compras, aviso en la UI incluido para que el muro no parezca vacio sin explicacion. | bajo | no | MVP-005 / MVP-506 (con P-051 y P-052) | resuelto | MVP-506 |
| P-057 | 2026-07-29 | MVP-003 / MVP-399 | ux | **El consumo sin compra previa no ofrece sugerencias de material.** El alta de compra sugiere el vocabulario del historico (`GET /api/v1/purchases/products`, RN-031), pero el formulario de consumo sin compra —el mismo campo de texto libre, en la misma pantalla— no sugiere nada, porque las sugerencias solo miran `purchases`. Favorece justo la dispersion de nombres que las sugerencias existen para evitar: «Abono NPK» comprado y «abono npk» consumido conviven sin que nadie lo note. Propuesta: que las sugerencias combinen el historico de compras **y** de consumos, o que el endpoint acepte un ambito. Sin impacto funcional: el registro nunca se bloquea. Hallazgo `R-03`. **Resuelto en MVP-708 (2026-08-08)**: se **combinan** los dos historicos, no se anade un ambito. Un ambito habria resuelto el sintoma dejando la causa: seguirian existiendo dos vocabularios en la misma pantalla y habria que decidir un defecto por formulario, que es la decision que produjo el defecto. El vocabulario sale ahora de un puerto propio (`IMaterialRepository`) que une en **SQL** compras y consumos **sin compra previa** —las imputaciones no cuentan: copian el material de su compra, asi que no aportan nombre nuevo y solo inflarian `times_used`—. La union va en SQL y no juntando dos listas ya recortadas porque con tope por lista un material presente en los dos libros puede caerse aunque sumando sea el mas usado (misma leccion que `P-051` en el diario). La ruta `GET /purchases/products` no se mueve: es la contratada y renombrarla no le da nada a nadie. | bajo | no | MVP-708 | resuelto | MVP-708 |
| P-058 | 2026-07-29 | MVP-003 / MVP-399 | funcional/ux | **Se admite imputar una compra con fecha anterior a la de la propia compra, sin aviso.** Verificado imputando el 2020-01-01 una compra del 2026-07-31: responde `201`. No debe **bloquearse** —la captura retroactiva es real y RN-032 ya asume que el papeleo va por detras del campo—, pero un consumo anterior a su compra es casi siempre un error de tecleo en la fecha. Encaja con la filosofia que ya usa RN-023 para la temporada: **avisar sin impedir**, con una senal en el formulario y una etiqueta en la fila. Se registra en vez de resolverse en `MVP-399` porque anade una regla de negocio nueva, no corrige una existente. Hallazgo `R-04`. **Resuelto en MVP-708 (2026-08-08)**: la regla nueva es **`RN-043`**, redactada en paralelo a `RN-023` porque es el mismo problema con otra referencia. **Sigue respondiendo `201`** —fijado por test de integracion con el caso exacto del punto, imputar el `2020-01-01` una compra del `2026-07-31`— y se avisa en tres sitios: el formulario mientras se teclea la fecha (con la fecha de la compra escrita y el boton de guardar activo), la fila de «Consumos por terreno» («antes de la compra») y la tarjeta del diario («ANTES DE LA COMPRA»), junto a la de `RN-023`. El aviso se **deriva en lectura** por `LEFT JOIN` a la compra, sin columna ni migracion: congelarlo mentiria en cuanto alguien corrigiese la fecha de la compra, que es una de las dos formas de arreglarlo. La igualdad de fechas no avisa. | bajo | no | MVP-708 | resuelto | MVP-708 |

| P-059 | 2026-07-29 | MVP-004 / MVP-401 | funcional/modelo | **La variedad de cultivo deberia vivir en el terreno, no en la cosecha.** Al cerrar el catalogo `harvest_product` (RN-030) se comprobo que la KB exigia el catalogo pero **no definia sus valores**: solo aparecia `aceituna_olivar` como ejemplo en un JSON de `contratos-api.md`. El prototipo (`CosechaModal`) captura «Producto / Variedad» en el registro de cosecha, pero **decision del PO (2026-07-29): la variedad es un atributo del terreno**, porque no cambia partida a partida. Registrarla en la cosecha obligaria a repetirla en cada alta y permitiria contradicciones dentro del mismo olivar. Propuesta: anadir la variedad a `PLOT` (maestro de terrenos) y que la cosecha la herede. Sin impacto funcional hoy: el MVP esta ligado al olivar y el dashboard no distingue variedades. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog con el resto del racimo: es una epica propia, no un ajuste, y tiene orden interno (`P-060` habilita `P-063`, `P-062` absorbe `P-061`), asi que trocearlo seria construir la mitad de un modelo. El roadmap ya lo ubica en el Hito I. | bajo | no | Backlog post-MVP (cluster modelo de produccion, Hito I) | backlog-post-mvp | - |
| P-060 | 2026-07-29 | MVP-004 / MVP-401 | funcional/alcance | **El producto deberia definirse a nivel de Workspace y modular el calculo de rendimiento.** `harvest_product` se cierra en `MVP-401` con **un solo valor** (`aceituna_olivar`) por decision del PO (2026-07-29): mientras el producto sea uno, obligar a elegirlo en cada cosecha es friccion sin informacion. La forma correcta —«gestionar distintos tipos de producto se abordara en ampliaciones posteriores»— es que el Workspace declare con que trabaja (o lo tenga como subapartado propio) y que **el calculo de rendimiento y los widgets del dashboard varien segun el producto**: L/100kg solo tiene sentido en olivar. Consolidar con **P-059**: los dos describen la misma evolucion del modelo de produccion. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog (cluster del modelo de produccion, Hito I). | medio | no | Backlog post-MVP (cluster modelo de produccion, Hito I) | backlog-post-mvp | - |

| P-061 | 2026-07-29 | MVP-004 / MVP-402 | funcional/modelo | **La densidad de conversion kg-L es una constante fija; RN-016 preve override por almazara.** `MVP-402` convierte el rendimiento graso (kg de aceite/100 kg) a la unidad canonica L/100kg con la densidad por defecto de RN-016, **0,92 kg/L**, aislada en una sola constante (`HarvestYieldConversion.DefaultOilDensityKgPerLitre`). La propia regla contempla «override por almazara, aplicable por defecto a futuros registros y editable por registro», pero **en el MVP no existe la entidad almazara** —la molturacion esta fuera de alcance por RN-029—, asi que no hay donde colgar el override ni quien lo consuma. Propuesta: al introducir la almazara (o el producto por Workspace de `P-060`, con el que comparte superficie) parametrizar la densidad por defecto y admitir el ajuste por registro. Impacto real bajo: la desviacion entre densidades reales de aceite de oliva es de decimas. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog (cluster del modelo de produccion, Hito I; lo absorbe `P-062`). | bajo | no | Backlog post-MVP (cluster modelo de produccion, Hito I) | backlog-post-mvp | - |

| P-062 | 2026-07-29 | MVP-004 / MVP-402 (peticion del PO) | funcional/alcance | **Maestro de almazaras y empresas compradoras, con rendimiento por defecto y una marcada como principal.** Peticion del PO (2026-07-29) al cerrar `MVP-402`: poder **definir las almazaras o empresas** a las que se vende la produccion, de modo que al seleccionar una en el registro de cosecha se **preseleccione el tipo de rendimiento que esa almazara aplica**, y que una de ellas se pueda marcar como **principal** para quedar preseleccionada al dar de alta una cosecha. El efecto buscado es de captura: que el formulario llegue ya con destino y tipo de rendimiento puestos, en vez de obligar a elegir los dos en cada partida. Implica una entidad nueva con ambito de Workspace (nombre, contacto, rendimiento por defecto, marca de principal), su vinculo desde `HARVEST` y el cambio de valores por defecto del formulario. **Absorbe P-061**: la entidad almazara es donde vive el override de densidad de RN-016. Relacionado con `P-054` (proveedor en compras: la misma clase de maestro de terceros). Fuera de alcance del MVP: `RN-029` limita la produccion a producto, kilos, destino y uno entre rendimiento o litros, sin capa comercial ni de molturacion. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog (cluster del modelo de produccion, Hito I). | medio | no | Backlog post-MVP (cluster modelo de produccion, Hito I) | backlog-post-mvp | - |
| P-063 | 2026-07-29 | MVP-004 / MVP-402 (peticion del PO) | funcional/ux | **Tipo de rendimiento por defecto y de visualizacion, configurable por tipo de cultivo.** Peticion del PO (2026-07-29): que **por tipo de cultivo** se pueda configurar (a) el tipo de rendimiento que se ofrece por defecto al registrar y (b) el que se muestra en el **Dashboard y en las pastillas de informacion**, para unificar la lectura segun las preferencias del usuario. Hoy el MVP fija la unidad canonica L/100kg tanto para guardar como para mostrar (RN-013), y `MVP-402` solo hace configurable la **entrada** (`yield_unit`, RN-014): la visualizacion no es configurable a proposito, porque una unidad unica es lo que hace comparables dos campanas mientras no exista mas de un cultivo. Se convierte en necesario justo cuando se materialice el producto por Workspace de **P-060**, con el que comparte superficie: no tiene sentido configurar «por tipo de cultivo» antes de que exista el tipo de cultivo. Consolidar con **P-060** y **P-062**. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog (cluster del modelo de produccion, Hito I; depende de `P-060`). | medio | no | Backlog post-MVP (cluster modelo de produccion, Hito I) | backlog-post-mvp | - |
| P-064 | 2026-07-30 | MVP-005 / MVP-501 | calidad/testing | **No hay cobertura E2E de navegador.** `MVP-501` entrega smoke E2E **de servidor**: recorre el nucleo del MVP (login, Workspace, temporada, maestros, labor, cosecha, compra, imputacion, diario y dashboard) por la API real con `WebApplicationFactory`, pero no ejercita el cliente React en un navegador. Playwright —que `docs/04-ingenieria/estrategia-testing.md` nombra como herramienta E2E— se descarto en esa pasada por decision del PO: el login es **Google OIDC** y automatizarlo exige sembrar sesion inyectando un token de desarrollo en `sessionStorage`, lo que anade un camino de acceso que solo existe para el test. Consecuencia real: una regresion que solo se manifieste en el navegador (rutas, guardas de `App.tsx`, render de una vista) no la caza nadie salvo la QA manual. Hallazgo `F-03` de `MVP-501`. Debe tenerse presente al leer el gate de `MVP-504`, que exige «smoke E2E en verde». **Recogido en el gate (2026-08-03)** como **riesgo aceptado y declarado**, no como bloqueo: el smoke de servidor cubre la logica de punta a punta y el gate dice expresamente que «smoke E2E en verde» significa de servidor. Sigue pendiente como mejora post-MVP. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog. El motivo por el que se descarto Playwright sigue vigente: un camino de siembra de sesion que solo existe para el test es superficie de ataque a cambio de cobertura. Se comprobo ademas que **no habria cazado `P-107` ni `P-108`**, que se manifiestan solo con un `season_id` ajeno en la URL, algo que ningun recorrido feliz produce. La cobertura que si faltaba -tests que miren la consola- entra con `P-116`. | medio | no | Backlog post-MVP (riesgo aceptado en el gate de MVP-504) | backlog-post-mvp | - |
| P-065 | 2026-07-30 | MVP-005 / MVP-501 | ux/bug | **La etiqueta de caducidad de una invitacion miente el ultimo dia.** `expiresLabel` (`src/frontend/terrenario-web/src/lib/invitation-ui.ts`) calcula los dias con `Math.ceil` sobre una fraccion, asi que una invitacion que vence **hoy a las 18:00** rotula «Caduca manana», y «Caduca hoy» solo aparece cuando **ya ha caducado** —momento en el que el texto es directamente falso—. Impacto bajo (la invitacion sigue siendo aceptable o no con independencia del rotulo) pero es informacion incorrecta en la bandeja y en el modal de MVP-107. Detectado por el arnes de tests nuevo, al primer intento; el test lo fija como comportamiento actual con nota. Hallazgo `F-01` de `MVP-501`. **Resuelto en la misma pasada de MVP-501 (2026-07-31, decision del PO: no arrastrar deuda conocida al PR)**: la cuenta pasa a hacerse en **dias de calendario** (diferencia entre medianoches locales), que es como cuenta una persona —«manana» es el dia siguiente del calendario, no «dentro de mas de 24 horas»— y una invitacion vencida se rotula **«Caducada»**, coherente con el badge `CADUCADA` de «Miembros y accesos». Seis casos de regresion, incluidos los dos limites que fallaban. | bajo | no | MVP-005 / MVP-501 | resuelto | MVP-501 |
| P-066 | 2026-07-31 | MVP-005 / MVP-501 | seguridad/dependencias | **Aviso de seguridad *high* en `react-router`, corregido migrando a la linea 8.** `GHSA-qwww-vcr4-c8h2` (bypass de CSRF en modo RSC) afecta a `react-router` **7.12.0-8.2.0**; el proyecto tenia 7.18.1. No es explotable aqui —la aplicacion es una SPA y no usa modo RSC— pero es un CVE vivo que el gate de `MVP-504` tendria que justificar en cada release. **No hay salida dentro de 7.x**: la correccion esta en **8.3.0** y `react-router-dom` no publica 8.x, porque en v8 el paquete se consolido en `react-router`. Descartadas bajar a 7.11.0 (cambia deuda de seguridad por deuda de version, siete minors atras) y aceptar el aviso. **Resuelto en MVP-501 (2026-07-31)**: migracion de los 28 ficheros de `react-router-dom` a `react-router@8.3.0` —los 12 simbolos que usa el proyecto los exporta `react-router` directamente y el agrupador de chunks de `vite.config.ts` sigue casando—. `npm audit` pasa de 2 avisos high a **0 vulnerabilidades**. Al ser un salto de *major*, verificado en navegador real (guardas, rutas anidadas con `Outlet`, `NavLink`, `useNavigate`, `useSearchParams` con la persistencia de filtros de MVP-405 y la ruta comodin `/app/*`), no solo con el build. Se registra aunque nazca resuelto: es una decision de dependencia que conviene poder rastrear desde el gate. | medio | no | MVP-005 / MVP-501 | resuelto | MVP-501 |
| P-067 | 2026-07-31 | MVP-005 / MVP-502 | seguridad | **La Content-Security-Policy estaba donde no protege.** `SecurityHeadersMiddleware` (MVP-105, P-005) emite `Content-Security-Policy: default-src 'self'` en las respuestas de la **API**, pero esas respuestas son JSON: no son un contexto de ejecucion de scripts, asi que la politica ahi apenas protege de nada. El **documento HTML del SPA no tenia ninguna**, que es justo donde la CSP mitiga XSS —y donde mas importa en este producto, porque el token de acceso vive en `sessionStorage` y un script inyectado podria leerlo—. **Resuelto en MVP-502**: plugin `terrenario-csp` en `vite.config.ts` que inyecta la politica en el `index.html` solo en el build de produccion (en desarrollo Vite necesita scripts en linea para React Refresh y un WebSocket para el HMR). Verificada sobre el build servido de verdad con la API real: fuentes e iconos cargados, todas las llamadas en 200, cero violaciones. **Pendiente menor de despliegue**: lo correcto es que la CSP la emita como cabecera quien sirva el estatico; mientras esa capa no exista, el `meta` deja la politica aplicada y versionada con el codigo. Anotar en el gate de `MVP-504`. | medio | no | MVP-005 / MVP-502 | resuelto | MVP-502 |
| P-068 | 2026-07-31 | MVP-005 / MVP-502 | bug/funcional | **El sucesor del traspaso automatico de Workspace no era determinista.** `RN-038`/CA-5 promete que lo sea y el criterio era «el copropietario activo mas antiguo» por `joined_at`, pero dos personas pueden compartir `joined_at` —la resolucion del reloj es de milisegundos y un alta en lote entra a la vez—: con el orden en memoria el resultado dependia del orden fisico de las filas y **parecia** estable; al pasar el orden a SQL en `MVP-501` quedo a la vista. La regla nunca fue determinista, solo lo aparentaba. **Resuelto en MVP-502**: desempate por `UserId` en los **dos** sitios, el repositorio que traspasa (`FindOtherActiveOwnerAsync`) y el handler que **anuncia** al sucesor en la confirmacion de baja (`GetWorkspaceClosureOptionsHandler`); sin el segundo, la pantalla podia nombrar a una persona y el traspaso acabar en otra. Con test de regresion propio. **Correccion en MVP-506 (2026-07-31)**: el arreglo de MVP-502 estaba **a medias**. `Guid.CompareTo` de .NET **no ordena igual** que el tipo `uuid` de PostgreSQL, asi que el repositorio (que ordena en SQL) y el handler que anuncia al sucesor (que reproducia el criterio en memoria) podian elegir personas distintas: exactamente el defecto que este punto venia a cerrar. Se corrige de raiz haciendo que el handler **pregunte al repositorio** en vez de repetir la regla —un unico sitio decide, asi que no puede haber discrepancia— y que los tests comprueben el criterio contra la base de datos en vez de reproducirlo con LINQ. | medio | no | MVP-005 / MVP-502 (corregido en MVP-506) | resuelto | MVP-506 |
| P-069 | 2026-08-03 | MVP-005 / MVP-505 | tecnico/entorno | **Smart App Control de Windows bloquea el arnes de tests de backend.** Al cerrar MVP-505, la suite paso de **666 en verde** a **156 fallos** sin ningun cambio de codigo: `System.IO.FileLoadException ... Una directiva de Control de aplicaciones bloqueo este archivo (0x800711C7)` sobre `Testcontainers.PostgreSql.dll`. Confirmado en el registro de Code Integrity (eventos 3077/3118) y con `VerifiedAndReputablePolicyState = 1`: **Smart App Control esta activo** y ha decidido bloquear ese ensamblado por reputacion. Los 156 fallos son **todos** el mismo fallo de carga del ensamblado, ninguno de logica; los 510 tests que no dependen de Testcontainers siguen pasando. **Consecuencia**: la decision de `MVP-501` de montar la integracion sobre Testcontainers (que cerro `P-031`) introduce una dependencia que la politica de seguridad de la maquina de desarrollo puede cortar en cualquier momento, sin aviso y sin cambio de codigo. Smart App Control **no admite exclusiones por fichero** y solo se puede desactivar de forma **irreversible** (Windows no permite volver a activarlo), asi que no es una decision de desarrollo. Opciones a decidir por el PO: (a) ejecutar la suite de backend en CI sobre Linux, donde no aplica, y aceptar que en local solo corran los 510 unitarios; (b) desarrollar en una maquina o WSL sin esta politica; (c) revertir a un arnes sin Testcontainers, lo que reabriria `P-031`. Debe resolverse **antes** de `MVP-504`, porque su gate exige la suite en verde. **Desbloqueado (2026-08-03)**: el PO **desactivo Smart App Control** y la suite vuelve a pasar entera (666 en verde), lo que confirma que ninguno de los 156 fallos era de logica. **El punto no se cierra**: la causa de fondo sigue ahi. El arnes depende de un ensamblado sin reputacion que cualquier politica de Application Control puede volver a bloquear —en otra maquina de desarrollo, o si se reactiva la proteccion— y la desactivacion de Smart App Control es **irreversible** en Windows, asi que la maquina se queda sin esa capa de proteccion. Queda como **riesgo declarado del arnes** para el gate de `MVP-504`: hay que dejar constancia de que la suite de backend exige Docker y una politica de Application Control permisiva, y de que el CI sobre Linux es el entorno de referencia. **Recogido en el gate (2026-08-03)**: `ci.yml` ejecuta la suite de backend en **Linux**, donde la politica no aplica, asi que el entorno de referencia queda establecido y el riesgo pasa a ser solo de desarrollo local. Declarado como riesgo aceptado en el gate. **Retriado en la revision del MVP (2026-08-10)**: se cierra como **riesgo aceptado y documentado**. Comprobado el 2026-08-10: Smart App Control esta **desactivado** en la maquina de desarrollo (`VerifiedAndReputablePolicyState = 0`) y `ci.yml` ejecuta la suite de backend en **Linux**, donde la politica no aplica, asi que el entorno de referencia esta establecido y no queda trabajo pendiente. Mantenerlo abierto sin accion posible es justo lo que `P-096` senalo como el problema del registro. La fila pasa a `resuelto` porque el gate ya lo recogio el 2026-08-03 y llevaba desde entonces diciendo lo contrario -el mismo patron que `P-096` describe-. Unica tarea residual, anotada en `MVP-008`: dejar escrito en `desarrollo-local.md` que la suite exige Docker y una politica de Application Control permisiva. **Tarea residual cerrada en `MVP-809` (2026-08-10)**: `docs/05-infraestructura/desarrollo-local.md` ya deja escrito que la suite de backend exige Docker en marcha y una politica de Application Control permisiva, que el CI sobre Linux es el entorno de referencia y como se reconoce el fallo (`0x800711C7` sobre `Testcontainers.PostgreSql.dll`) para no confundirlo con un fallo de logica. | alto | no | MVP-005 / MVP-504 (riesgo aceptado y documentado en el gate) | resuelto | MVP-504; nota de entorno en MVP-809 |
| P-070 | 2026-08-03 | MVP-005 / MVP-503 | producto | **Exportacion de los datos de la explotacion.** Nacio como hallazgo de cumplimiento (`R-06` de `MVP-503`: sin exportacion, el derecho de portabilidad del art. 20 no se puede ejercer desde el producto) y **se replantea en el gate de `MVP-504` (2026-08-04)** tras analizar el alcance real del art. 20. **La obligacion legal ya no esta aqui**: se atiende por via manual, con procedimiento documentado en `docs/07-seguridad/privacidad-datos.md` (que se entrega, en que formato y en que plazo). Lo que queda es una **funcion de producto**, mas amplia y mas valiosa que lo que exige la norma: que alguien pueda llevarse los datos de su explotacion y no sentirse encerrado. El art. 20 resulto ser mas estrecho de lo que parecia —cubre lo que la persona aporto, **no lo derivado** (los agregados del dashboard quedan fuera) y **no puede perjudicar derechos de terceros**—, y ahi esta la dificultad real: los nombres de la cuadrilla (`workers.name`) y de los propietarios de terrenos cedidos (`plots.owner_name`) son de otras personas, y un Workspace compartido contiene lo que registraron otros. **Decision de diseno previa e ineludible**: cual es la unidad de exportacion, la persona o la explotacion. Con la explotacion como unidad el resultado es util y coherente, pero hay que resolver que se hace con las aportaciones de otros miembros; con la persona como unidad se cumple la norma pero se entrega algo que no le sirve a nadie. Alcance estimado: exportar en formato interoperable (JSON o CSV) terrenos, temporadas, labores, cosechas, compras y consumos de los Workspaces de su propiedad, con la decision anterior tomada. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog. La obligacion legal esta cubierta por via manual, asi que no hay reloj corriendo, y con explotaciones de una sola persona la exportacion por explotacion y por persona coinciden: hoy no se aprenderia nada del caso dificil, que es justo el que decide el modelo. | medio | no | Backlog post-MVP (exige decidir antes la unidad de exportacion) | backlog-post-mvp | - |
| P-071 | 2026-08-04 | MVP-005 / MVP-504 | cumplimiento | **Los refresh tokens revocados o caducados no tienen plazo de expurgo.** Detectado al implementar la rutina de expurgo de `RN-041` (bloqueo `B-3` del gate). La tabla de retencion de `privacidad-datos.md` dice «hasta caducidad o revocacion», pero una vez caducado o revocado el hash se queda en `refresh_tokens` indefinidamente: se limpia solo si se purga la cuenta entera, que arrastra la tabla por cascada. No se incluyo en la rutina **a proposito**: `RN-041` enumera cinco categorias y esta no es una de ellas, asi que anadirla es una decision de producto, no de desarrollo. Aplicarles los mismos 24 meses seria coherente pero conservador de mas para un dato de sesion; un plazo corto (dias o semanas tras la caducidad) es mas defendible. Alcance: decidir el plazo, anadirlo a `RN-041` y una linea en `RetentionPurgeService`. **Resuelto en MVP-714 (2026-08-08)**: sexta categoria de `RN-041` con plazo propio de **30 dias** desde la revocacion o la caducidad, lo primero que ocurra (`AccountRetentionPolicy.RefreshTokenRetentionDays`), aplicada por `RetentionPurgeService` y contada aparte en el informe de la rutina. Los 30 dias son el mismo orden que la vida del propio token, asi que un token muerto no dura mas de lo que habria durado vivo, y dejan cuatro ciclos de la revision operativa semanal para investigar una sesion sospechosa. **Corrige ademas una suposicion de este propio punto**: se afirmaba que purgar la cuenta arrastraba los tokens por cascada, pero `refresh_tokens` **no tiene FK** hacia `users` (verificado en la migracion `InitialAuth` y en el snapshot del modelo), asi que las filas quedaban huerfanas indefinidamente; el plazo propio cierra tambien ese hueco. Verificado con datos sembrados contra PostgreSQL real, no por lectura del codigo: tres tests de integracion (caducado, revocado, muerto dentro de plazo, vivo y la frontera exacta del dia 30), comprobados ademas por mutacion. | bajo | no | MVP-714 | resuelto | MVP-714 |
| P-072 | 2026-08-04 | MVP-005 / MVP-599 | ux/frontend | **Los filtros y la pagina del diario no persisten en la URL, y los del dashboard si.** Detectado en la revision de cierre de la epica (`R-01`). Cambiar de pagina, buscar o filtrar por responsable en `/app/diario` deja la URL sin rastro; la misma accion en Vision General produce `/app/vision-general?season_id=...`, porque `VisionGeneralView` usa `useSearchParams` como fuente unica y asi se decidio expresamente. No es un defecto de `MVP-506` —sus criterios no lo pedian— pero es una **decision ya tomada que no se aplico a la vista que mas la necesita**: el diario tiene cinco filtros y paginacion, asi que es donde mas duele no poder compartir un enlace ni volver a la pagina 3 despues de abrir un registro. Alcance: llevar `type`, `plot_id`, `season_id`, `worker_id`, `search` y `page` a la URL como fuente unica, **conviviendo con el rebote de 350 ms de la busqueda y con la guarda de respuestas obsoletas de `MVP-506`** (que se anadio por un fallo real: una respuesta vieja dejaba el muro vacio). Ojo tambien a no ensuciar el historial del navegador con una entrada por pulsacion. **Resuelto en MVP-705 (2026-08-08)**: `lib/diary-url-state.ts` lleva filtros y pagina a la URL con el mismo criterio que ya usaba el dashboard. Los cambios de filtro y de pagina apilan historial (`push`) y la busqueda lo reemplaza (`replace`), para que «atras» no deshaga letra a letra lo tecleado. Los valores por defecto **no se escriben** en la URL: una URL limpia sigue significando «lo de siempre». | medio | no | MVP-705 | resuelto | MVP-705 |
| P-073 | 2026-08-06 | MVP-006 / MVP-699 | observabilidad | **Parte del desglose de observabilidad solo es consultable por SQL.** Detectado en la revision de cierre (`R-01`), contrastando las metricas almacenadas contra la respuesta de `GET /api/v1/ops/signals`. El informe publica los agregados, pero **no** el desglose: que widget concreto falla (`dashboard.widget.{widget}.{estado}`), que codigo de error de Google causa los fallos del embudo (`login.error.{codigo}`), que recurso se esta creando (`api.created.{recurso}`), el historial de cuantas veces ha saltado cada alerta (`alert.fired.{nombre}`), las visitas frente a las sesiones (`dashboard.viewed`) y el histograma de latencia completo (solo se deriva el P95). Consecuencia: ante una cobertura de widgets por debajo del 100 % se sabe que algo falla pero no que, y hay que abrir la base de datos. `R-01` corrigio lo que bloqueaba el baseline (la serie diaria); esto es el resto. Alcance: exponer los desgloses en el mismo informe, con cuidado de no convertirlo en un volcado de la tabla. **Decision del PO (2026-08-06): se difiere aqui**, la serie diaria era lo urgente. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog. El desglose esta disponible -incomodo, no ausente- para quien hace la revision semanal, y su valor real depende de `P-074`: exponer mas detalle en un JSON que se consulta con `curl` mueve poco frente a exponerlo en una pantalla. | bajo | no | Backlog post-MVP (con P-074) | backlog-post-mvp | - |
| P-074 | 2026-08-06 | MVP-006 / MVP-699 | ux/observabilidad | **No hay ninguna interfaz para las senales operativas.** Confirmado por `grep`: ninguna pantalla del cliente consume `/api/v1/ops/signals`. La revision semanal de 15 minutos que exige `observabilidad.md` se hace con `curl` y una llave de servicio. **No es un incumplimiento**: la KB declara los dashboards «N/A en fase C (revision manual de logs y metricas)», asi que un endpoint JSON cumple lo especificado. Pero conviene decidir si cumple lo que hace falta. Alcance: una pantalla interna de operacion. **Abre una pregunta que hoy no tiene respuesta**: quien puede verla. El producto no tiene roles (`autenticacion-autorizacion.md` los declara pero el MVP solo controla pertenencia al Workspace), asi que una pantalla asi exigiria antes decidir el modelo de acceso, o vivir fuera del producto. **Decision del PO (2026-08-06): se difiere aqui.** **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog. El bloqueo no es de esfuerzo sino de diseno: sin roles, una pantalla de operacion dentro del producto la veria cualquier miembro de cualquier Workspace, y anadir roles solo para esto es abrir `RU-13` por la puerta de atras. | bajo | no | Backlog post-MVP (exige decidir antes el modelo de acceso) | backlog-post-mvp | - |
| P-075 | 2026-08-06 | MVP-006 / MVP-602 | ux/tecnico | **Un widget roto deja el dashboard entero en error.** Las cuatro peticiones de Vision General van en un unico `Promise.all`, asi que si una falla se descarta el resultado de las otras tres y la pantalla muestra solo el mensaje de error. `MVP-602` lo asume al medir (informa los cuatro widgets como `error`), pero la experiencia es peor de lo necesario y la medida tambien: no se puede atribuir el fallo al widget que lo causo, que es justo lo que pide el KPI de cobertura. Propuesta: `Promise.allSettled`, pintando los widgets que si resolvieron y un estado de error acotado al que fallo. No se hizo en `MVP-602` porque cambia el comportamiento de una pantalla ya entregada, lo que excede el alcance de una historia de medicion. Enlaza con `P-073` (el desglose por widget). **Resuelto en MVP-706 (2026-08-08)**: `Promise.allSettled` en vez de `Promise.all`, con `WidgetResult<T>` por widget y un `WidgetError` que se pinta en el hueco del que falla. Un widget roto ya no descarta los otros cuatro. El ambito se deriva de cualquiera de las respuestas que si llegaron, asi que la cabecera tampoco depende de que responda uno concreto. | medio | no | MVP-706 | resuelto | MVP-706 |
| P-076 | 2026-08-06 | MVP-006 / MVP-603 | seguridad/privacidad | **Una direccion de correo personal esta versionada en un repositorio publico.** Detectada al decidir donde configurar `Ops__AlertEmail`: `prototype/terrenario-mvp/src/data/initialData.ts` contiene la direccion personal del titular en los datos de ejemplo del prototipo. El prototipo no se despliega, pero el repositorio es **publico** y el valor esta en el historial de git, donde lo recogen los rastreadores de spam. Borrarlo del fichero **no lo saca del historial**: haria falta reescribir la historia del repositorio, que es una decision con coste (invalida clones y referencias existentes). Opciones: (a) sustituirlo por una direccion de ejemplo y aceptar que el historial la conserva, (b) reescribir el historial, (c) no hacer nada por ser un dato ya publico del titular. Requiere decision del PO. **Resuelto en MVP-714 (2026-08-08)** con la opcion (a), decidida por el PO: `userEmail` pasa a `juan.perez@ejemplo.test` —coherente con el `userName` de la maqueta y sobre un TLD reservado, no entregable ni registrable— y queda anotado en el propio fichero que **el historial de git conserva la direccion original**, porque reescribirlo invalidaria clones, tags y referencias existentes (incluidos los tags de release desde los que se despliega) sin recuperar lo ya copiado. Barrido de comprobacion sobre `src/`, `prototype/`, `docs/` e `infra/`: era la unica direccion personal filtrada. La otra direccion real que aparece, `hola@andresgilabert.dev` en `legal-entity.ts`, **se conserva a proposito**: es el contacto del responsable del tratamiento que la LSSI y el RGPD obligan a publicar, esta versionado con ese motivo escrito y ya es publico en la Politica de Privacidad. | bajo | no | MVP-714 | resuelto | MVP-714 |
| P-077 | 2026-08-06 | MVP-006 / MVP-603 | observabilidad/infra | **Una caida total no avisa a nadie.** `MVP-603` deja las alertas dentro de la aplicacion, asi que si el proceso muere no queda quien envie el correo; es el punto ciego declarado en `observabilidad.md`. La sonda de salud que configura la historia hace que App Service **reinicie** la instancia, pero **no notifica**. Consecuencia: la alerta `ServiceDown` de la KB solo cubre la degradacion observable desde dentro (base de datos inalcanzable), no la caida completa. Alcance: una regla de Azure Monitor sobre el health check que envie aviso al mismo destinatario de `Ops__AlertEmail`. Es infraestructura, no codigo, y quedo fuera del alcance de `MVP-603`. **Retriado en la revision del MVP (2026-08-10)**: se mantiene en backlog. Con pocos usuarios reales, enterarse por el propio uso se considera aceptable; el punto ciego sigue declarado en `observabilidad.md`. | medio | no | Backlog post-MVP | backlog-post-mvp | - |
| P-078 | 2026-08-06 | MVP-006 / MVP-699 | tecnico/observabilidad | **La senal de «sesion activa» nunca se emite en onboarding, y el codigo dice lo contrario.** Hallazgo `R-02`. `TelemetryController` documenta que no exige Workspace activo «a proposito: una sesion en onboarding tambien es una sesion activa, y dejarla fuera del divisor subiria el KPI de uso del dashboard justo con los casos en los que el producto aun no sirve de nada». Pero `app_session_started` se emite desde `AppLayout`, que esta anidado dentro de `RequireWorkspace` (`App.tsx`), y esa guarda redirige a `/onboarding` cuando no hay Workspace activo. Resultado: el endpoint lo permite y el cliente no lo manda nunca, asi que el divisor sigue excluyendo exactamente lo que se dijo que incluia. El KPI «uso del dashboard en sesiones activas» mide en realidad «de las sesiones que llegaron al area operativa, cuantas abrieron el panel», que es una pregunta mas estrecha y mas favorable. Dos salidas posibles: (a) emitir la senal desde un punto que cubra tambien las sesiones sin Workspace, o (b) aceptar la definicion estrecha y corregir el comentario y la KB, que hoy afirman otra cosa. **Requiere decision de producto sobre que es una «sesion activa»**, por eso no se corrige en la revision. **Resuelto en MVP-703 (2026-08-08)**: la senal se emite donde de verdad empieza la sesion de trabajo. El codigo y la documentacion decian cosas distintas y ahora dicen la misma. | medio | no | MVP-703 | resuelto | MVP-703 |
| P-079 | 2026-08-06 | MVP-006 / MVP-699 | bug/observabilidad | **Un codigo de autorizacion de Google caducado o reusado devuelve 500 y puede disparar una alerta critica.** Hallazgo `R-04`. `GoogleOidcService` mapea **cualquier** respuesta no exitosa del endpoint de token de Google a `AUTH_GOOGLE_EXCHANGE_FAILED`, que `AuthController` traduce a **HTTP 500**. Eso incluye `invalid_grant`, que es lo que Google devuelve ante un codigo ya usado o expirado: basta con recargar la pantalla de callback. Consecuencias: cuenta contra el SLO de tasa de error (objetivo < 0,1 %) y contra `HighErrorRate`, que es **critica**. Verificado en la revision: un solo 500 de este tipo sobre 70 peticiones dio 1,43 % y disparo la alerta, con envio de correo real. Es comportamiento anterior a la epica (MVP-101/MVP-502), pero solo tiene consecuencia desde `MVP-603`, porque antes no lo miraba nadie. Alcance: distinguir los errores de cliente del vocabulario cerrado de OAuth 2.0 (`invalid_grant`, `invalid_request` -> 401/400) de los de servidor o configuracion (`invalid_client`, caida de Google -> 500). Toca autenticacion, fuera del alcance de una epica de observabilidad. **Resuelto en MVP-713 (2026-08-08)**: la clasificacion pasa a una tabla unica (`GoogleOAuthErrors` mas `GoogleOidcErrorMapper`) en vez de repartirse en clausulas `catch ... when`, con el **500 como defecto** para lo no clasificado: un fallo propio contado como error de cliente desapareceria de las alertas, que es peor que lo contrario. `invalid_grant` responde 401 `AUTH_GOOGLE_CODE_INVALID` y la pantalla de callback dice que hay que volver a entrar; `invalid_request`, 400. Reproducido el escenario de la revision en test —69 peticiones mas un codigo caducado— y `HighErrorRate` ya no se dispara, mientras que con `invalid_client` sigue disparandose. El nivel de log se clasifica igual: recargar la pantalla ya no escribe un `Warning`. | medio | no | MVP-713 | resuelto | MVP-713 |
| P-080 | 2026-08-06 | MVP-999 / revision del MVP (PO) | ux/marca | **El icono de pestaña es el logo de Vite, no el de Terrenario.** El PO lo reporto como «falta favicon»; verificado que el fichero **si existe y se sirve** (`GET https://app.terrenario.com/favicon.svg` -> 200, 9.522 bytes), pero su contenido es el rayo degradado morado/azul del andamiaje (`#863bff`, `#47bfff`, `#7e14ff`), versionado desde `MVP-101` y nunca sustituido. La marca real es verde oliva (`#33450d`) con el icono `eco`. En la misma superficie faltan: `apple-touch-icon`, `manifest.webmanifest` con iconos (sin el, anadir la aplicacion al inicio de un movil la deja como marcador con captura de pantalla, que pesa en una herramienta de campo), `theme-color`, y `<meta name="description">` / Open Graph en una landing **publica** (compartir el enlace por WhatsApp —canal natural entre agricultores— muestra una tarjeta vacia). Sin impacto funcional: es identidad de producto. **Resuelto en MVP-710 (2026-08-08)**: el favicon pasa a ser el distintivo que la aplicacion ya pinta —baldosa `#33450d` con el glifo `eco`, el mismo de `AppSidebar`, `HomeView`, `LoginPage` y `LandingPage`— en SVG, con respaldo `.ico` de 16/32/48 escrito como DIB y validado leyendolo con `System.Drawing.Icon`. Se anaden `apple-touch-icon` de 180 px a sangre, iconos de 192/512 y un 512 `maskable`, `manifest.webmanifest` (nombre, colores, `start_url` `/app`), `theme-color`, y `description`, Open Graph y Twitter Card con `og-image.png` propia de 1200x630. **Todo autoalojado**: la CSP se leyo del build (`dist/csp.policy`) y sigue siendo `default-src 'self'` sin relajar ninguna directiva, asi que `RN-042` no se reabre. Dos huecos que el punto no mencionaba y se cierran de paso: la guarda `sin-recursos-externos.test.ts` solo miraba `src/` —los iconos y el manifest viven fuera, que es por donde ya se colo la foto de Unsplash— y `UseStaticFiles` responde 404 a las extensiones que su proveedor de tipos no conoce, lo que habria dejado el manifest sin servir en un fallo que nadie ve. Pendiente de la comprobacion del PO sobre el build servido y sobre un movil real (`CA-5`). | bajo | no | MVP-710 | resuelto | MVP-710 |
| P-081 | 2026-08-06 | MVP-999 / revision del MVP (PO) | bug/ux | **Cambiar de Workspace no recarga las vistas: sigue viendose la informacion del anterior.** Causa raiz localizada: `ApiProvider` memoiza el cliente HTTP con `useMemo(..., [])`, asi que su identidad **nunca cambia**; los `*.service` derivan de el con `useMemo([http])` y los `reload` de cada vista con `useCallback([service, ...filtros])`. Ninguna cadena incluye el Workspace, de modo que el efecto de carga no se vuelve a disparar. **Nueve de las diez vistas operativas no mencionan el Workspace en ninguna dependencia** (Diario, Cosechas, Compras, Terrenos, Temporadas, Trabajadores, Tareas, Miembros y Vision General); solo `HomeView`, `AjustesView` y `SeasonContext` se rehacen con `activeWorkspace?.id`. Agravante: `switchWorkspace` **si** reemite la sesion en el destino, asi que durante ese lapso se puede pulsar «corregir» o «eliminar» sobre una fila del Workspace anterior —el servidor la rechazara con `AUTH_WORKSPACE_FORBIDDEN`, pero el usuario esta operando sobre datos que ya no son los suyos— y un alta lanzada desde el formulario abierto llevaria `plot_id`/`season_id` del Workspace viejo. **Resuelto en MVP-701 (2026-08-08)**: `DataScopeContext` con un contador de version y remontaje del arbol de rutas (`<Fragment key={workspace:version}>`) en vez de recarga. Se eligio **remontar y no recargar** porque una recarga tambien vaciaria filas, formularios y confirmaciones a medias, que es el agravante del punto. Verificado en MVP-799: con un modal abierto el conmutador **ni siquiera se alcanza** —esta dentro del subarbol inerte de `MVP-704`, no puede recibir foco y un clic aterriza en el velo—, asi que la parte mas fea del agravante quedo directamente inalcanzable. | alto | no | MVP-701 | resuelto | MVP-701 |
| P-082 | 2026-08-06 | MVP-999 / revision del MVP (PO) | bug/ux | **El ambito de temporada es incoherente entre pantallas y produce dos totales distintos de la misma explotacion.** El PO lo reporto sobre Cosechas; la revision confirma que el problema es transversal: `CosechasView`, `DiarioView` y `ComprasView` arrancan con `seasonFilter = 'todas'`, mientras `VisionGeneralView` arranca con la **temporada de trabajo** del usuario porque el servidor resuelve el defecto (RN-008, MVP-209). Medido hoy en el Workspace «Rafa», el mismo dia y con los mismos datos: `GET /dashboard/summary` devuelve **4.460,50 kg · 4 partidas · 20,86 L/100kg** (Campana 2026) y `GET /harvests` sin filtro devuelve **5.460,5 kg · 5 partidas**, que es lo que rotula la pantalla de Cosechas, con un rendimiento medio de 20,0 L/100kg. La diferencia es una partida de 1.000 kg de «Campana 2025» colada en el total y en la media ponderada. No es una preferencia de filtro: dos pantallas del producto responden distinto a «cuanto llevo esta campana». Decidir si el defecto pasa a ser la temporada de trabajo en las tres vistas (coherente con RN-008 y con la pildora de la cabecera) o si se declara que esas vistas son historico completo y se rotula asi. **Resuelto en MVP-701 (2026-08-08)**: el defecto de temporada lo resuelve el **servidor** y devuelve el ambito aplicado en `meta.scope`, para que la regla no viva en dos sitios y vuelva a divergir. `RN-008` reescrita en consecuencia. Verificado en MVP-799 contra la API con datos reales: para la misma campana, kg, numero de cosechas, gasto e ingresos **coinciden** entre diario, cosechas, compras, consumos, `dashboard/summary` y `dashboard/economics`, y tambien con el calculo a mano sumando fila a fila. La unica diferencia entre diario (390 EUR) y compras (300 EUR) es la mano de obra de las actividades, que es lo que el diario incluye por ser el libro unificado. | alto | no | MVP-701 | resuelto | MVP-701 |
| P-083 | 2026-08-06 | MVP-999 / revision del MVP (PO) | ux | **No se puede cambiar de temporada desde la cabecera.** La pildora de temporada de `AppTopbar` es un `<span>` decorativo cuando **si** hay temporada de trabajo, y solo se vuelve pulsable cuando **no** la hay (y entonces lleva a crear o elegir). Cambiar de campana obliga a ir a Temporadas y pulsar «Activar», tres pasos fuera del flujo. Desde `MVP-209` la temporada de trabajo es **por usuario** y es el contexto que gobierna la autoseleccion al registrar y el defecto del dashboard: es exactamente la clase de conmutador que pertenece al shell, como el de Workspace. Peticion del PO. Consolidar con **P-082**: si el defecto de las vistas pasa a ser la temporada de trabajo, el conmutador de cabecera deja de ser comodidad y pasa a ser necesario. **Resuelto en MVP-701 (2026-08-08)**: `SeasonSwitcher` en la cabecera, con la campana de trabajo, su estado y el cambio en un clic. Verificado en MVP-799 sobre la aplicacion: cambiar de campana desde el shell deja diario, cosechas y panel mostrando la elegida **sin una sola recarga** (`performance.getEntriesByType('navigation').length` sigue en 1). | medio | no | MVP-701 | resuelto | MVP-701 |
| P-084 | 2026-08-06 | MVP-999 / revision del MVP (PO) | funcional/alcance | **La cosecha no admite importe por kg, y el producto no mide ingresos en absoluto.** Peticion del PO sobre el destino «Venta de aceituna». Verificado: `HARVEST` no tiene ningun campo economico —ni el modelo, ni el contrato, ni el formulario— porque `RN-029` limita la produccion a producto, kilos, destino y uno entre rendimiento o litros, «sin capa comercial ni de molturacion». La consecuencia va mas alla del campo pedido: el MVP **solo registra gasto** (compras y consumos, `RN-003`) y **ningun ingreso**, asi que la pregunta «¿me ha salido a cuenta la campana?» no tiene respuesta en el producto. Alcance minimo: precio por kg en las cosechas con destino de venta e importe derivado. Alcance coherente: el margen de la campana en la Vision General, que arrastra **P-094**. Comparte superficie con el clúster post-MVP `P-059`..`P-063` (el maestro de almazaras y empresas compradoras de `P-062` es donde vive el precio pactado). **Resuelto en MVP-707 (2026-08-08)**: `unit_price` opcional en la cosecha, con el importe **derivado** y no almacenado —guardarlo permitiria que divergiera de sus dos factores tras una correccion—. `null` significa «no se sabe» y no cero. `RN-029` matizada. Verificado en MVP-799 poniendo precio a dos partidas: los ingresos calculados a mano coinciden exactamente con los del diario y con los del panel economico. | medio | no | MVP-707 | resuelto | MVP-707 |
| P-085 | 2026-08-06 | MVP-999 / revision del MVP (PO) | ux/observabilidad | **¿Tiene sentido el boton «Actualizar» de la Vision General?** Pregunta del PO. Existe **por diseno**: `RN-006` decide que no hay refresco en segundo plano y que recargar es un acto explicito. Pero tiene una atadura que no es obvia desde la pantalla: es la **unica fuente** de la senal `dashboard.manual_refresh`, que `MVP-602` (CA-2) usa para medir el uso del panel; cambiar un filtro tambien relanza la carga y **a proposito no cuenta**, porque es otra pregunta. Retirarlo deja ese KPI sin numerador y obliga a rehacer `RN-006` y el criterio de MVP-602. Decidir entre: conservarlo, sustituirlo por un refresco al recuperar el foco de la ventana (que habria que instrumentar igual), o retirarlo asumiendo el cambio de regla y de metrica. **Resuelto en MVP-706 (2026-08-08)**: el Product Owner decidio **retirar** el boton «Actualizar». El motivo es el perfil de uso —explotaciones pequenas, donde no es habitual que unos introduzcan datos mientras otros esperan—: un control que casi nadie necesita ocupa sitio y hace creer que sin pulsarlo los datos estan viejos. `RN-006` lo recoge y `dashboard.manual_refresh` queda discontinuada, que era su unica fuente. | bajo | no | MVP-706 | resuelto | MVP-706 |
| P-086 | 2026-08-06 | MVP-999 / revision del MVP (PO) | ux | **En escritorio se desaprovecha la mayor parte del ancho.** Causa exacta: `AppLayout` encierra **todas** las secciones en un contenedor `max-w-3xl` (768 px). Verificado en navegador a 1920x1000: con el lateral de ~256 px, el contenido ocupa 768 px y el resto es fondo. Duele especialmente donde hay tabla —Cosechas, Terrenos, Trabajadores, Miembros— y en la rejilla `lg:grid-cols-4` del dashboard, que apretuja cuatro tarjetas en 768 px porque el breakpoint de Tailwind mira el **viewport**, no el contenedor. El contenedor unico se introdujo en `P-016` para dar coherencia de tamano y espaciado entre secciones, que sigue siendo el objetivo correcto: lo que hay que revisar es la cota, probablemente por tipo de contenido (formulario estrecho / listado ancho), no retirarla. **Resuelto en MVP-702 (2026-08-08)**: dos cotas de ancho **por tipo de contenido** —1280 px para listados y panel, 768 para formularios y lectura— en vez de un unico `max-w-3xl` para todo. Las rejillas pasan a consultas de contenedor (`@container`), asi que las columnas las decide el ancho **real disponible** y no el del viewport, que era lo que apretujaba las tarjetas del panel en 768 px con la pantalla a 1920. | medio | no | MVP-702 | resuelto | MVP-702 |
| P-087 | 2026-08-06 | MVP-999 / revision del MVP (PO) | ux | **La pantalla de arranque es el dashboard, que esta vacio la mayor parte del ano.** Desde `MVP-499` (`P-040`), con los maestros poblados el Home **es** la Vision General. Como la cosecha se concentra al final de campana, durante la mayor parte del ano lo primero que se ve al entrar es «Sin cosechas en {temporada}». Ademas contradice a `RN-033`, que declara el **diario cronologico unificado** como vista principal del MVP: el producto dice que su vista principal es el diario y arranca en otra. Propuesta del PO: que el arranque sea el Diario de campo. A decidir tambien si el checklist de preparacion sigue siendo la primera cara mientras falten maestros (hoy lo es) y si conviene que el destino de arranque sea configurable. **Resuelto en MVP-703 (2026-08-08)**: con la explotacion preparada, `/app` redirige al diario (`RN-033`), que es donde se entra a trabajar; el panel queda a un clic en la navegacion lateral. Verificado en MVP-799 en los dos estados: con una tarea pendiente en el catalogo se sigue viendo el checklist —que es lo correcto, porque queda algo que preparar— y en cuanto se completa, `/app` lleva al diario. | medio | no | MVP-703 | resuelto | MVP-703 |
| P-088 | 2026-08-06 | MVP-999 / revision del MVP (PO) | producto/soporte | **No hay ningun canal para que el usuario reporte un fallo o pida algo.** Confirmado por busqueda en todo el cliente: no existe «Ayuda», «Contacto», «Soporte» ni `mailto` fuera de las paginas legales; la unica direccion publicada es la de **privacidad**, en la Politica, que es un canal de derechos RGPD y no un canal de producto. Un usuario real que se topa con un fallo no tiene donde decirlo, y el equipo se entera solo por las alertas de `MVP-603`, que ven caidas y errores 5xx pero no «esto no se entiende» ni «me falta esto». Peticion del PO: buscar un sistema de tickets gratuito. Ojo al encaje legal: cualquier formulario que recoja el correo del usuario es tratamiento de datos y toca `privacidad-datos.md` y `RN-042` si el proveedor es de terceros y carga scripts. **Resuelto en MVP-711 (2026-08-08)**: formulario propio en «Sugerencias e incidencias», entrada visible en la navegacion lateral y envio por correo al buzon `Feedback__Recipient`, que vive fuera del repositorio con el mismo criterio que `Ops__AlertEmail`. La peticion de «sistema de tickets gratuito» se resolvio **sin tercero**, decision del PO del 2026-08-06 recogida en el spec: cualquier proveedor externo seria un encargado del tratamiento nuevo y, si carga scripts, activaria `RN-042` y obligaria al banner de cookies que `MVP-505` evito. El reporte llega con contexto tecnico —version desplegada, pantalla, `X-Request-Id` del ultimo fallo y navegador— y **sin nada de la explotacion**: la query de la URL se recorta en servidor porque los filtros del panel llevan identificadores de terreno. El tratamiento queda descrito en `privacidad-datos.md` con base (interes legitimo) y plazo (24 meses en el buzon, mismo criterio que `RN-041`), y no anade ninguna fila al inventario de almacenamiento en el navegador: el contexto vive en memoria de la pestaña. | medio | no | MVP-711 | resuelto | MVP-711 |
| P-089 | 2026-08-06 | MVP-999 / revision del MVP (PO) | ux/acceso | **Una cuenta que no sea de Google no puede entrar, y el producto no explica la salida que si existe.** `RN-018`/`RN-036` fijan Google OIDC como unico proveedor del MVP, asi que la limitacion es una decision, no un defecto. Pero tiene dos consecuencias que si son de producto: **(a)** un correo de Hotmail/Outlook **si puede** usar la aplicacion creando una Cuenta de Google con esa direccion, y ni el login ni la landing lo dicen —«accede directamente con tu cuenta de Google» se lee como «necesitas un Gmail»—; **(b)** invitar a una direccion que no tenga Cuenta de Google es un **callejon sin salida**: la comprobacion de aptitud exige coincidencia de correo y responde «Esta invitacion esta dirigida a otra cuenta de correo. Entra con esa cuenta para aceptarla», sin decir que la salida es dar de alta esa direccion en Google. Alcance minimo: explicarlo en login, en la landing y en el aviso de aptitud. Alcance mayor: un segundo proveedor (Microsoft) o enlace magico por correo, que es evolucion posterior por `RN-036` y arrastra `RN-019` (passkeys). **Resuelto en MVP-712 (2026-08-08)**: se dice en las **cuatro** superficies —login, landing publica, aviso de aptitud y correo de invitacion, que el alcance minimo no contaba y es donde llega la persona por primera vez—, con la misma frase, que sale de una constante compartida (`lib/google-account.ts`) para que ninguna se quede corta: «No hace falta que tu correo sea de Gmail: sirve el de Hotmail, Outlook o el de tu cooperativa, siempre que des de alta esa misma direccion como Cuenta de Google. Es gratis y no crea un buzon nuevo», con enlace a `https://accounts.google.com/signup`. El aviso de `email_mismatch` **no se ramifica**: sus dos causas —entrar con otra cuenta que si se tiene, o no tener ninguna en la direccion invitada— no se pueden separar porque el preview oculta a proposito el correo destinatario, asi que el mensaje nombra las dos salidas y el enlace acompana solo a este motivo. `RN-036` sigue intacta —no se toco ni un fichero del flujo de autenticacion— y se le anade una aclaracion: Cuenta de Google no es lo mismo que direccion de Gmail; lo que la regla limita es el **proveedor**, no el dominio del correo. Pendiente de la comprobacion del PO en navegador y sobre un correo real. | medio | no | MVP-712 | resuelto | MVP-712 |
| P-090 | 2026-08-06 | MVP-999 / revision del MVP | ux/movil | **En movil los datos quedan por debajo del pliegue en las vistas de listado.** Verificado en navegador a 375x812 sobre Cosechas: cabecera de seccion + tarjeta de titulo con su descripcion + boton de alta + **tres** tarjetas de resumen apiladas a ancho completo + **tres** filtros apilados suman ~780 px antes de la primera fila de datos, de modo que la pantalla completa se ocupa con contexto y hay que desplazarse para ver una sola cosecha. El mismo patron esta en Diario (cinco tarjetas de resumen y cinco filtros) y en Compras. En escritorio no se nota porque el resumen y los filtros van en rejilla. Es la contraparte movil de `P-086`: la misma composicion se apila en vez de repartirse. Propuesta: resumen en una sola fila desplazable o plegable, y filtros detras de un desplegable, dejando los datos arriba. **Resuelto en MVP-702 (2026-08-08)**: resumen en fila desplazable y filtros plegados a una accion en movil, con `MobileDisclosure`. Ojo a la trampa que costo una correccion: la primera version renderizaba los hijos **dos veces** (`sm:hidden` mas `hidden sm:block`), duplicando identificadores y rompiendo la relacion `label`/campo. Se resolvio con un unico arbol —CSS para el resumen, consulta de medios para los filtros— y hay test que lo fija. | medio | no | MVP-702 | resuelto | MVP-702 |
| P-091 | 2026-08-06 | MVP-999 / revision del MVP | ux/tecnico | **El producto no reacciona de ninguna forma a la perdida de conectividad.** Confirmado por busqueda: cero referencias a `navigator.onLine`, sin service worker y sin ninguna cola de reintento. `ADR-0002` decide «online-first» y esto es coherente con esa decision, asi que **no es un defecto**; pero el usuario objetivo trabaja en campo, donde la cobertura falla, y hoy un corte se traduce en un «No se pudieron cargar…» generico y, si estaba escribiendo, en perder lo escrito al fallar el guardado. Se registra para decidir el minimo aceptable —detectar la caida y decirlo, y no perder el contenido del formulario— frente a offline real, que es una reescritura del modelo de datos del cliente y no es alcance de unos ajustes. **Resuelto en MVP-709 (2026-08-08)**: `NetworkError` que **hereda de `HttpError`** a proposito, porque media aplicacion esta escrita como `error instanceof HttpError ? error.message : generico` y con una clase aparte todas esas pantallas habrian caido en el texto que el punto prohibe. Aviso persistente en el shell que se retira solo. Y un hallazgo mas grave que el punto: habia **tres** sitios donde una red caida se leia como sesion invalida —el refresco programado, `getAccessToken` y el arranque con token guardado—, asi que un corte de cobertura cerraba la sesion y se llevaba lo tecleado. Verificado parando el proceso de la API con el formulario abierto y 1847 kg escritos: se conservan, el aviso aparece, la sesion sobrevive y al volver la API el mismo boton guarda. | medio | no | MVP-709 | resuelto | MVP-709 |
| P-092 | 2026-08-06 | MVP-999 / revision del MVP | funcional/ux | **La Vision General no dice nada de dinero.** `RN-009` fija cuatro widgets obligatorios —resumen de temporada, kg por destino, kg por terreno y evolucion de rendimiento— y ninguno es economico, asi que el panel que resume la campana no publica ni el gasto. La cifra **si existe y ya se calcula**: el diario unificado rotula «GASTO 390,00 €» en su cabecera de totales. Resultado: el gasto de la campana se ve en la vista de registro cronologico y no en la de resumen, que es donde se busca. Junto con `P-084` (sin ingresos) hace que «¿me ha salido a cuenta?» no tenga respuesta en el producto. Alcance minimo y barato: llevar el gasto de la temporada a la Vision General. Alcance completo: margen, que depende de `P-084`. **Resuelto en MVP-707 (2026-08-08)**: quinto widget de `RN-009`, con gasto e ingreso. El gasto ya existia y ya se calculaba —el diario lo rotula en su cabecera—, pero se veia en la vista de registro cronologico y no en la de resumen, que es donde se busca. El panel **pregunta al diario** por el gasto en vez de recalcularlo, para que panel y diario no puedan divergir; es la leccion de `P-082`. Verificado en MVP-799: con una imputacion viva, el gasto **no duplica** el coste de una compra ya contada. | medio | no | MVP-707 | resuelto | MVP-707 |
| P-093 | 2026-08-08 | MVP-007 / MVP-716 | documentacion | **Los checklists de ocho `tech-design.md` de epicas cerradas difieren la documentacion de modulo a una consolidacion que nunca llegaba.** `MVP-101` («Modulo Auth documentado en `docs/03-modulos/`»), `MVP-102`, `MVP-103`, `MVP-104`, `MVP-105` y `MVP-107` («se consolidara al cerrar la epica»), `MVP-201` («se consolidara con MVP-203») y `MVP-203` (marcado `[~]`, remitiendo a `P-020`). No eran olvidos: en su momento no se podian dar por hechos porque el modulo no existia todavia, y esa es exactamente la clase de casilla que se queda sin cerrar para siempre. **Resuelto en `MVP-716` (2026-08-08)**: los seis primeros los satisface la ficha `identidad-y-workspaces` y los dos ultimos `maestros-operativos`; comprobado que cada ficha enlaza las historias que difirieron antes de marcar. Decision del PO: se marcan en la propia rama en vez de dejarlos abiertos. | bajo | no | MVP-716 | resuelto | MVP-716 |
| P-094 | 2026-08-08 | MVP-007 / MVP-716 | documentacion/arquitectura | **`docs/02-arquitectura/componentes.md` describe un sistema anterior al que hay.** Detectado al recorrer el codigo para repartir los modulos. `actualizado_en: 2026-07-24`, con lo que su tabla de interfaces no lista `/api/v1/diary` (MVP-305), `/api/v1/consumptions` (MVP-304), `/api/v1/tasks` (MVP-205), `/api/v1/account` (MVP-503), `/api/v1/ops` ni `/api/v1/telemetry` (MVP-601/602/603) —seis superficies vivas—, y las URLs de produccion siguen «pendiente de definir por entorno» pese a que el Hito F se publico el 2026-08-05 en `app.terrenario.com`. No lo arregla `MVP-716`: su alcance es el catalogo de modulos, y `02-arquitectura/` es otro bloque de la KB. Riesgo real: es de los primeros documentos que lee quien se incorpora, y hoy le da un mapa incompleto. **Resuelto (2026-08-09)**. Lo saco a la luz la propia comprobacion de `P-096` en su primera ejecucion: tenia destino `MVP-799` y esa historia se cerro sin recogerlo, que es el patron de `P-055` repitiendose —la revision filtro por las filas en `aprobado-crear-historia` y no miro las `pendiente`—. `componentes.md` se reconstruyo **leyendo los atributos `[Route]` de los controladores**, no la documentacion anterior: la tabla pasa de 10 a 21 superficies, con las seis que faltaban y cinco mas que tampoco estaban (`auth`, `workspace-members`, `workspaces/reactivations`, `feedback`, `health`). Aparecieron ademas dos inexactitudes que el punto no mencionaba: decia **.NET 10** en dos sitios cuando el proyecto es `net9.0`, y el servicio de correo figuraba como «cuenta pendiente de provisionar». La URL de produccion pasa a `https://app.terrenario.com`, con la nota de que API y cliente comparten origen. | medio | no | chore/reglas-que-nadie-comprueba | resuelto | chore/reglas-que-nadie-comprueba |
| P-095 | 2026-08-08 | MVP-007 / MVP-702 | ux/responsive | **A 768 px exactos el contenido se queda en 448 px y las tablas anchas piden desplazamiento horizontal.** Detectado al barrer los cuatro anchos de `MVP-702`. La causa no es la cota de ancho: es que el lateral aparece justo en `md:` (768 px) y se lleva 256, de modo que al contenido le quedan 448 —menos que en movil mas un poco— mientras la tabla de Cosechas necesita 917. **Medido tambien en `develop`: 448 y 917 igual**, asi que es anterior a `MVP-702` y esa historia no lo introduce ni lo empeora. La tabla desplaza dentro de su tarjeta, asi que no rompe la maqueta, pero en tableta en vertical se leen tres columnas de ocho. Arreglarlo pasa por mover el punto de corte del lateral a `lg:` —o por plegarlo a iconos en ese tramo—, y **cambiar la navegacion lateral esta fuera del alcance de `MVP-702`** por decision del propio spec. **Triado en la revision del MVP (2026-08-10): a la epica de ajustes, ampliado a movil.** La medicion a 375 px muestra que mover el punto de corte del lateral no lo arregla: la misma tabla mide **891 px dentro de un contenedor de 341**, y Cosechas es la **unica lista operativa sin maqueta adaptada** —las demas usan tarjetas—. Se trata como «poner Cosechas a la altura de las demas listas», que resuelve los dos anchos de una vez y es lo que exige `RT-01`. **Resuelto en `MVP-803` (2026-08-10)**: el punto de corte del lateral pasa de `md:` a `lg:`, de modo que en 768-1023 px se usa el menu desplegable y el contenido pasa de **448 a 689 px** utiles (+241; los 15 px que faltan para los +256 teoricos son la barra vertical que aparece al alargarse la pagina). Las listas que no caben pasan a **tarjeta** por debajo de ese corte, con una pieza comun (`RecordCard`) para que las cuatro se lean igual. **La medida amplio el alcance**: el `spec` daba por hecho que Compras «ya tenia maqueta adaptada» y no era cierto -su tabla mide 881 px en un contenedor de 341, el mismo defecto-, asi que entra tambien, con su bloque de consumos (decision del PO, 2026-08-10). Los maestros si estaban bien: cero desbordes medidos en Terrenos y en Miembros. A 1024 y a 1440 px nada cambia respecto a `MVP-702`, comprobado con las dos medidas. | bajo | no | MVP-008 / MVP-803 (ampliado a movil) | resuelto | MVP-803 |
| P-096 | 2026-08-08 | MVP-007 / MVP-799 | proceso/gobernanza | **El registro de puntos no esta respaldado por el gate, y por eso se pierden puntos.** Hallazgo `R-01` de `MVP-799`. Al cerrar la epica, **quince** filas seguian en `aprobado-crear-historia` con su historia de destino ya `completado` y mergeada: `P-021`, `P-055`, `P-072`, `P-075`, `P-078`, `P-081`, `P-082`, `P-083`, `P-084`, `P-085`, `P-086`, `P-087`, `P-090`, `P-091` y `P-092`. Ninguna era funcionalidad perdida —se verificaron una a una y las quince estaban construidas—, pero el registro llevaba semanas mintiendo, y **es exactamente el patron que perdio `P-055`**: se anota el destino, la historia de destino se cierra sin recogerlo y nadie vuelve a mirar. Que `P-055` estuviera ya construido y su fila siguiera diciendo «pendiente de crear historia» demuestra que el registro no sirve como fuente de verdad mientras dependa de que alguien se acuerde. Cinco historias si lo hicieron bien (`P-057`, `P-058`, `P-080`, `P-088`, `P-089`), asi que el proceso existe: lo que falta es que algo lo compruebe. Propuesta: que `validar_kb.py` falle cuando un punto con destino a una historia en estado `completado` no este en `resuelto`, igual que ya falla con los indices desactualizados. Coste bajo —los dos datos ya viven en la KB— y cierra el `CA-6` de la epica de forma estructural en vez de por diligencia. **Resuelto (2026-08-09)**: `validar_registro_de_puntos()` en `validar_kb.py`, bloqueante por decision del PO. Falla cuando un punto que no esta `resuelto` ni `descartado` apunta a una historia cuyo `spec.md` esta en `completado`. **El error se imputa al `spec.md` de la historia, no al registro**, y eso es lo que lo hace funcionar: en modo `--solo-cambios` un hallazgo sobre un fichero que el PR no toca se degrada a aviso, y el PR que cierra una historia toca su spec pero casi nunca el registro; atribuyendolo al registro, la comprobacion no bloquearia nunca y seria otra regla que nadie comprueba. Las filas se leen indexando **desde el final** porque las descripciones son largas y pueden llevar barras verticales escapadas. **Caza un caso real en su primera ejecucion**: `P-094`, que `MVP-799` habia dejado sin recoger. **Punto ciego corregido(2026-08-09)**: la primera version solo miraba los destinos que nombran una historia, porque se apoyaba en el `estado` de su `spec.md`. Los once derivados de `MVP-799` fueron a ramas de tipo *chore*, que no tienen spec, y **tres se quedaron en `pendiente` con el trabajo ya en `develop`** sin que nada lo dijera: el mismo patron, repetido dias despues de construir la guarda contra el. Se cierra por vocabulario y no preguntandole a git —una rama borrada tras el merge no se puede consultar de forma fiable en un clon superficial—: una rama es **donde se hace** el trabajo, no un plan, asi que en una fila abierta el destino solo puede ser una historia o «por decidir», y la rama va en la columna de historia, que es su sitio. La comprobacion cazo las tres filas en cuanto se anadio. | alto | no | chore/reglas-que-nadie-comprueba | resuelto | chore/reglas-que-nadie-comprueba |
| P-097 | 2026-08-08 | MVP-007 / MVP-799 | documentacion/tooling | **La regla de BOM obligatorio en los `.md` no la comprueba nadie, y 74 de 210 ficheros no la cumplen.** `docs/AGENTS.md` exige UTF-8 **con BOM** en todos los `.md` y dice «nunca crear un `.md` sin BOM». Medido leyendo los 210 ficheros de `docs/` byte a byte: **74 no lo llevan**, entre ellos `03-modulos/maestros-operativos/README.md`, `05-infraestructura/runbooks/revision-operativa.md` y varios `spec.md` y `tech-design.md`. El validador lee con `utf-8-sig`, que tolera las dos formas, y **no lo comprueba en ningun momento**, asi que la regla lleva sin respaldo desde el principio. Consecuencia practica: al editar uno de esos ficheros se le aplica el BOM y su diff incluye un cambio en el byte 1 que no es de contenido y confunde en la revision —paso en `MVP-712`—. Dos salidas coherentes: comprobarlo en el validador y normalizar los 74 de una vez, o **retirar la regla** si lo unico que la sostiene es la costumbre. Lo que no puede quedarse es una regla escrita que incumple el 35% del arbol. **Resuelto (2026-08-09)**: el PO decidio **retirar la regla**, no hacerla cumplir. Siete epicas con un tercio del arbol incumpliendola sin que se rompiera nada es la evidencia de que no hace falta; los scripts leen con `utf-8-sig`, que acepta las dos formas. `docs/AGENTS.md` pasa a exigir UTF-8 a secas y deja escrito el porque, para que nadie la reponga por costumbre. **No se normalizaron los 74 ficheros**: habria sido un commit tocando un tercio del arbol sin cambiar una palabra. | medio | no | chore/reglas-que-nadie-comprueba | resuelto | chore/reglas-que-nadie-comprueba |
| P-098 | 2026-08-08 | MVP-007 / MVP-799 | tooling/higiene | **Los once ficheros de `artifacts/correos/` estan versionados contra lo que dice el propio `.gitignore`.** `git ls-files artifacts/` devuelve los once —`LEEME.txt` y los cinco correos en `.html` y `.txt`—, mientras `.gitignore` ignora `artifacts/` con este motivo escrito al lado: «es salida, no contenido: versionarla obligaria a revisar un diff de HTML generado en cada cambio de plantilla». Entraron antes de la regla, y `.gitignore` no afecta a lo ya rastreado. Es justo lo que la regla queria evitar y **ya esta pasando**: el PR de `MVP-712` incluyo un diff de `invitacion-a-workspace.{html,txt}` que nadie necesitaba revisar, y cada `dotnet test` los regenera y ensucia el arbol. Arreglo: `git rm --cached -r artifacts/correos/`. Conviene decidir antes un efecto lateral: quien quiera ver un correo tendra que ejecutar los tests para generarlo, que es justo lo que la regla asume. **Resuelto (2026-08-09)**: `git rm --cached -r artifacts/correos/`, los once. Los ficheros siguen en disco y los regenera `ProductEmailPreviewTests`, que es como el propio `.gitignore` asume que se consultan. Efecto lateral aceptado por el PO: para ver un correo hay que ejecutar los tests, ya no se puede abrir desde GitHub sin clonar. | bajo | no | chore/reglas-que-nadie-comprueba | resuelto | chore/reglas-que-nadie-comprueba |
| P-099 | 2026-08-08 | MVP-007 / MVP-799 | tecnico/sesion | **Arrancar con un token guardado no programa el refresco de sesion.** Descubierto en `MVP-709` al montar la prueba de regresion: el escenario montado por ese camino no llegaba a disparar el temporizador. El motivo es que el arranque de `AuthContext` —token en `sessionStorage` mas `getMe`— despacha `LOGIN_SUCCESS` **sin llamar a `scheduleRefresh`**; solo lo programa `login()`, que es el retorno de Google. Consecuencia: en una pestana recuperada el token vive lo que le quede y no se renueva por su cuenta; cuando caduca, la primera peticion se va en 401. Hoy queda tapado porque `getAccessToken` refresca cuando no hay token en memoria, asi que no es un fallo visible, pero **la sesion depende de un camino de rescate en vez del ciclo previsto**. `MVP-709` no lo empeora y arreglarlo toca el ciclo de sesion, asi que se registra en vez de resolverse alli. **Resuelto (2026-08-09)**: el arranque con token guardado programa ya el refresco. La vida restante se lee del **`exp` del propio token** en vez de refrescar en cada carga para obtener un `expires_in` fresco: eso anadiria una fila a `refresh_tokens` por cada carga de pagina, y la rotacion ya genera miles al ano por usuario activo (`RN-041`). No se valida nada del token al leerlo, ni hace falta: el `exp` solo decide **cuando** programar, y quien decide si el token vale es el servidor en cada peticion. Sin `exp` legible o ya caducado se refresca de inmediato, que es lo que hacia el camino de rescate pero de forma explicita y sin esperar a que falle una peticion del usuario. Cuatro tests nuevos, comprobados **rompiendo el arreglo a proposito**: los cuatro fallan sin el y los de `MVP-709` siguen pasando. | medio | no | chore/deuda-menor-del-cliente | resuelto | chore/deuda-menor-del-cliente |
| P-100 | 2026-08-08 | MVP-007 / MVP-799 | tecnico/configuracion | **Un valor no reconocido en `Email:SecurityMode` cambia el modo de transporte en silencio.** Detectado en `MVP-799` al montar el envio real del canal de feedback: se configuro `SecurityMode=None` con mayuscula y `ResolveSecurity` —que compara contra constantes en minuscula (`starttls`, `ssl`, `none`, `auto`)— cayo en su rama por defecto, StartTLS, sin decir nada; el sintoma fue un `NotSupportedException` de MailKit al pie del envio. **Falla del lado seguro**, que es lo unico que lo mantiene en severidad baja: el defecto es el modo mas exigente de los comunes. Pero un error de tecleo en `Email__SecurityMode` de App Service cambia el comportamiento de la conexion sin que nada lo advierta, y el diagnostico aparece en la primera entrega fallida y no al arrancar. Propuesta: normalizar a minusculas y avisar al arrancar cuando el valor no sea ninguno de los cuatro, con el mismo criterio que ya se usa para el buzon de alertas y el de feedback. **Resuelto en `chore/deuda-menor-del-backend` (#92, 2026-08-09)**: el valor se normaliza (`Trim` mas minusculas) en `EmailOptions` y **no en el mailer**, para que la guarda del arranque y el transporte decidan por la misma regla en vez de por dos copias. El arranque avisa si no es ninguno de los cuatro, diciendo el recibido, el que se aplicara y el catalogo valido —mismo patron que `Ops:AlertEmail` y `Feedback:Recipient`—. Ocho tests: `None`, `StartTLS`, `ssl` con espacios alrededor y `AUTO` se reconocen y normalizan; `tls`, `starttls!` y la cadena vacia quedan marcados como desconocidos. | bajo | no | chore/deuda-menor-del-backend | resuelto | chore/deuda-menor-del-backend |
| P-101 | 2026-08-08 | MVP-007 / MVP-799 | deuda/frontend | **`formatDate` esta duplicado en siete ficheros del cliente**: `DiarioView`, `CosechasView`, `ComprasView`, `ConsumptionFormModal`, `TemporadasView`, `SeasonSetupPage` e `InvitePeoplePage`, cada uno con su propia definicion. No hay divergencia visible hoy, pero es la clase de duplicado que la produce: basta con que alguien ajuste el formato en la vista que esta tocando. Extraerlo a `lib/` es mecanico; se registra en vez de hacerse a mitad de historia porque toca siete ficheros de cuatro modulos distintos y ensuciaria el diff de cualquiera de ellas. **Resuelto (2026-08-09)**, y no era el duplicado que parecia. Al juntar las siete copias resulto que habia **tres formatos** distintos y **dos formas de parsear**, una con un defecto latente: `SeasonSetupPage` formateaba fechas de negocio (`YYYY-MM-DD`) con `new Date(iso)`, que las interpreta como medianoche **UTC**; en Espana no se nota —vamos en UTC+1 o +2— pero en cualquier huso negativo el 1 de septiembre se pinta como 31 de agosto. `lib/fechas.ts` separa lo que de verdad distingue los casos: `fechaDeNegocio` para un dia del calendario, parseado por componentes, y `fechaDelInstante` para un momento con hora y huso —la caducidad de una invitacion ocurre en un instante, no en un dia—. Los tres formatos se **conservan**: cambiar como se ve una fecha en cuatro pantallas no es lo que pedia el punto. | bajo | no | chore/deuda-menor-del-cliente | resuelto | chore/deuda-menor-del-cliente |
| P-102 | 2026-08-08 | MVP-007 / MVP-799 | deuda/tests | **Dos clases de test declaran `_userId` y nunca lo asignan** (`CS0649` en `HarvestRepositoryPostgresTests` y `ActivityRepositoryPostgresTests`). El campo vale siempre `Guid.Empty`, asi que los registros que crean esas pruebas no tienen autor real. Inofensivo hoy —ninguna asercion depende de ese valor—, pero es un campo muerto que **aparenta** identificar al autor, y su aviso sale en cada compilacion, sumando al ruido que hace que se dejen de leer los avisos. **Resuelto en `chore/deuda-menor-del-backend` (#92, 2026-08-09)**: `private readonly Guid _userId = Guid.NewGuid();` en las dos clases, siguiendo el patron de sus tres hermanas del mismo tipo. No es para callar el aviso: los registros que crean esas pruebas pasan a tener autor. Se verifico que **no hay clave ajena sobre `created_by`**, asi que no hace falta sembrar el usuario. | bajo | no | chore/deuda-menor-del-backend | resuelto | chore/deuda-menor-del-backend |
| P-103 | 2026-08-08 | MVP-007 / MVP-799 | deuda/build | **Conflicto de versiones de EF Core en el proyecto de tests, en cada compilacion** (`MSB3277`): `Microsoft.EntityFrameworkCore.Relational` 9.0.1 frente a 9.0.18, y gana la 9.0.1 por ser la principal. Verificado en el log de `dotnet build`. Que gane la **mas antigua** es lo que lo hace algo mas que ruido: la suite se ejecuta contra una version distinta de la que llevaria el runtime si alguien alineara las referencias, asi que un fallo dependiente de version podria no verse en los tests. Se arregla alineando la referencia transitiva. **Resuelto en `chore/deuda-menor-del-backend` (#92, 2026-08-09)**. La causa se verifico con `dotnet list package --include-transitive`: `Terrenario.Api` llega a Relational 9.0.18 porque se la sube `EntityFrameworkCore.Design`, pero esa referencia es `PrivateAssets=all` y **no propaga**; al proyecto de tests solo llegaba la 9.0.1 de `Npgsql.EntityFrameworkCore.PostgreSQL`, y ganaba por ser la principal. Referencia explicita anadida al `.csproj` de tests. `MSB3277` pasa de 22 lineas a **cero**, confirmado en el log y no dado por hecho. | bajo | no | chore/deuda-menor-del-backend | resuelto | chore/deuda-menor-del-backend |
| P-104 | 2026-08-08 | MVP-007 / MVP-799 | ux/accesibilidad | **El drawer de navegacion de movil es el ultimo overlay sin trampa de foco.** `AppLayout` lo pinta como `fixed inset-0 z-50 md:hidden` con un velo que cierra al pulsar, pero sin `role`, sin `aria-modal`, sin `Escape` y sin apagar el fondo: con el abierto, el tabulador sigue recorriendo la pagina de detras. Es el mismo defecto que `P-055` en los once modales, en el unico sitio que quedo fuera. **No se migro en `MVP-704` a proposito**: su forma es otra —panel lateral a sangre, sin titulo ni cabecera— y meterlo en el `Modal` comun obligaria a un modo «lateral» que condicionaria el componente por un solo uso. Alcance menor que el de `P-055`: solo aparece por debajo de 768 px, donde el teclado es minoritario, pero afecta a quien navega con teclado en una ventana estrecha o con teclado bluetooth en tableta. **Resuelto (2026-08-09)**: `lib/use-capa-modal.ts` extrae de `Modal` las tres piezas que **no dependen de su maqueta** —`inert` sobre el fondo con su contador, trampa de foco en los extremos y restauracion al disparador— y el drawer pasa a `MobileNavDrawer`, con portal a `body`. El portal no es opcional: `inert` se aplica sobre `#root`, asi que un drawer pintado dentro se apagaria a si mismo. Se descarto darle a `Modal` un modo «lateral»: seria una segunda personalidad por un solo uso. Los 13 tests de `Modal` siguen pasando tras la extraccion y el drawer suma 8 propios. | bajo | no | chore/deuda-menor-del-cliente | resuelto | chore/deuda-menor-del-cliente |
| P-105 | 2026-08-09 | MVP-007 / MVP-799 | deuda/tests | **Tres avisos de nulabilidad en los tests de correos** (`CS8619` en `ProductEmailInventoryTests.cs:27` y `FeedbackEmailComposerTests.cs:31`, `CS8602` en `FeedbackEmailComposerTests.cs:84`). Los detecto el agente que cerro `P-100`/`P-102`/`P-103` al dejar el resto de la solucion sin avisos: eran los tres unicos que quedaban. No eran ruido. `HtmlBody`, `TextBody` y `Subject` son anulables en MimeKit, y los helpers los devolvian como no anulables: un correo del producto sin cuerpo o sin asunto **es un defecto** que estas pruebas existen para cazar, y tal como estaban habria salido como una referencia nula en la asercion siguiente en vez de decir que faltaba el cuerpo. **Resuelto (2026-08-09)**: se comprueba con `?? throw` en vez de callar la nulabilidad con `!`, asi el fallo nombra el correo y lo que le falta. `dotnet build -t:Rebuild` sobre `src/backend/Terrenario.sln` pasa a **0 avisos y 0 errores** y la suite sigue en 954. Da pie ademas al criterio general del PO: los avisos del compilador se corrigen siempre, salvo que el arreglo tenga consecuencias peores. | bajo | no | chore/avisos-del-compilador | resuelto | chore/avisos-del-compilador |
| P-106 | 2026-08-09 | Produccion / Hito G | funcional/frontend | **El canal de feedback decia «no hemos podido enviar tu mensaje» con el correo ya entregado.** Detectado por el PO el primer dia en produccion. La API responde `202 Accepted` **sin cuerpo** y el cliente HTTP solo contemplaba el `204`: `response.json()` sobre un cuerpo vacio lanza un `SyntaxError`, que **no** es un `HttpError`, asi que `FeedbackView` caia en su mensaje generico. En un canal de incidencias es el peor fallo posible —el usuario reintenta y manda el reporte dos veces— y ademas oculta que el envio si funciona. **Resuelto (2026-08-09)**: el cliente decide por **el cuerpo que hay** y no por una lista de codigos; anadir el `202` a la lista habria tapado este caso dejando vivo el siguiente. No lo cazo ningun test porque los dobles de `Response` de la suite solo definian `json()`, y un `Response` real siempre tiene `text()`: un doble que ofrece menos que el original no prueba el original. Corregido tambien el doble. Verificado de extremo a extremo contra la bandeja de Mailtrap del proyecto: `202` y «Enviado» en pantalla. Se aprovecha para retirar `scripts/smtp-sink.py` y `ProductEmailDeliveryTests` —un receptor SMTP casero construido en `MVP-715` para hacer lo que Mailtrap ya hacia, por no haber leido `desarrollo-local.md`— y para enlazar desde `correos-del-producto.md` la guia que faltaba. | alto | no | fix/respuestas-correctas-sin-cuerpo | resuelto | fix/respuestas-correctas-sin-cuerpo |
| P-107 | 2026-08-10 | Revision del MVP (MVP-999) | bug/ux | **La Vision General se queda en blanco al cambiar de Workspace, y ademas pide crear una temporada que ya existe.** `RN-008` dice que un `season_id` que no exista en el Workspace **cae al defecto**, y da el motivo: desde `MVP-705` el filtro viaja en la URL y al cambiar de Workspace puede quedar el de otro. El diario, cosechas y compras lo cumplen; **el dashboard no**. Medido: `GET /api/v1/dashboard/summary?season_id=<de otro Workspace>` responde `200` con `scope.season: null` y todo a cero, mientras `GET /api/v1/diary` con el mismo id devuelve el ambito por defecto resuelto. Reproducido en el navegador: estando en «Rafa» —3 campanas, 4 partidas vivas, 4.460,50 kg— con `?season_id=` de otro Workspace en la URL, la pantalla muestra el estado vacio «Todavia no hay temporada que mirar. Crea o activa una temporada» **mientras su propio selector lista las tres campanas**. Basta con cambiar de Workspace desde la Vision General para provocarlo: `switchWorkspace` (`WorkspaceContext.tsx`) reemite la sesion y remonta el area operativa, pero **no limpia la query**. El mismo camino afecta a `plot_ids`: con terrenos de otro Workspace el ambito queda en `plots: 0` y el panel sale igual de vacio. Es la clase de `P-082`: dos pantallas del producto respondiendo distinto a la misma pregunta. **Decision del PO (2026-08-10, entrevista de triaje): a la epica de ajustes.** Es un bug con regla ya escrita, no funcionalidad nueva. **Resuelto en `MVP-801` (2026-08-10)**: `DashboardScopeResolver` cae al defecto de `RN-008` cuando la temporada pedida no es del Workspace, y cuando la interseccion de `plot_ids` queda vacia cae en todos los terrenos activos. Verificado contra la API real con un `season_id` de otro Workspace: `GET /dashboard/summary` y `GET /diary` devuelven ahora el **mismo** `scope.season` (antes `null` frente a la campana de trabajo), y con `plot_ids` ajenos el ambito pasa de `plots: 0` a los 2 terrenos activos. Ademas `RequireWorkspace` limpia `season_id`/`plot_id`/`plot_ids` de la URL al cambiar de Workspace, que es el camino que lo producia. `RN-008` recoge las dos precisiones. | alto | no | MVP-008 / MVP-801 | resuelto | MVP-801 |
| P-108 | 2026-08-10 | Revision del MVP (MVP-999) | bug/ux | **El filtro del diario dice «Todas las temporadas» cuando en realidad esta filtrando por una.** Con `/app/diario?season_id=<de otro Workspace>`, el servidor aplica correctamente el defecto (`meta.scope.season` = «Campana 2025», 1 registro de los 35 del Workspace) pero el `<select>` de temporada muestra **«Todas las temporadas»**. La pantalla afirma algo falso sobre lo que se esta viendo, que es peor que no decir nada. Causa localizada en `lib/season-scope.ts`: `value` da por buena cualquier seleccion explicita (`isExplicit ? selection : applied`), y desde `MVP-705` la seleccion del diario viene de la URL; como ese id no esta entre las `<option>`, el `<select>` cae en la primera, que es «Todas». Cosechas y compras **no** lo sufren porque su seleccion vive en estado local y arranca vacia: verificado que ambas muestran «Campana 2025» con el mismo id ajeno en la URL. Se resuelve reconciliando la seleccion con el ambito que devuelve el servidor. Hermano de `P-107`: el mismo escenario, el otro lado. **Decision del PO (2026-08-10): a la epica de ajustes, en la misma historia que `P-107`.** Es el mismo escenario por el otro lado y el arreglo vive en la misma pieza; separarlos dejaria media incoherencia viva. **Resuelto en `MVP-801` (2026-08-10)**: `useSeasonScope` registra **para que seleccion** se recibio el ambito aplicado, y con eso puede distinguir «el servidor no me ha hecho caso» de «todavia no ha contestado». Si el servidor aplico otra cosa, manda la aplicada y la seleccion se corrige **sustituyendo** la entrada de historial (`onCorrect`), para que «atras» no devuelva a la URL con el ambito ajeno. La Vision General adopta la misma pieza en vez de resolverlo por su cuenta con `seasonParam ?? scope.season.id`, que era la expresion equivalente al defecto; sus `plot_ids` se reconcilian igual, conservando los que el servidor honro. | medio | no | MVP-008 / MVP-801 (con P-107) | resuelto | MVP-801 |
| P-109 | 2026-08-10 | Revision del MVP (MVP-999) | ux | **Cosechas y Compras pierden sus filtros al recargar y no se pueden compartir por enlace.** `RN-007` («la recarga mantiene los filtros activos del usuario») se materializo en la URL solo para dashboard (`MVP-405`) y diario (`MVP-705`, tras `P-072`, «es donde mas duele»). Las otras dos vistas operativas se quedaron fuera y su estado vive en memoria. Verificado: filtrar Cosechas por «Campana 2026» pasa la tabla de 1 a 4 filas y la URL sigue siendo `/app/cosechas`; al recargar vuelve a «Campana 2025». Cosechas tiene tres filtros (terreno, temporada, destino) y Compras los suyos, asi que no es un caso de un solo control. Decidir si `RN-007` rige en las cuatro vistas operativas o si se documenta la exclusion a proposito. **Decision del PO (2026-08-10): a la epica de ajustes**, secuenciado **despues** de `P-107`/`P-108`: llevar el filtro a la URL es justo lo que expone ese defecto, asi que hacerlo antes lo propagaria a dos vistas mas. **Resuelto en `MVP-802` (2026-08-10)**: los tres filtros de Cosechas (`plot_id`, `season_id`, `destination`) y los dos de Compras (`season_id`, `product`, aplicados por igual al libro y a sus consumos) pasan a la URL. La mecanica se extrae a **una sola pieza** (`lib/list-url-state.ts`) de la que `useDiaryUrlState` pasa a ser un envoltorio: copiarla habria dejado tres implementaciones de las mismas invariantes de higiene. Verificado en el navegador: elegir «Campaña 2026» en Cosechas pasa la tabla de 1 a 4 filas y la direccion la recoge; recargar la mantiene. De paso se corrige un efecto colateral no registrado: la busqueda de material de Compras disparaba **una peticion por pulsacion** —dos, contando los consumos— y ahora va rebotada a 350 ms sustituyendo la entrada de historial, como el diario. `RN-007` rige ya en las cuatro vistas operativas. | medio | no | MVP-008 / MVP-802 | resuelto | MVP-802 |
| P-110 | 2026-08-10 | Revision del MVP (MVP-999) | funcional/alcance | **`RU-24` (aviso de cosecha duplicada) esta marcado «Estado: MVP» y no se ha construido ni descartado.** El requisito dice: al crear una cosecha con mismo terreno, fecha, producto y unidad que una existente, mostrar aviso y permitir guardar igual. Verificado en el navegador: con la partida del 20 oct 2025 en «Matorral» ya registrada, abrir el modal y poner exactamente ese terreno, esa fecha y ese producto **no produce ningun aviso**. No existe logica de duplicados fuera de la unicidad de nombre de los maestros (`MVP-205`/`MVP-207`). Ninguna historia de `MVP-001..MVP-007` lo menciona, asi que no es una decision de alcance registrada: es un requisito que se perdio. **Decision del PO (2026-08-10): a la epica de ajustes.** El producto ya tiene dos avisos no bloqueantes del mismo tipo en esos formularios (`RN-023` y `RN-043`); este es un tercero, sin decisiones de producto abiertas. **Resuelto en `MVP-805` (2026-08-10)**: formalizado como **`RN-044`** y construido con la comparacion en **servidor** (`GET /api/v1/harvests/duplicates`), no en el formulario: el listado de Cosechas esta filtrado y el diario trae una pagina, asi que buscar en lo cargado daria un aviso que aparece o no segun lo que el usuario tuviera puesto. La comparacion es terreno + fecha + producto; quedan fuera el modo de entrada, los kilos y la temporada, cada uno con su motivo. Verificado contra la API real con la partida del 20 de octubre en «Matorral», que es el caso con el que se detecto el punto: avisa, nombra los 1.000 kg y su destino, se excluye a si misma al corregir, y una partida borrada deja de contar. `RU-24` recoge su entrega y la decision sobre «misma unidad». | medio | no | MVP-008 / MVP-805 | resuelto | MVP-805 |
| P-111 | 2026-08-10 | Revision del MVP (MVP-999) | funcional/alcance | **`RU-32`, `RU-33` y `RU-34` (sugerencias de tareas) estan marcados «Estado: MVP» y no existen.** Son tres requisitos encadenados: sugerir tareas segun epoca del ano y recurrencia historica sin repetir las ya hechas en la campana; marcar una sugerencia como «omitida» con motivo para refinar las siguientes; y convertirla manualmente en tarea planificada con fecha limite. Verificado: no hay modulo de sugerencias en `Domain/` ni en `Application/` —el unico `suggestions` del backend es el de materiales de compras (`RN-031`)— y ninguna epica los cita. Es, con diferencia, **el bloque funcional mas grande que la KB da por MVP y no esta**: implica planificacion de tareas, un concepto que el producto hoy no tiene (el catalogo de `MVP-205` es un maestro de nombres, no un plan). Decidir si el MVP los reclama, si pasan a ampliaciones o si se marcan como descartados en `definicion-requisitos-usuario.md`, que es lo unico que no se puede dejar como esta. **Decision del PO (2026-08-10): backlog post-MVP, y se corrige el requisito.** No es un defecto de la entrega sino alcance nuevo de tamano epica (entidad de plan, motor de recurrencia, senal de omision y superficie propia); meterlo en una epica de ajustes la convertiria en otra cosa. Lo que si entra en `MVP-008` es cambiar el estado de `RU-32/33/34` en `definicion-requisitos-usuario.md` para que dejen de figurar como MVP, junto al trabajo de `P-114`. **Correccion documental hecha en `MVP-809` (2026-08-10)**: `RU-32`, `RU-33` y `RU-34` pasan de «Estado: MVP» a «Estado: Backlog post-MVP» en `definicion-requisitos-usuario.md`, con el motivo de la decision escrito en el bloque y con destino explicito a este punto en la matriz de trazabilidad. **La fila no pasa a `resuelto`**: lo entregado es la correccion del estado, no la funcionalidad, y la planificacion de tareas sigue siendo alcance de epica propia en backlog post-MVP. Marcarla resuelta diria que existe algo que no existe. | medio | no | Backlog post-MVP (planificacion de tareas); correccion documental en MVP-809 | backlog-post-mvp | MVP-809 (solo el estado del requisito) |
| P-112 | 2026-08-10 | Revision del MVP (MVP-999) | doc/cumplimiento | **La baja de cuenta se confirma con una frase tecleada; `RU-36` pedia un codigo enviado por email.** `MVP-505` eligio «una frase tecleada, no un clic, y se comprueba tambien en servidor», y esta bien razonado; pero `RU-36` («Confirmacion por codigo de email para borrado», Estado: MVP) no se cita en ningun sitio, asi que la divergencia no consta como decision sino como olvido. Es defendible mantenerla —la cuenta ya esta verificada por Google y el codigo anade un paso a un flujo irreversible pero voluntario—, y tambien es defendible que un borrado irreversible pida una prueba de posesion del buzon. Lo que no se sostiene es que el requisito siga diciendo una cosa y el producto haga otra. **Decision del PO (2026-08-10): se mantiene la frase tecleada y se corrige `RU-36`.** La frase ya cumple el requisito real —confirmacion explicita e informada, verificada en servidor— y el codigo por email anadiria un sexto correo del producto y un punto de fallo de entrega a un flujo que la persona inicia ya autenticada. Entra en `MVP-008` solo como correccion documental, dentro del trabajo de `P-114`. **Resuelto en `MVP-809` (2026-08-10)**: `RU-36` pasa a llamarse «Confirmacion explicita del borrado con frase tecleada» y describe lo que el producto hace de verdad —teclear `ELIMINAR MI CUENTA`, comprobado **tambien en servidor** en `AccountController` y no solo en el dialogo—, con la decision del PO y su motivo escritos en el propio requisito. El requisito sigue en «Estado: MVP» porque esta entregado; lo que se corrige es que describia un mecanismo que nunca se construyo. | bajo | no | MVP-008 / MVP-809 (solo correccion documental) | resuelto | MVP-809 |
| P-113 | 2026-08-10 | Revision del MVP (MVP-999) | funcional | **Se guarda quien creo y quien edito cada registro operativo, pero no hay forma de consultarlo.** `created_by`/`updated_by` y sus fechas existen en las cuatro tablas operativas (`activities`, `harvests`, `purchases`, `purchase_consumptions`), pero **cero apariciones** en `Application/`, en `Controllers/` y en todo el cliente: ni la API los devuelve ni la UI los muestra. `RU-21` («Se guardan usuario y fecha de la ultima edicion/creacion de cada registro critico», Estado: MVP) esta cumplido a medias, y la mitad que falta es la que sirve para algo. Pesa mas por `RN-034`: con permisos planos, cualquier miembro edita o borra el registro de cualquiera, y hoy nadie puede saber quien lo hizo. El dato ya esta escrito, asi que exponerlo es leer y pintar, sin migracion. **Decision del PO (2026-08-10): a la epica de ajustes.** Alcance minimo: mostrar quien registro y quien hizo la ultima edicion en el detalle de cada registro operativo, sin historico de cambios. | medio | no | MVP-008 / MVP-804 | triado | MVP-804 |
| P-114 | 2026-08-10 | Revision del MVP (MVP-999) | proceso/trazabilidad | **44 de los 47 requisitos de usuario no se citan en ningun documento fuera del que los define.** Contado sobre `RU-01..RU-47`: solo `RU-18`, `RU-19` y `RU-31` aparecen en otro fichero de la KB, y los tres por casualidad (los arrastran `P-017` y `P-029` de este mismo registro). Las epicas trazan contra `RN-xxx`, no contra `RU-xxx`, asi que el primer eslabon de la cadena que el propio roadmap declara como criterio de priorizacion —«maximizar trazabilidad requisito -> regla -> contrato -> validacion»— **no existe**. Es la causa comun de `P-110`, `P-111` y `P-112`: tres requisitos marcados MVP que nadie comprobo porque nada obliga a comprobarlos. Mismo patron que `P-096` (el registro de puntos que no comprueba nadie) en el otro extremo del ciclo. Alcance minimo: una matriz `RU -> RN/historia/estado` con una comprobacion en el gate de KB. **Decision del PO (2026-08-10): a la epica de ajustes**, con el mismo argumento de `P-096`: mientras dependa de que alguien se acuerde, se seguira perdiendo lo mismo. Alcance: matriz `RU -> RN/historia/estado` en el documento de requisitos y comprobacion en el pipeline de KB que falle si un `RU` marcado MVP no tiene destino. Ahi caen las correcciones de estado de `P-111` y `P-112`. **Resuelto en `MVP-809` (2026-08-10)**: la matriz `RU -> destino` esta al final de `docs/01-producto/definicion-requisitos-usuario.md`, con los 47 requisitos, su estado declarado, su destino y su estado real. Lo que la sostiene es `validar_trazabilidad_requisitos_usuario()` en `validar_kb.py`: falla si un requisito marcado «Estado: MVP» no cita en su destino una regla (`RN-xxx`), una historia (`MVP-xxx`), un punto del registro (`P-xxx`) o un ADR —prosa sin identificador no cuenta—, y falla si su destino son historias ya `completado` y el requisito no consta entregado, **imputando ese segundo error al `spec.md` de la historia** por el mismo motivo que `P-096`: en `--solo-cambios` el hallazgo sobre un fichero que el PR no toca se degrada a aviso, y el PR que cierra una historia toca su spec, no el documento de requisitos. El criterio de destino se apoya deliberadamente en identificadores que **otra** guarda ya vigila, asi que la cadena `RU -> P-xxx -> historia` queda cerrada por los dos extremos. El repaso de los 47 —hecho contra el codigo, no contra lo que se suponia— dejo 28 entregados, 5 entregados con hueco, 2 en curso (`RU-21` y `RU-24`) y 12 en backlog, y saco cuatro huecos nuevos: `P-119` a `P-122`. | medio | no | MVP-008 / MVP-809 | resuelto | MVP-809 |
| P-115 | 2026-08-10 | Revision del MVP (MVP-999) | rendimiento/ux | **La fuente de iconos pesa 3,78 MB y es el 68 % de la primera carga.** Medido sobre `npm run build`: `dist/assets` suma 5,57 MB, de los que **4,27 MB son tipografias** y **3,78 MB un unico fichero**, `material-symbols-outlined.woff2`. Todo el JavaScript y el CSS de la aplicacion suman 643 kB, es decir, los iconos pesan **casi seis veces** la aplicacion entera. Se descarga completo en la primera visita (confirmado en la pestana de red) porque es la fuente variable con el catalogo completo de Material Symbols, mientras el producto usa unas decenas de glifos. La autoalojacion es correcta y viene de `RN-042` (no transferir la IP del visitante a Google); lo que no se decidio fue el **subconjunto**. Importa por el usuario objetivo: `RT-01` exige que funcione bien en movil y `MVP-709` existe justamente porque estas personas trabajan con cobertura mala. Se corrige generando un subconjunto con los iconos usados, o pasando a SVG en linea (el proyecto ya tiene `public/icons.svg`). **Decision del PO (2026-08-10): a la epica de ajustes.** Se genera un subconjunto con los iconos usados (o se pasa a SVG en linea, que el proyecto ya tiene en `public/icons.svg`); no se toca la decision de autoalojar, que viene de `RN-042`. | medio | no | MVP-008 / MVP-810 | triado | MVP-810 |
| P-116 | 2026-08-10 | Revision del MVP (MVP-999) | bug/tecnico | **React avisa en cada carga de la aplicacion: «Cannot update a component (`DataScopeProvider`) while rendering a different component (`WorkspaceProvider`)».** Visible en la consola del navegador en cualquier entrada a `/app`. Causa exacta en `WorkspaceContext.tsx` (`applyActiveWorkspace`): la invalidacion se llama **dentro del updater** de `setActiveWorkspace`, y ese updater lo ejecuta React en fase de render, asi que `invalidateScope()` es un `setState` sobre otro componente durante el render. Hoy no rompe nada visible —`scopeVersion` es solo una clave de remontaje— pero es el patron que React declara invalido, en `StrictMode` el updater corre dos veces y en versiones posteriores esto escala de aviso a fallo. Los **256 tests del cliente pasan** sin detectarlo, porque ninguno mira la consola. Arreglo acotado: comparar contra el valor actual fuera del updater, o invalidar en un efecto. **Decision del PO (2026-08-10): a la epica de ajustes**, por el mismo criterio que ya rige para los avisos del compilador: se corrigen siempre salvo que el arreglo empeore algo. **Resuelto en `MVP-811` (2026-08-11)**: la comparacion sale del updater y se hace contra una referencia con el Workspace del ultimo render. **No** se movio a un efecto sobre `activeWorkspace?.id`: ese tambien se dispararia en la primera resolucion -de «no hay» a «este»-, que es justo lo que `DataScopeContext` explica que no debe remontar nada. Lo que aporta la historia es la prueba: `WorkspaceContext` no tenia cobertura propia y ahora espia `console.error`; comprobada **en rojo** sin el arreglo, con el mensaje literal del punto. La acompanan dos que fijan `CA-2` -invalida al cambiar de verdad, no invalida al resincronizar el mismo-, porque «quitar el aviso» se podria conseguir quitando la invalidacion. | medio | no | MVP-008 / MVP-811 | resuelto | MVP-811 |
| P-117 | 2026-08-10 | Revision del MVP (MVP-999) | tecnico/contrato | **Un 404 de enrutado responde con el cuerpo vacio.** `contratos-api.md` dice que las respuestas de error son «**siempre** JSON con `{ error: { code, message, details } }`». Verificado: `GET /api/v1/noexiste`, `DELETE /api/v1/seasons` y `GET /api/v1/plots/no-es-un-guid` devuelven `404` **sin cuerpo y sin `Content-Type`**. Los 404 de dominio si cumplen (`RESOURCE_NOT_FOUND`, `SEASON_NOT_FOUND`, comprobados). Impacto real bajo: ningun cliente propio llama a rutas inexistentes. Se registra porque es el mismo borde de transporte que `P-027` y `P-043` y porque un cliente que asuma el contrato al pie de la letra revienta al parsear. **Decision del PO (2026-08-10): a la epica de ajustes.** Es el mismo borde de transporte que cerro `MVP-502` para `P-027`/`P-043`, y el arreglo reutiliza el formateador de error existente. **Resuelto en `MVP-811` (2026-08-11)**: el fallback de `/api` escribe el envoltorio canonico con su `Content-Type`. En ese borde caen los tres casos -ruta inexistente, metodo no permitido y parametro que no cumple su restriccion- y los tres siguen respondiendo `404`: no se introduce un `405` que el contrato no declara, lo que cambia es que ahora dicen algo. Con dos guardas de no-regresion, porque «que todo error sea JSON» se puede aplicar de mas: los 404 de dominio conservan su codigo y su mensaje, y las rutas del cliente (`/app/…`) siguen sirviendo el SPA. `contratos-api.md` precisa que «siempre JSON» incluye el enrutado. | bajo | no | MVP-008 / MVP-811 | resuelto | MVP-811 |
| P-118 | 2026-08-10 | Revision del MVP (MVP-999) | ux/copy | **La pantalla de baja de cuenta llama «compartidos» a Workspaces donde la persona esta sola, y descuadra el numero con el adjetivo.** En `DeleteAccountPanel.tsx` el texto es «Sales de {n} Workspace(s) **compartidos**» con el adjetivo fijo: con un solo Workspace sale «Sales de 1 Workspace compartidos», y con varios en los que se es la unica persona —el caso verificado en el entorno de pruebas— afirma que se comparten cuando la misma pantalla dice mas arriba «Eres la unica persona en este Workspace». En un flujo irreversible, un texto que describe mal la situacion resta confianza justo donde hace falta. **Decision del PO (2026-08-10): a la epica de ajustes.** Correccion de una linea que arregla la concordancia y la afirmacion falsa a la vez. **Resuelto en `MVP-811` (2026-08-11)**: arreglar solo la concordancia no bastaba, porque **el cliente no tenia el dato**: la lista de membresias no dice cuanta gente hay en cada Workspace, asi que la pantalla lo afirmaba sin saberlo. Se anade `shared_memberships` a la previsualizacion de baja y el texto pasa a expresar los tres casos: «Sales de 1 Workspace compartido», «Sales de 1 Workspace» cuando la persona esta sola, y «Sales de 3 Workspaces, 2 de ellos compartidos». Una prueba por caso. | bajo | no | MVP-008 / MVP-811 | resuelto | MVP-811 |
| P-119 | 2026-08-10 | MVP-008 / MVP-809 | funcional | **`RU-06` promete filtrar el historico por tarea y ese filtro no existe.** El requisito dice «filtrar y consultar por terreno, temporada, **tarea** y trabajador», y la matriz de cobertura del propio documento lo da por «Cubierto». Verificado en el codigo: `DiaryFilter` (`Domain/Diary/IDiaryRepository.cs`) admite `PlotId`, `SeasonId`, `WorkerId`, `Types` y `Search`, y `ActivityFilter` (`Domain/Activities/IActivityRepository.cs`) admite `From`, `To`, `PlotId`, `SeasonId` y `WorkerId`: **ninguno de los dos tiene tarea**. En el cliente, `DiarioView` monta cinco controles (tipo, terreno, temporada, responsable y busqueda) y carga el catalogo de tareas solo para el aviso de nombre duplicado de `MVP-302`, nunca como filtro. La busqueda libre en servidor de `MVP-506` lo tapa a medias —encuentra el texto de la tarea— pero no es lo mismo: no acota por identificador ni sobrevive a un nombre guardado con otra grafia. Los otros tres filtros de `RU-06` si estan. Alcance estimado: un `task_id` en el filtro del diario y de actividades, mas el selector en la UI, reutilizando el catalogo que la vista ya carga. | bajo | no | Backlog post-MVP (ampliacion del filtro del diario) | pendiente | - |
| P-120 | 2026-08-10 | MVP-008 / MVP-809 | funcional/ux | **Al editar una actividad antigua, el selector de responsable no reofrece al trabajador si se ha inactivado.** `RU-29` dice que los inactivos «no se ofrecen en selectores pero aparecen en historico». El listado del diario cumple —proyecta `worker_name`—, pero el formulario de edicion no: `ActivityFormModal` pide `workerService.listWorkers({ isActive: true })` y **no reinyecta** el responsable actual de la actividad cuando ya no esta activo. El mismo modal si hace esa reinyeccion para la **tarea** inactiva, asi que el patron correcto ya existe al lado. Consecuencia: abrir a editar un registro viejo para cambiar las horas puede dejar el selector sin la persona que figuraba y guardar con otro responsable sin que nadie lo advierta, justo en el flujo que `MVP-804` va a hacer visible. Alcance estimado: replicar en el selector de responsable la reinyeccion que ya hace el de tarea. | medio | no | Backlog post-MVP (o oportunista en la proxima historia que toque `ActivityFormModal`) | pendiente | - |
| P-121 | 2026-08-10 | MVP-008 / MVP-809 | funcional/producto | **Las compras no tienen unidad de medida, asi que la cantidad y el precio unitario no dicen de que.** `RU-39` fija un estandar de unidades para el MVP y `RU-10` pide «cantidad total y coste total» con trazabilidad de «que se compro, donde se consumio y cuanto costo». Verificado: `Purchase` guarda `Product` (texto libre), `TotalQuantity`, `TotalCost` y un `UnitPrice` derivado, **sin ningun campo de unidad**, y la UI rotula la cantidad como «unidades». Un abono de 25 kg y un herbicida de 5 L se guardan igual, el precio unitario significa cosas distintas en cada fila y el reparto proporcional por terreno hereda la ambiguedad. No rompe nada hoy porque el consumo se expresa en la misma magnitud que la compra, pero cualquier lectura agregada por material es enganosa. Alcance estimado: una unidad por compra desde un catalogo corto (kg, L, unidad) heredada por sus consumos; decision de producto previa sobre si el catalogo es fijo o libre. | bajo | no | Backlog post-MVP (cluster de compras y consumos) | pendiente | - |
| P-122 | 2026-08-10 | MVP-008 / MVP-809 | funcional/alcance | **`RU-41` dice que se admite una actividad de duracion 0 y `RN-002` exige horas mayores que 0: se contradicen, y manda la regla.** Verificado en `Domain/Activities/Activity.cs`: la validacion rechaza `hours <= 0` (y tambien `hours > HoursMax`) con `ValidationActivityHoursRange`, y el cliente refuerza lo mismo con `min="0.25"`. El requisito, marcado «Estado: MVP», pedia justo lo contrario: «un registro de actividad puede tener duracion 0 (anotacion rapida sin tiempo)». No es un olvido de implementacion sino **dos documentos de la KB que dicen cosas opuestas** y nadie los cruzo, que es el patron que `P-114` describe. Hay que decidir: o se corrige `RU-41` porque el PO prefiere que una actividad sin tiempo no exista, o se corrige `RN-002` y se permite la anotacion rapida. En el mismo lote: `RU-44` dice «sin limite maximo de horas» y existe un tope de **999,99 h** que no es una regla de negocio sino la cota de la columna `decimal(5,2)`; es inocuo en la practica (41 dias en un solo registro) pero el requisito afirma algo falso. | bajo | no | Backlog post-MVP (decision de producto: `RU-41` frente a `RN-002`) | pendiente | - |

### Revision completa del MVP y triaje (2026-08-10)

Revision del MVP publicado contra el **sistema en marcha** —API y base de datos reales mas navegador
conducido—, no releyendo la KB. El contraste numerico de partida cuadro entero (4.460,50 kg, 930,65 L,
20,86 L/100kg, 17,16 kg/arbol y 390 € de gasto, todos verificados contra la base de datos), asi que los
hallazgos salieron de otro sitio: de **provocar los escenarios que la propia KB anticipa** y comprobar
si el producto se comporta como dice.

Doce puntos nuevos, `P-107` a `P-118`, mas el triaje de `P-095`, que llevaba desde `MVP-702` sin
destino. Los tres bloques en que caen:

| Bloque | Puntos | Origen del hallazgo |
|---|---|---|
| Contexto y filtros | `P-107`, `P-108`, `P-109` | Poner en la URL el `season_id` de otro Workspace, que es justo el escenario que `RN-008` dice cubrir |
| Requisitos marcados MVP sin construir | `P-110`, `P-111`, `P-112`, `P-113`, `P-114` | Contar cuantos `RU` se citan fuera del documento que los define: **3 de 47** |
| Calidad de entrega | `P-115`, `P-116`, `P-117`, `P-118` | Medir el peso real del `build`, leer la consola del navegador y probar los bordes del contrato |

**El hallazgo con mas filo es `P-107`**, y no aparecio leyendo codigo: la regla estaba escrita, tres de
las cuatro vistas la cumplian y la cuarta no, y eso solo se ve pidiendole al sistema la misma pregunta
por dos caminos distintos. El resultado es que la pantalla insignia del producto le dice al usuario que
cree una temporada mientras su propio selector lista las tres que ya tiene.

**El hallazgo de fondo es `P-114`**, y es de proceso, como lo fue `P-096` en la revision anterior: las
epicas trazan contra `RN-xxx` y nadie traza contra `RU-xxx`, de modo que un requisito puede quedarse
marcado «Estado: MVP» durante todo el roadmap sin que nada lo delate. `P-110`, `P-111` y `P-112` son
tres consecuencias del mismo agujero, no tres despistes independientes.

**Resultado del triaje (entrevista con el PO, 2026-08-10):** doce a la nueva epica de ajustes
—`MVP-008 — Ajustes MVP 02`, redactada el mismo dia con once historias funcionales mas su revision de
cierre— y uno a backlog. Solo `P-111` sale, y por tamano: sugerencias y planificacion de tareas es
alcance de epica propia —entidad de plan, motor de recurrencia sobre el historico, senal de omision y
superficie propia—, y meterlo entre ajustes convertiria la epica en otra cosa. Lo que si entra ya es
**corregir el estado de `RU-32/33/34` y de `RU-36`** en el documento de requisitos: un requisito que
dice una cosa mientras el producto hace otra es deuda que se paga en la siguiente revision.

Dos secuenciaciones que conviene respetar dentro de `MVP-008`: `P-109` va **despues** de
`P-107`/`P-108` —llevar los filtros a la URL es lo que expone ese defecto, asi que hacerlo antes lo
propagaria a dos vistas mas— y las correcciones documentales de `P-111` y `P-112` van **dentro** del
trabajo de `P-114`, que es el que monta la matriz que las hace verificables.

#### Repaso del backlog previo (los 24 puntos heredados)

Con el MVP ya publicado y en uso, se repasaron ademas **los 24 puntos que seguian en
`backlog-post-mvp`**, decididos casi todos **antes** de publicarlo. Seis de ellos estan registrados en
la propia KB como «resolver juntos», asi que se decidieron por unidad y no por fila: 24 puntos, 15
decisiones.

| Decision | Puntos | Motivo |
|---|---|---|
| Pasan a `MVP-008` | `P-036`+`P-041` (`MVP-806`), `P-048`+`P-049` (`MVP-807`), `P-011`+`P-029` (`MVP-808`) | Los tres tienen la **premisa cumplida** o un hueco sin alternativa: el «sin uso historico» de los maestros ya es comprobable, no existe ninguna via para abandonar un Workspace, y una decision irreversible depende hoy de que llegue un correo |
| Siguen en backlog | `P-019`, `P-022`, `P-032`, `P-035`+`P-054`, `P-044`+`P-047`, `P-059`..`P-063`, `P-064`, `P-070`, `P-073`, `P-074`, `P-077` | Ninguno tiene hoy consumidor real, y **siete de ellos exigen una decision de diseno previa** que a la escala actual del producto no se puede tomar con informacion: el discriminador de homonimos, la unidad de exportacion, el modelo de acceso de la pantalla de operacion y el modelo de produccion entero |
| Se cierra | `P-069` | No quedaba trabajo pendiente: comprobado que Smart App Control esta desactivado y que el CI corre en Linux. Ver la fila |

El criterio que separa las dos primeras columnas no es la severidad sino **si la decision se puede
tomar hoy con lo que se sabe**. `P-070` (exportacion) es un buen ejemplo de la segunda: con
explotaciones de una sola persona, exportar «la persona» y exportar «la explotacion» dan lo mismo, asi
que decidirlo ahora seria decidirlo sin el caso que lo decide. `P-044`/`P-047` es el mismo patron.

Tres consolidaciones que quedan anotadas por si cambia la prioridad: `P-022` (vincular una fila de
cuadrilla a una cuenta) pierde casi todo su motivo en cuanto exista la **fusion** de `P-036`/`P-041`;
`P-032` (nombre de visualizacion propio) es uno de los discriminadores candidatos de `P-047`; y `P-073`
solo cobra valor real si antes se decide `P-074`.

Se dejo constancia del repaso **en cada fila**, no solo aqui: un punto que sigue en backlog porque se
ha vuelto a mirar no es lo mismo que uno que sigue en backlog porque nadie lo miro, y esa distincion
es exactamente lo que `P-096` echaba en falta.

### Retriage de la 3a pasada de MVP-299 (2026-07-28)

Al cerrar `MVP-002` se revisaron **los 32 puntos pendientes** del registro con una pregunta concreta:
¿tiene ya dueño natural en alguna de las epicas por delante? El resultado reasigna 16 puntos y crea
dos historias nuevas. Las columnas `Destino propuesto` e `Historia creada` de la tabla ya lo reflejan;
aqui queda el criterio.

**Movidos a una epica pendiente (16 puntos).** Todos tenian consumidor identificable y solo estaban
aqui por no haberse revisado desde que se registraron:

| Destino | Puntos | Motivo |
|---|---|---|
| `MVP-501` (tests) | `P-012`, `P-023`, `P-031` | Su alcance ya es «tests unitarios, de integracion y smoke E2E». `P-012` y `P-023` son **el mismo punto registrado dos veces** desde MVP-106: se tratan como uno |
| `MVP-502` (hardening) | `P-027`, `P-043` | «Revision de validacion de entrada en bordes API» es literalmente su alcance, y los dos viven en el mismo borde: una sola pasada con un helper comun |
| `MVP-505` (nueva) | `P-008`, `P-024`, `P-033` | Ver abajo |
| `MVP-403`/`MVP-405` | `P-021` | Necesita datos de cosecha y el filtro por temporada. (`P-045` se adelanto y resolvio en **MVP-209** al reformularse como rediseño del modelo de temporadas) |
| `MVP-499` (revision de MVP-004) | `P-036`, `P-040`, `P-041` | Revisados al cierre: **`P-040` resuelto en MVP-499** (el Home pasa a ser la Visión General cuando la explotación está preparada); **`P-036`/`P-041` diferidos a post-MVP** (funcionalidad nueva, no defecto; su premisa «sin uso comprobable» ya se cumple) |
| `MVP-406` (nueva) | `P-025`, `P-037`, `P-046` | Ver abajo |
| `MVP-506` (nueva) | `P-051`, `P-052`, `P-056` | Ver abajo |
| `MVP-004` | `P-019` (parte de detalle) | Entregado en **MVP-407** (detalle de terreno con historico de cosechas y labores); la parte de ER sigue aqui |
| `MVP-303` | `P-050` | Alta de esta misma pasada |

**Tres historias nuevas.** Los grupos que no cabian en ninguna historia existente:

- **`MVP-505` — Cumplimiento funcional de salida** (`P-008`, `P-024`, `P-033`). Estaban registrados
  con destino «`MVP-005`/`MVP-502`», pero **ninguna historia de `MVP-005` los admitia**: `MVP-502` es
  hardening tecnico, `MVP-503` es revision documental con «nuevas politicas» fuera de alcance y
  `MVP-504` es el gate. Sin historia que los construya, `MVP-503` habria detectado el incumplimiento y
  `MVP-504` habria bloqueado la salida sin remedio. Pasan a **bloqueantes** y entran en `MVP-005` como
  correccion bloqueante, que es la excepcion que esa epica ya se reservaba.
- **`MVP-406` — Navegacion del area operativa** (`P-025`, `P-037`, `P-046`). Los tres tocan la misma
  superficie y los tres estaban anotados con «resolver en la misma pasada». Se hacen al cerrar
  `MVP-004`, cuando estan encendidos los diez modulos y el menu alcanza su tamaño definitivo.
- **`MVP-506` — Navegacion y escala del diario** (`P-051`, `P-052`, `P-056`). Los tres tocan `GET /diary`
  y estaban anotados como «la misma pasada»: paginacion (con la mezcla movida a SQL), busqueda en
  servidor y filtro por responsable. Se ubica en `MVP-005` (endurecimiento) —no en `MVP-003`, ya
  cerrado— porque el nucleo es escala/salida a MVP. Decision del PO (2026-07-30) tras la revision de
  `MVP-004`.

**Se quedan aqui (6 puntos), con motivo explicito:**

| Punto | Por que sigue en MVP-999 |
|---|---|
| `P-001` + `P-030` + `P-039` | Inventario y maquetacion unificada de los emails salientes. No bloquea salida y ninguna epica pendiente lo consume; `MVP-505` solo toca el correo que necesite la baja de cuenta |
| `P-011` + `P-029` | Generalizacion del centro de notificaciones in-app. Mejora de UX sin consumidor en el roadmap |
| `P-048` + `P-049` | Abandonar un Workspace y `can_revoke` con copropietarios. Ciclo de vida de la **membresia**: no rompe ningun CA, no bloquea a MVP-003/004 y es alcance nuevo |
| `P-020` | Consolidar el catalogo de `03-modulos`. Documentacion sin impacto funcional; candidato tambien a `MVP-504` |
| `P-022`, `P-032`, `P-035` | Mejoras diferibles de maestros y perfil, sin consumidor identificado |
| `P-044` + `P-047` | Homonimos de Workspace y desempate de responsables: legibilidad, no integridad |

### Clasificacion de la revision completa del MVP (2026-08-06/07)

Con el MVP ya publicado (`v0.6.0-hito-f`, 2026-08-05) se hizo una **revision completa conducida sobre
la aplicacion en marcha**, no sobre la KB: se verificaron los once puntos aportados por el Product
Owner, se anadieron cuatro mas y se reabrio uno. Despues se clasificaron **los 50 puntos abiertos**,
uno a uno los catorce nuevos y por bloques los treinta y seis heredados.

Resultado: **25 aprobados**, **24 a backlog post-MVP** y **1 descartado**.

**Los 25 aprobados entran en la epica `MVP-007 — Ajustes MVP 01`** (`Hito G — Ajustes de uso real`):

| Historia | Puntos |
|---|---|
| `MVP-701` — Coherencia de contexto: Workspace y temporada | `P-081`, `P-082`, `P-083` |
| `MVP-702` — Aprovechamiento del espacio: escritorio y movil | `P-086`, `P-090` |
| `MVP-703` — Arranque en el diario y definicion de sesion activa | `P-087`, `P-078` |
| `MVP-704` — Modales accesibles | `P-055` |
| `MVP-705` — Navegacion del diario en la URL | `P-072` |
| `MVP-706` — Comportamiento de la Vision General | `P-075`, `P-085` |
| `MVP-707` — Valor economico de la campana | `P-084`, `P-092` |
| `MVP-708` — Roces de captura en compras y consumos | `P-057`, `P-058` |
| `MVP-709` — Respuesta a la perdida de conexion | `P-091` |
| `MVP-710` — Identidad de marca y presencia del producto | `P-080` |
| `MVP-711` — Canal de feedback del usuario | `P-088` |
| `MVP-712` — Acceso con cualquier direccion de correo | `P-089` |
| `MVP-713` — Errores de OAuth y ruido en las alertas | `P-079` |
| `MVP-714` — Higiene de datos: retencion y secretos | `P-071`, `P-076` |
| `MVP-715` — Correos del producto: inventario y maquetacion | `P-001`, `P-030` |
| `MVP-716` — Consolidacion del catalogo de modulos | `P-020` |
| `MVP-799` — Revision de cierre de la epica MVP-007 | Cierre de las quince filas que seguian abiertas; alta de `P-096` a `P-104` |
| `chore/reglas-que-nadie-comprueba` | `P-094`, `P-096`, `P-097`, `P-098` |
| `chore/deuda-menor-del-backend` | `P-100`, `P-102`, `P-103` |
| `chore/deuda-menor-del-cliente` | `P-099`, `P-101`, `P-104` |
| `chore/avisos-del-compilador` | `P-105` |
| `fix/respuestas-correctas-sin-cuerpo` | `P-106` |

**Los 24 diferidos** quedan como `backlog-post-mvp`, agrupados por la evolucion a la que pertenecen:
centro de notificaciones (`P-011`, `P-029`), ciclo de vida de la membresia (`P-048`, `P-049`), borrado
y fusion de maestros (`P-036`, `P-041`), homonimos (`P-044`, `P-047`), campos de maestros no portados
(`P-032`, `P-035`, `P-054`), modelo de produccion (`P-059` a `P-063`), calidad y cumplimiento (`P-064`,
`P-069`, `P-070`), deuda de modelo y documentacion (`P-019`, `P-022`, `P-073`, `P-074`) y aviso de
caida total (`P-077`).

**El descartado es `P-039`** (avisar por correo de que una invitacion se ha anulado): basta con que se
avise al intentar unirse, que es lo que ya hace el preview del enlace.

#### Lo que ensena esta pasada

- **`P-055` demuestra que anotar un destino no construye nada.** Se marco «aprobado-crear-historia →
  `MVP-502`» y esa historia cerro sin recogerlo, porque su alcance era hardening de seguridad y PII, no
  accesibilidad. Nadie lo detecto hasta usar el producto. De ahi el `CA-6` de `MVP-007`: antes de
  cerrar la epica hay que comprobar que **cada punto aprobado tiene historia que lo recoge**, y que esa
  historia lo tiene escrito en su alcance.
- **Los hallazgos con filo salieron de comparar cifras, no de releer.** `P-082` aparecio al contrastar
  `GET /dashboard/summary` (4.460,50 kg · 4 partidas) con `GET /harvests` sin filtro (5.460,5 kg · 5
  partidas): la misma explotacion, el mismo dia, dos totales. Ninguna lectura de codigo lo habria dado.
- **Un punto puede estar «servido» y aun asi estar mal.** El favicon existia y respondia 200 en
  produccion; lo que fallaba era su contenido, que es el logo de Vite.

### Criterios de uso del registro

- `Estado de revision`: `pendiente`, `en-analisis`, `aprobado-crear-historia`, `backlog-post-mvp`, `descartado`.
- `backlog-post-mvp` se introduce en la revision completa del MVP (2026-08-07): el punto **se ha revisado y se ha decidido**, y la decision es diferirlo. No es lo mismo que `pendiente`, que significa que nadie lo ha mirado todavia.
- `Destino propuesto`: usar `MVP-999` solo si no encaja claramente en una epica activa o si es diferible.
- `Historia creada`: informar el ID final cuando el punto pase a historia formal.
- No borrar filas historicas: si un punto se descarta, conservarlo con motivo en `Descripcion breve`.

### Flujo al cierre de epicas

1. Cada historia `MVP-x99` revisa este registro y anade nuevos puntos detectados.
2. Se reevalua impacto y destino propuesto para cada punto pendiente.
3. Se crean historias nuevas para los puntos aprobados y se vincula el ID en `Historia creada`.
4. Se mantiene en `MVP-999` solo lo que siga pendiente de planificacion.

## Notas y decisiones

- Fundacion transversal de UI disponible tras MVP-107 (2a revision de MVP-001): el **sistema de
  diseño** (fuentes Inter + Plus Jakarta Sans / `.font-headline`, Material Symbols, utilidades;
  P-015) y el **shell de aplicacion** (`AppSidebar` + `AppTopbar` + `AppLayout` con los 8 modulos
  de navegacion, los de MVP-002..004 deshabilitados con "Pronto"; P-016) ya existen. Las epicas
  MVP-002..004 solo deben **encender** sus vistas sobre esa base, sin recrear shell ni tipografia.
- **Cluster «modelo de produccion» (post-MVP): `P-059` + `P-060` + `P-061` + `P-062` + `P-063`.**
  Los cinco salieron del cierre de la produccion en `MVP-401`/`MVP-402` y describen **una sola
  evolucion**, no cinco mejoras sueltas: la variedad baja al terreno (`P-059`), el producto sube al
  Workspace (`P-060`), aparece el maestro de almazaras y empresas compradoras con su rendimiento por
  defecto y su principal (`P-062`, que absorbe el override de densidad de `P-061`) y, con el tipo de
  cultivo ya existiendo, se hace configurable el rendimiento por defecto y el de visualizacion
  (`P-063`). Conviene priorizarlos juntos: hacerlos por separado obligaria a tocar el formulario de
  cosecha y los widgets del dashboard tres o cuatro veces. Ninguno bloquea el MVP —`RN-029` limita la
  produccion al nucleo operativo y `RN-013` fija una unidad unica— pero todos mejoran directamente la
  friccion de captura, que es el criterio con el que el PO los pidio.
- MVP-999 no debe utilizarse para ocultar deuda critica de una epica activa.
- Si un pendiente termina encajando en una epica existente, debe moverse a esa epica en la siguiente refinacion.
- Se acuerda usar MVP-999 como contenedor transversal para documentar los envios de email del producto y planificar una maquetacion coherente al cierre del roadmap principal.
