---
id: "v0.7.1-hito-g"
tipo: release
titulo: "Release v0.7.1-hito-g — El canal de feedback daba por fallido un envío que sí salía"
estado: publicado
fecha: "2026-08-09"
hito: "Hito G — Ajustes de uso real"
responsable: "@andres"
revisores: []
tickets: []
creado_en: "2026-08-09"
actualizado_en: "2026-08-09"
---

# Release v0.7.1-hito-g — Corrección del canal de feedback

> **Fecha**: 2026-08-09
> **Tipo**: patch — una corrección, sin funcionalidad nueva
> **Hito**: Hito G — Ajustes de uso real
> **Tag**: `v0.7.1-hito-g` · sobre [v0.7.0-hito-g](./v0.7.0-hito-g.md)

---

## Resumen

Corrige `P-106`, la primera incidencia que encontró un usuario real en producción — y la encontró
**usando el canal que la contó mal**.

El canal de sugerencias e incidencias decía «No hemos podido enviar tu mensaje» **con el correo ya
entregado**. En un canal de incidencias es el peor fallo posible: el usuario reintenta y manda el
reporte dos veces, y de paso deja de fiarse del único sitio donde puede avisar de que algo falla.

---

## Qué estaba mal

La API responde `202 Accepted` **sin cuerpo**. El cliente HTTP solo contemplaba el `204`:

```ts
if (response.status === 204) return undefined as T;
return (await response.json()) as T;   // SyntaxError con cuerpo vacío
```

Ese `SyntaxError` no es un `HttpError`, así que la pantalla caía en su mensaje genérico.

**No se corrige añadiendo el `202` a la lista.** Eso taparía este caso y dejaría vivo el siguiente:
cualquier respuesta correcta sin cuerpo. Ahora el cliente decide por **el cuerpo que hay**, que es lo
que la pregunta realmente era.

### Por qué no lo vio ningún test

Los dobles de `Response` de la suite solo definían `json()`. Un `Response` real **siempre** tiene
`text()`. Un doble que ofrece menos que el original no prueba el original, prueba otra cosa. Corregido
también el doble, que es lo que hace que el próximo fallo de esta familia sí se vea.

---

## Además: se retira el receptor SMTP casero

`scripts/smtp-sink.py` y `ProductEmailDeliveryTests`, construidos en `MVP-715` para hacer exactamente
lo que **Mailtrap ya hacía**. El proyecto lo tenía configurado en `Email:*` de user-secrets y
documentado en [desarrollo-local.md](../05-infraestructura/desarrollo-local.md), en una sección
titulada «Opción recomendada en local: bandeja de pruebas (Mailtrap)».

Fue un error de quien no leyó esa guía. Se retira el código y se anotan las cuatro referencias de la
KB: las historias cerradas no se reescriben, se deja dicho que el medio se retiró y por qué. Lo que se
comprobó con él sigue siendo válido.

**Y se tapa la laguna que lo permitió**: `correos-del-producto.md` no decía nada sobre cómo probar un
envío, y ahora enlaza la guía.

---

## Breaking changes

Ninguno. Tampoco migraciones ni configuración nueva.

---

## Verificación

De extremo a extremo contra la bandeja de Mailtrap del proyecto, que es como debió hacerse desde el
principio:

| Comprobación | Resultado |
|---|---|
| `POST /api/v1/feedback` | `202 Accepted` |
| Pantalla | «Enviado. Gracias por contarlo» |
| Correo | entregado en la bandeja |
| Tests | **953 backend** · **256 cliente** |

El test que reproduce el fallo se escribió **antes** del arreglo y se vio fallar.

---

## Estado de despliegue

**Publicado el 2026-08-09** en `https://app.terrenario.com`, con el tag `v0.7.1-hito-g` sobre el merge
commit `602fab0` de `main`.

| Aspecto | Estado |
|---|---|
| Disparador | Tag `v0.7.1-hito-g`. El merge a `main` no despliega |
| Gate de CI para ese commit | ✅ Verde antes de etiquetar |
| Aprobación del despliegue | ✅ Manual, por el Product Owner en el entorno `produccion` |
| Resultado | `Comprobar que el gate está en verde` ✅ · `Publicar aplicación` ✅ · `Smoke de publicación` ✅ |

### Verificación en producción

| Comprobación | Resultado |
|---|---|
| La salud responde y alcanza la base | ✅ `GET /api/v1/health` → `200` con `{"status":"healthy","database":"healthy"}` |
| El cliente se sigue sirviendo | ✅ `200` |
| **El canal confirma en vez de fallar** | ✅ Enviado desde «Sugerencias e incidencias» en producción: sale la confirmación, no el error |

### Y la evidencia que cierra el diagnóstico

**El reporte que se envió con `v0.7.0` sí había llegado al buzón.** Lo confirmó el Product Owner
mirándolo. Es la prueba de que el fallo estaba donde se dijo —en leer la respuesta, no en enviar— y de
que la corrección era la correcta y no un parche sobre un síntoma.

Vale la pena dejarlo escrito por lo que enseña: el envío funcionaba, la API respondía `202`, el correo
salía, y aun así **la pantalla decía que no**. Un fallo de lectura de la respuesta se ve exactamente
igual que un fallo de entrega, y el usuario no tiene forma de distinguirlos. Por eso el arreglo no fue
añadir un código a una lista, sino dejar de decidir por una lista.
